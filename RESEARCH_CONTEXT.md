# DoomDiT — Research Context

> Updated 2026-09-01 (evening, after the kickoff session). Entry point for every new chat in this repo.
> Read order: this file → `REQUIREMENTS.md` (what done means for the CoRL paper) → `.claude/analyses/DOOMDIT_AGENT_CONTEXT.md` (dense technical brief, verified against code July 15) → `.claude/analyses/doomdit-full-recap-2026-07.md` (full recap: literature through July 2026, venues, robotics extension) → `IDEAS.md` (scenario-latent skill files).

## 0. Where we are going now (as of Sep 25, 2026 19:30 EDT; everything below the Sep 25 block is history)

**Paper (CoRL 2026 PhysWM workshop, Sep 30 AoE, 4 pages; draft to Changliu Sunday Sep 27 evening, final tables Sep 28 to 29).** Authors Rohan Nagabhirava and Keerthana Chirumamilla. The spine, decided Sep 24 to 25:

1. **A matched-recipe comparison of three backbones as Doom world models** on one public dataset: SD 1.4 U-Net (4-channel latents), SD 3.5 Medium (DiT, 16-channel latents), PixArt-alpha (DiT, 4-channel latents); every tic (35 Hz), 32-tic context, executed-control history, v-prediction, 200k updates of batch 32 each. The DiT-versus-U-Net question stays as the main table; it is no longer the whole contribution, because the rows land within about 0.6 dB of each other and the findings came from how we evaluate.
2. **Evaluation against a copy-last-frame persistence reference on the same windows.** One tic ahead on held-out episodes of the training maps (persistence 21.57 dB / 0.203 LPIPS): U-Net 200k EMA 22.49 / 0.177 (+0.92 dB; +2.1 dB at four tics); SD 3.5 100k EMA 23.06 / 0.142 (+1.49; +2.18 at four tics; it passed the U-Net's final LPIPS at 40k and both its final numbers at 50k); PixArt 95k EMA +0.71 (+1.86 at four tics). SD 3.5's EMA 256-tic rollouts sit above the copy-seed baseline at every horizon. No 2024 to 2026 world-model paper reports such a reference (5.9 of the dossier); on unseen maps the U-Net is below it at one tic.
3. **The map-distance generalization study** (`.claude/analyses/distance-study-design-2026-09-24.md`, results frozen under `results/distance_study/`): each map is a motion-weighted cloud of per-frame SD 1.x latents, its distance is the sliced Wasserstein distance to the nearest training map, the outcome is the model's PSNR gain over persistence on that map. U-Net 200k over 30 maps: partial Spearman (controlling for motion) -0.73 [-0.84, -0.32] at one tic and -0.80 [-0.88, -0.46] at four tics, permutation p 1e-4, leave-one-map-out same sign on all 30, replicated in pixel space (map ranking agrees at 0.86), no relation between episodes of one map. Pre-registered and frozen before scoring. **Open:** the pre-declared verdict gate prints "does not support" because two distance sanity checks failed (same-WAD arenas not all nearer than campaign maps; 4-episode subsets of eight campaign maps disagree by more than 10 percent); Rohan rules, Astra reviews Sep 26 13:30 EDT. SD 3.5 and PixArt are scored through the study at their final 200k.
4. **Two closed-loop findings.** The live SD 3.5 weights have an intermittent absorbing failure under autoregression (latent channel 13 captured at a fixed value, blank frames; 50k and 70k, brushed at 95k and 100k) that the fp32 EMA weights never fall into; and a directional check on real turning windows (swap TURN_LEFT/TURN_RIGHT in the newest control) reverses the predicted camera motion in 87 percent of windows for the U-Net and 84 percent for SD 3.5 at the right magnitude, so neither learned persistence.

**Runs (Spiderman):** U-Net done at 200k Sep 24. SD 3.5 (GPU 2, 0.53 updates/s) 200k about Sep 27 21:30 EDT. PixArt (GPU 1, 1.4 updates/s when the card is not shared) 200k about Sep 27 01:00 EDT. Steward 7 reads every 5k on GPU 3 (TF, rollouts, probes, directional) and reports to the main session; every run streams to W&B `doomdit-nexttic`.

**Dataset (public on Hugging Face):** 6,000 training episodes on arenas 2 to 5 encoded in both spaces (rows train on ids 0:2000); validation 6000:6100, test 7000:7100 (sealed); evaluation corpus of 210 seeded episodes on 30 maps (seen: 60 on the original 17 maps; unseen: 20 on arenas 16 and 17; unseen2: 130 on 13 campaign maps) per tic in the SD 1.x space, SD 3.5 space encoding.

**Open for Rohan:** the verdict-gate ruling; deletions on Spiderman (`hf_release_arnold`, `raw_arnold`, `latents_arnold_sd35`, `_aborted_safe_to_delete`, 483 GB; disk at 336 GB free with a ladder armed at 300 and 150 GB); a word to hengjinz about GPU 1; the GPU 2 plan after SD 3.5 stops (proposal: a 6,000-episode U-Net row); the sealed-corpora rule for the study's fixed-checkpoint reads.

**For Keerthana (Sep 25 division of labour, `.claude/analyses/weekly-update-2026-09-25.md`):** related work from `.claude/analyses/distance-study-literature-2026-09-25.md`, the dataset and method sections, the PhysWM template with the page budget, figure 1 candidates (rollout strip, W&B curves). Rohan holds results, tables, the generalization figure, scoring and the server. Reading order: this section, then the log below from Sep 23, then `docs/RESEARCHER_DOSSIER.md` 3.12 to 3.14, 4.10 to 4.11 and 5.9.

---

*Sep 16 framing (history):*



**Framing (decided with Rohan, Sep 16).** The paper is a warm-start study for small-budget world models, with Doom as the testbed, delivered on the first open reproducible Doom benchmark. Headline: pretraining exposure in the target latent space, not architecture, predicts small-budget Doom world-model quality. Supporting findings: (a) map transfer, where the U-Net's advantage concentrates on the maps it trained on and the transformer gives back less on unseen maps (two maps today, 17 once the new corpus is scored); (b) evaluation, where every model falls to or below the frozen-seed persistence reference perceptually by horizon 64, so long-horizon metrics need persistence references. Explicitly not claimed: first DiT for Doom (PlayGen 2024), LPIPS parity with GameNGen, "stable dynamics", "superior generalization", pretraining as the proven cause. General lessons for small-budget single-environment world models go in the conclusion: pick the warm start by exposure to your latent space; architecture is second-order at this budget; report rollouts against persistence; measure transfer to unseen scenes.

**Rows under one recipe** (90k updates, batch 32, lr 5e-5, warmup 2000, context 32, v-prediction, SD KL-f8 latents): DiT-XL/2 ImageNet (two seeds, done), SD 1.4 U-Net (done), PixArt-alpha 512 (done, evaluating Sep 16), UniDiffuser v1 (LAION-2B transformer in our latent space; build started Sep 16, train Sep 17 on GPU 2, the fourth exposure point), video-pretrained SkyReels-V2 DF 1.3B in its own autoencoder (exploratory, 10k updates of batch 8, running on GPU 3, due Sep 17 morning). Compute-matched 2.5k-update runs of the image rows (80k samples) queue behind PixArt's evaluation on GPU 2.

**Audits in flight (Sep 16):** motion-magnitude test of the rollouts (stable vs static); 15-map unseen corpus (Freedoom maps 18 to 32, 10 seeded episodes each) for zero-shot transfer scoring of every finished row; compute-matched short runs. Done: blur control, late-drop audit, episode-level bootstrap CIs, trajectory identity check.

**Timeline:** PixArt numbers Sep 16 night; video row and 17-map scores Sep 17; UniDiffuser trained by Sep 19 and evaluated Sep 20; draft Sep 20 (skeleton being written under paper/); final Sep 28; deadline Sep 30 AoE. Release: dataset shards and weights under Rohan's Hugging Face account before the final.

**Open for Rohan:** Hugging Face account for the release; whether to commit Superman cards; human study (deferred; likely skipped).

- **Sep 16 Astra round 7 (independent proposal):** lead with a reproducible multi-map Freedoom benchmark and persistence/motion diagnosis; prioritize one bounded audit of existing paired rollouts over another model or map-training pair. Higher h64 PSNR does not establish better dynamics: both DiT seeds are near copy-seed and all completed models have worse h64 LPIPS. PlayGen already uses a DiT for Doom and action-aware evaluation, so retire older first-Doom-DiT language. Details and source checks: `.claude/analyses/astra-contribution-2026-09-16.md`. No experiment or queue change adopted here.

- **Sep14 round5b video-row revision (proposal):** Open-Sora1.1 remains first, SkyReels-V2 DF1.3B becomes runner-up; adopt shared VP-v/no variance and try L16 only if full throughput/evaluation gate fits (else L8). Gate-passed video now outranks unstarted seed1 for the expanded research goal: proposed GPU3 after U-Net, seed behind PixArt GPU2; no gate means existing seed starts, no idle reservation. No queue changes made. Native Matrix-Game2 tree has inference but no verified world-model training path. Updated `.claude/analyses/astra-video-row-2026-09-14.md` records forecasts as subjective, token-cost arithmetic, objective correction and conditional priority.

- **Sep 14 night video-row review (proposal, awaiting comparison):** preserve PixArt and seed1; prototype Open-Sora 1.1 stage3 as a separate video-pretrained system row (8 decision-frame context, one target, native epsilon DDPM, same SD VAE), then use a released card only after fit and full-evaluation timing gates. Up to 36 training + 36 evaluation card-hours proposed, not allocated. Wan1.3B is runner-up; Cosmos2.5 is the Cosmos-specific alternative. The observed gap does not establish pretraining causality. Correct Cosmos1 CV8x8x8 temporal factor to 8; Cosmos2 video tokenizer and Cosmos2.5 Wan-family setup use factor4. Detailed verified sources, estimates, action-alignment and Sep22 stop gate: `.claude/analyses/astra-video-row-2026-09-14.md`. No remote/queue/code changes.

- **Sep 14 agreed follow-up:** run both PixArt-alpha 512 on GPU 2 after the current DiT and its evaluation, and ImageNet DiT seed 1 on GPU 3 after the U-Net and its evaluation. Rohan delegates implementation separately; this review does not alter queues. Target completion Sep 17–18, subject to measured throughput and shared-card headroom. PixArt uses two learned 4096-dimensional action/bucket tokens through the existing caption projection, unchanged timestep conditioning, zero-inflated context channels, native regenerated positions and the first four unpatchified output channels trained as velocity.

- **Sep 14 literature follow-up (recommendation, awaiting agreement):** to address Rohan's explicit pretraining objection, Astra now favors one full PixArt-alpha512 third-backbone run over seed1, subject to a one-hour wrapper/fit gate. Open-Sora1.1 is the strongest verified SD-latent video candidate, but preserving temporal tokens makes it a different and much costlier recipe. Compatible larger image transformers do exist: Large-DiT3B/7B, FiTv2-3B, DiT-MoE as well as UniDiffuser. Primary-source recipe matrix, corrected checkpoint inventory and gates: `.claude/analyses/astra-world-model-literature-2026-09-14.md`. Current paired runs and queued seed unchanged; no remote execution.

- **Sep 14 Astra round 4 (recommendation, awaiting comparison):** finish the corrected pair unchanged; audit EMA versus live and report matched exposure separately from measured compute; retain the queued full DiT seed for the narrow shared-recipe result. PixArt-alpha 512 is the strongest alternate pretraining-package experiment, but Sigma 512 uses SDXL latents and is not compatible with the fixed SD1.x corpus. Detailed ranking, pilot gates and sources: `.claude/analyses/astra-review-2026-09-14-dit-gap.md`. No training changes or remote execution in this review.

- Sep 10 launch recommendation: retain shared lr 5e-5 / warmup 2000 / batch 32; early adaLN relative-update probes alone do not justify a DiT-only LR multiplier.

**Sep 10 launch-gate follow-up:** review of 1106c97 accepts the accumulation skip/continue logic for the adopted positive clipping threshold. Remaining narrow fixes: compute a real norm when clipping is disabled; preserve skip count across recovery. New-corpus reproducibility requires seeding the newly constructed ViZDoom instance before init and resetting episode-local anti-stuck state, as well as Python/NumPy/Torch RNGs. Recommendations below; no training/recorder code edited in this review.

**Sep 10 follow-up:** Rohan's requested headline evaluation is PSNR, LPIPS, FVD16/32 and a small human study; IDM is exploratory. Round-4 pairing recommends 24 raters x 60 balanced real-versus-generated trials (14/28 decision frames), fresh test-only episodes because the old held-out set informed development, and one additional full DiT seed if only one A6000-day is available. These protocol recommendations await comparison and agreement.

**Sep 10 round-3 pairing review (recommendations, not adopted):** owner decisions remain corrected-transition warm-start restarts, decision-rate latents, and the existing frozen encoder / fine-tuned decoder. Independent review recommends shared lr 5e-5, warmup 2000, batch 32, L32; recovery and validation fixes before launch; K8 IDM with a matched-sequence real-data reference; A6000 placement when free. Findings and unresolved choices are logged below. No training code was changed by this review.

**Project goal (finalized Sep 8, 2026).** DoomDiT rebuilds a GameNGen-style Doom world model and asks one question: what changes when the U-Net backbone is replaced by a Diffusion Transformer. DiTs have become the default in industry video and world models, but no controlled comparison exists for action-conditioned game simulation. We train both backbones on the same data, latents, and conditioning under the same small compute budget (16 GB GPUs, tens of GPU-hours), so the backbone is the only variable between our two rows. We do not attempt GameNGen's scale; GameNGen enters as a reference point, not a matched baseline. We measure per-frame quality, autoregressive stability, action following, and training feasibility, release the lossless 320x240 Doom dataset we build, and target the CoRL 2026 PhysWM workshop on Sep 30. Multi-map generalization and memory (MultiGen) are the follow-up paper.

**Target: CoRL 2026 PhysWM workshop, Sep 30 AoE, 4 pages** (working assumption from Rohan's Sep 1 lean; confirm at the Sep 2 1-on-1). The Sep 2 and Sep 5 NeurIPS workshops are out: the Superman project directory is gone and nothing can be re-measured in four days. ICLR 2027 (Sep 24) needs the full experiment set and is not reachable either. Section 0c is the experiment list written backwards from Sep 30.

**The paper** is the controlled U-Net vs DiT world-model comparison under matched data and compute, plus the small-compute recipe (section 3). Two facts found on Sep 1 change what "the final run" means:

1. The released checkpoint trained on all 500 episodes, and the 26.04 dB / 0.153 headline was scored on the training-time eval segments, which are training data. There is no honest held-out number for it. **Both backbones must be retrained on a 450-episode split**; the final DiT run is not optional.
2. Superman holds nothing from April except the `Doom` conda env and logs. Latents, runs 004/012, and any U-Net artifacts were deleted (disk cleanup; home is now LEGO and perseve work). Weights are safe on the GitHub release. The source dataset is 48 GB of JPEG parquet on Hugging Face; `encode_episodes.py` (new) rebuilds per-episode latents from it in a few GPU-hours.

**Sep 9 state (morning, after the overnight run):** the data-collection agent is Arnold (paper-backed, MultiGen's choice) recording every tic losslessly on 17 maps; 850 episodes recorded, encoded, split, and on both servers; the 1,000-episode Stiegler set is the continuity set, and the April checkpoint reproduces on it (27.4 dB / 0.117 held-out, copy-last 26.2 dB). The new stack (velocity objective, L = 32 stacked context, GameNGen noise augmentation, one recipe for both backbones) is validated on real data and **both main runs are training on Superman**: `010-dit-l32` (four GPUs, 0.9 steps/s, done about Sep 10 06:00) and `011-unet-l32` (one GPU, resumed from 5k after an OOM at checkpoint save, done about Sep 13). The DiT context sweep, the IDM (ceiling 69.3% top-1), and decoder fine-tuning (23.7 to 29.1 dB, HUD 17.9 to 34.7; an LPIPS variant is training) are done or in flight. Early DiT numbers at step 15k with the fine-tuned decoder: 20.3 dB / 0.356 LPIPS held-out, above the 19.7 dB copy-last baseline; the U-Net's held-out v-loss at 5k (0.2445) is below the DiT's (0.2552), so the comparison is live. Evaluation scripts, figures, paper skeleton, data cards, and the Hugging Face release script exist. What remains: let the runs finish, run the M3 checklist (`REQUIREMENTS.md` section 10), fill the tables, write. Open for Rohan: HF account for the upload, a second seed, and the Friday update (draft in `docs/updates/`).

**Why it matters beyond the paper:** the same action-conditioned latent DiT is the starting point for fall goal 3, a world model for LEGO trajectory decisions, and Changliu's May 4 suggestion of action rollouts for long-horizon planning plus sim-switching at contact-rich points using the perseve Isaac Sim pipeline. `doom_data.py` and `doomdit_utils.py` are written to be reused there.

**Standing rhythm:** weekly 1-on-1 with Changliu every Wednesday 1:00 to 1:30 PM from Sep 2 to Dec 9; Friday Slack update tagged by goal.

## 0b. How we got here (condensed)

DoomDiT is an action-conditioned latent diffusion world model: DiT-XL/2 (673M params) predicts the next VizDoom frame from 4 past frame latents plus one discrete action, trained on Superman (4× A4000 16 GB, about 72 GPU-hours) as the CMU 18-789 final project with Keerthana Chirumamilla. Best checkpoint is step 87,200, loss 0.0481, released as GitHub tag `002-DiT-XL-2-best-90k`. Headline teacher-forced numbers: DiT 26.04 dB PSNR / 0.153 LPIPS vs a matched U-Net baseline 24.60 / 0.198 (GameNGen reports 29.43 / 0.249, but that is a cross-paper comparison). The defensible contribution is the **controlled U-Net vs DiT comparison under matched data and compute plus the small-compute recipe**, not "first DiT world model" (Oasis, Matrix-Game, NFD, SANA-WM already exist). Nothing has been trained or evaluated since April 22. The fall goal is a workshop paper by mid October.

## 0c. Experiment list, backwards from Sep 30 (CoRL PhysWM, 4 pages)

Compute anchor: DiT-XL/2 ran at 1.62 steps/s at global batch 32 on 4 A4000s, so 90k steps is about 15.5 h; the U-Net is assumed comparable. Six GPUs are usable at most (GPUs 0 and 3 are occupied by others on Sep 1), so DiT on four and U-Net on two, or the two runs back to back.

| Dates | Must be true by the end | Runs on Superman |
|---|---|---|
| Sep 30 (Wed) | Submitted on OpenReview. | none |
| Sep 27 to 29 | Figures final (drift curves, rollout strip, table), related work, limitations (teacher-forced-only history, fixed 4-frame context, no real time). | none |
| Sep 24 to 26 | 4-page draft written; **numbers frozen Sep 26**. | re-runs only |
| Sep 20 to 23 | Full eval on DiT-final, U-Net-final, DiT-from-scratch: val PSNR/LPIPS (`eval_metrics.py`), drift curves and FVD16/32 (`rollout_eval.py`), IDM action accuracy. Human-study is out of scope. | eval, 1 to 2 GPUs |
| Sep 12 to 19 | **Final runs launched by Sep 15 (DiT) and Sep 16 (U-Net)**: 450-episode split, identical recipe, at least 90k steps each. Warm-start ablation: DiT-XL/2 from scratch for 20k steps vs warm-started for 20k steps (about 3.5 h each). | 4 + 2 GPUs, 3 days |
| Sep 9 to 11 (M1, reset Sep 8) | Fit check at 320 tokens; data regenerated and encoded (generate-and-encode, one arena); decoder fine-tuned; agent map test. See `REQUIREMENTS.md` section 9. |
| Sep 5 to 11 (original) | U-Net baseline implemented and smoke-tested (design decision in section 6); IDM trained on real pairs (`train_idm.py`); `rollout_eval.py` written per the design doc; harness reproduces the April headline on the legacy segments with `best.pt` (accept a small delta from re-encoded latents); split committed as `data/split.json`. | encode 3 to 5 h, IDM 20 min, harness runs |
| Sep 2 to 4 | Decisions in section 6 taken; repo cloned to Superman; `lpips` installed in the `Doom` env; `best.pt` fetched from the release; latents re-encoded from Hugging Face (or the Drive zip if Keerthana re-shares it); Keerthana message sent. | encode job |

Fallback if the final runs slip past Sep 19: report the released checkpoint on the legacy segments with the training-data caveat stated in the text, keep the U-Net row from April as "reported in the class project, not reproduced", and lead with the autoregressive metrics on the DiT. That is a weaker paper; the point of launching by Sep 15 is not to need it.

Disk budget on Superman (98 GB free on Sep 1, other users hold the rest): latents 5.9 GB, raw frames for ten held-out episodes about 2.8 GB, released weights 5.2 GB, parquet shards streamed one at a time (0.5 GB), two final runs with `--keep-last 3` plus `best.pt` about 21 GB, IDM and eval outputs under 2 GB. **About 37 GB total**, leaving roughly 60 GB free. The 250 GB rule cannot be met by us because the shortfall is other people's data; this needs Rohan's explicit go-ahead.

## 1. Where work happens

| What | Where |
|---|---|
| Repo | `~/Documents/Github/Doom` on `main` (`github.com/RohanNaga/Doom`). Open chats here. Two worktrees under `.claude/worktrees/` are leftovers from July; `IDEAS.md` has been copied to the main checkout. |
| Server | Superman `rohan@128.2.204.116`. **The April project directory is gone** (verified Sep 1: no `Doom/` under home, no latents, no runs 004/012, no U-Net artifacts); the `Doom` conda env (torch 2.11, diffusers 0.37, no `lpips`) and `~/logs/doom-*.log` survive. Disk 98 GB free of 3.5 TB. GPUs 0 and 3 in use by others. Leave 2 GPUs free. Use the `/run-server` skill. |
| Weights | GitHub release `002-DiT-XL-2-best-90k` (`best.pt`, `0090000.pt`, 2.6 GB each, bf16). `bash download_weights.sh`. |
| Collaborator | Keerthana Chirumamilla holds the frame-to-latent encoding script, the U-Net baseline, the PSNR/LPIPS harness, and the Drive copy of `full_latents.zip` (5.43 GB; the link now needs a Google login). Draft ask: `.claude/analyses/keerthana-ask-2026-09-01.md`. |

## 2. What is missing for a paper (in priority order)

1. **U-Net baseline code and checkpoints**: not in the repo, not in any transcript. Recover from Superman or Keerthana, or re-train a matched-params U-Net from the documented recipe.
2. **PSNR/LPIPS harness**: rebuilt Sep 1 as `eval_metrics.py`; April numbers reproduced Sep 9 (27.43 dB / 0.117 on lossless held-out frames of the same agent, copy-last 26.24 / 0.141). New-stack harness is `eval_tf.py`.
3. **Autoregressive metrics**: none exist. Design in `.claude/analyses/autoregressive-eval-design.md` (drift curves, FVD16/32, IDM accuracy; one rollout engine feeding all three). Teacher-forced-only evaluation is the most likely rejection reason.
4. **No validation split, single seed**; `best.pt` was picked by smoothed train loss and trained on every episode. Split policy now lives in `doom_data.make_split` (episode-level, seeded, 10% held out); the released checkpoint cannot be scored on it honestly.
5. README is still upstream fast-DiT; presentation claims gradient checkpointing was on but the code default is off; every comment says "DDIM" but sampling is respaced ancestral DDPM (`p_sample_loop`).
6. Inference is 50-step DDIM at about 5 s per 8 frames. Real time is out of scope for the workshop paper; state it as future work (Self-Forcing / CausVid style distillation).

## 3. Paper plan (decided July 15, reaffirmed Aug 21)

- **Spine 1**: controlled backbone ablation, U-Net vs DiT, same VAE, same conditioning, same data, same compute.
- **Spine 2**: small-compute reproducibility, about 72 GPU-hours on 16 GB cards, full recipe and weights released.
- **Supporting ablation**: ImageNet image-model warm start vs from scratch (cheap; warm start gave a 20× loss drop in the first 1,000 steps).
- **Future work**: cross-game transfer with weight-level analysis (which layers move: adaLN action pathway vs spatial blocks), differentiated from GameFactory; scenario-latent skill files (`IDEAS.md`); robotics grasp-candidate rejection (recap section 8).
- Must-cite and differentiate: GameNGen, DIAMOND, Diffusion Forcing, Oasis, Next-Frame Diffusion (310M AR DiT, closest cousin), MineWorld (IDM metric), Matrix-Game (GameWorld Score), GameFactory. arXiv IDs in the agent context brief.

## 4. Fall 2026 goal and timeline (submitted Aug 21)

Goal 1 of 4: *DoomDiT world model to a final result, workshop paper by mid October. Evaluate the current checkpoint vs the U-Net / GameNGen baseline, plan and run the final larger model and dataset run, write up as a controlled U-Net vs DiT comparison at small compute. Stretch: one world model learning multiple scenarios or games with a game flag.*

| Block | Doom items |
|---|---|
| Aug 24 to Sep 11 | Evaluate current checkpoint vs baseline, find gaps, size the final run. |
| Sep 14 to Oct 2 | Launch the final run, start the workshop paper. |
| Oct 5 to 16 | Workshop paper submitted. |
| Oct 19 to 30 | Stretch: multi-game conditioning experiment. |

Rohan's Sep 1 framing: get the workshop paper together in the next three to four weeks. Risk stated in the plan: if the final run needs more compute or time than Superman allows, fall back to the current checkpoint plus the new metrics.

## 5. Venues (checked 2026-09-01)

The originally targeted general NeurIPS 2026 workshop date (Aug 29) has passed. Live options:

| Venue | Deadline | Fit | Notes |
|---|---|---|---|
| NeurIPS 2026 WS "Robot Learning with World Models" | **Sep 2, 2026 AoE** | high | 2 to 4 page short papers accepted; non-archival; notification Sep 25. Only reachable with a short paper on existing results. |
| NeurIPS 2026 WS "World Models in Physical AI" | **Sep 5, 2026 AoE** (extended) | high | up to 8 pages, non-archival, OpenReview, notification Sep 29. Topics include generative simulation and evaluation. Reachable with the current checkpoint plus a rebuilt metric harness if U-Net numbers can be reproduced in 4 days. |
| NeurIPS 2026 WS "Foundation Models for Temporal Systems" | Sep 15, 2026 AoE | medium | forecasting / simulation framing. |
| ICRA 2027 | Sep 15, 2026 | low for Doom alone | only with the robotics extension. |
| ICLR 2027 main | abstract Sep 19, paper Sep 24, 2026 | medium | needs the full experiment set (autoregressive metrics, baseline, ablations). |
| CoRL 2026 WS "Bringing Physics Simulation and World Models Together for Robotic Manipulation" | **Sep 30, 2026 AoE** | high for Doom and for the Lego world-model direction | up to 4 pages, non-archival, OpenReview (`PhysWM`), Nov 12 in Austin. Topics include world models for planning and decision making, learning dynamics from video, sim-to-real. Closest match to the mid-October plan. |
| CoRL 2026 WS "Do Robots Need World Models?" | not checked | medium | debate format; site do-robots-need-world-models.github.io. |
| AAMAS 2027 | Oct 10, 2026 | low | agent / simulator framing. |
| IEEE Transactions on Games | rolling | archival home for the extended version | |

The mid-October target in the semester plan does not match any of the NeurIPS workshop dates, which all close by Sep 15. Decision needed: sprint for Sep 5 with existing results, or aim at the CoRL PhysWM workshop (Sep 30, 4 pages, the one that fits the three-to-four-week plan), or ICLR 2027 (Sep 24) with the full experiment set.

## 6. Open decisions (owner: Rohan)

- **Sep 16 round 9 (proposed, Rohan decides Thu morning):** night plan below in section 7 (2026-09-16 23:00 EDT); EMA-as-reported-weights, inference context-noise sweep, optional U-Net continuation to 180k as a budget curve.

- **Sep 16 round 8:** adopt UniDiffuser option A (rewind to 5k, lr 2.5e-5), verify the resume override and early validation/update recovery, and budget completion plus evaluation before Friday.

- **Sep 16 round 7:** adopt benchmark-plus-persistence framing and a motion audit capped at one A6000-day (proposed, unmeasured), or prioritize the broader map-transfer study; revise winner-required R4.5/R6.1 if adopting the outcome-independent study. Complete release/reproduction work for a benchmark claim.

- Round5b: approve/reject conditional GPU3 video priority over an unstarted second seed; shared-v/L16 Open-Sora proposal supersedes native-epsilon/L8 default in the first-pass review. No queue edit authorized or executed by this review.

- Sep14 night: adopt or reject gated Open-Sora1.1 exploratory video row and its additional card-hour budget; current queue remains authoritative. Stop new video-model engineering after Sep22 if trainer/evaluator is not operational.

- **Sep 14 literature follow-up:** choose the claim before the extra run: repeatability of the existing two packages (seed1), sensitivity to broader transformer pretraining (recommended Alpha512 third row), or native video modeling (Open-Sora1.1, separate temporal-context and compute design). No single run isolates architecture/pretraining/context simultaneously. New larger-model inventory supersedes the earlier incomplete survey; do not claim no larger transformer exists in SD1.x latent space.

- **Sep 14 follow-up gates:** both PixArt and seed1 are agreed; implementation and queue transfer belong to the separate coding agent. Verify original-model parity after inflation, both conditioning-token gradients/dependence, real velocity-loss optimizer steps, checkpoint/EMA reload and evaluation routing, and measured completion time including evaluation. Live/EMA reporting and compute accounting still need finalization. Any DiT-only LR branch requires a tested post-resume LR override and separate tuning accounting.

- Sep 10 early probe interpretation: option (a) recommended; no adaLN LR multiplier before launch. Verify loaded step-zero modulation norms and log absolute update/weight RMS plus functional gate/residual scales; owner launch decision remains pending.

- **Sep 10 seeding/skip review:** use a versioned stable hash of (corpus ID, episode ID, RNG stream), independent of worker count/order; seed ViZDoom during its construction before `init`, reset cross-episode anti-stuck counters, and verify reordered/retried episode replay under a pinned environment. Current committed anti-stuck preamble is deterministic and upstream DQN next_action is greedy; do not introduce exploration when adding seeds. Adoption of exact recorder integration awaits owner implementation and real-server replay checks.

- **Sep 10 follow-up, pending agreement:** human-study assignment and episode/rater-clustered analysis; fresh test-only collection versus transparently quarantined historical holdouts; one full DiT seed as the single-run robustness check. Accept 5000-step recovery cadence given measured 2–3 minute upload cost; reshuffled data order is acceptable for crash recovery if disclosed, but DDP must not restore rank 0's RNG on every rank. No claim of exact trajectory replay without sampler position and independent rank RNG.

- **Sep 10 round-3 review, pending comparison with Rohan:** shared lr 5e-5 / warmup 2000; live versus EMA selection under one fixed policy; freeze the 850-episode main pair and prioritize a paired second seed over a data-scale training ablation; obtain free A6000 slots versus protected Superman fallback. Distinguish equal updates/examples from equal measured compute. Before relaunch, resolve incomplete recovery, batch-dependent validation corruption, and disk-failure handling; freeze untouched episode-level test data before further selection.

- **Sep 9 independent pairing recommendation (not adopted):** frame the workshop as a budget-constrained comparison of two pretrained backbone packages, with outcome-independent acceptance criteria. Prioritize paired final evaluation, action interventions, episode-level uncertainty, and measured compute accounting; demote extra training ablations and FVD if its implementation cannot be validated. Existing winner-required requirements R4.5/R6.1 need discussion, not silent enforcement.

1. **Deadline**: Sep 30 CoRL PhysWM (assumed) vs ICLR 2027 Sep 24 vs slipping to a later workshop. Decides everything in section 0c.
2. **Disk go-ahead**: about 37 GB on a Superman disk that is already at 98 GB free. The 250 GB rule is unmeetable by us; proceed or find another machine (Spiderman has 3 TB free but 4 A6000s shared with perseve).
3. **Data path**: re-encode from Hugging Face with `encode_episodes.py` (48 GB streamed, 3 to 5 GPU-hours, exact match to Keerthana's latents not guaranteed) vs waiting for the Drive zip. Recommendation: start the re-encode now, swap in the zip if it arrives, because the final runs need latents either way and the re-encode makes the repo self-contained.
4. **Matched U-Net design**: match parameter count (about 670M, a 2D U-Net with the same 20-in / 8-out channel contract and adaLN-style action+time conditioning) or match training FLOPs; warm-start from SD 1.4 or not. Recommendation: parameter-matched, both backbones from scratch for the main row, ImageNet warm start as a DiT-only supporting row. The April comparison's warm-start status is unknown until Keerthana answers.
5. **Action offset**: training used the action stored on the target frame's row (`ACTION_OFFSET = 4` in `doom_data.py`). Whether that is the causally right action depends on the dataset's logging convention. Keep it for reproduction; decide before the final runs.
6. Whether the multi-game stretch belongs in this paper at all (recommendation: future work).

## 7. What changed (log; newest first)

- **2026-09-26 09:00 EDT: the EMA closed-loop event rate has roughly doubled since 105k while TF improves every read; decision: report per-window rates, add a third EMA seed on the surviving snapshots, and put checkpoint selection to Rohan.** 125k EMA seed 1: 21.51 / 19.66 / 18.76 / 17.25 / 17.36 at 4/32/64/128/256 (-0.10 against copy-seed); rollout 15 (map 5) ends at 9.66 dB with channel 13 under -0.9, the first EMA seed-1 rollout to end captured; three rollouts with a frame under 10 dB (maps 4, 5, 5). Totals 50k to 125k: live 272 rollouts, (a) 48 (17.6 percent), (b) 37 (13.6), (c) 24 (8.8); EMA 304 rollouts, (a) 19 (6.2), (b) 6 (2.0), (c) 5 (1.6). EMA event (a) by window: 55k to 100k 7 of 160 (4.4 percent), 105k to 125k 12 of 144 (8.3), 115k to 125k 10 of 96 (10.4), almost all on maps 4 and 5; four of the six (b) and two of the five (c) events fall in 105k to 125k. Directional steady (EMA 0.852 at 125k, its best). Reading for the paper: the gap between teacher-forced quality and closed-loop stability widens late in training for both weight sets, the EMA at a lower rate. Decisions: the series continues and the paper reports rates per window; EMA seed-2 rollouts run on the surviving snapshots (100k, 110k, 120k, 125k, 90k if present) in idle GPU 3 time for 48 EMA rollouts per late checkpoint; the matched table stays at 200k for every row, and whether the released SD 3.5 weights are chosen by validation-rollout stability (legitimate, the rollouts are on validation windows) rather than by step count is Rohan's call. 130k read about 09:10 EDT; SD 3.5 at 128.2k.
- **2026-09-26 08:50 EDT: SD 3.5 125k: TF bests again (EMA h1 23.18 / 0.135, h4 21.52 / 0.200); EMA seed 0 under copy-seed at 256 (17.16) with one transient map-5 near-blank stretch that recovered; the odd-5k probe rise again.** Rollouts at 4/32/64/128/256: EMA 21.36 / 18.98 / 17.87 / 18.40 / 17.16, live 21.46 / 18.84 / 18.53 / 18.23 / 17.38. EMA 256-tic series 75k to 125k: 18.54 / 17.94 / 17.44 / 17.56 / 17.55 / 17.60 / 16.86 / 18.13 / 17.55 / 17.95 / 17.16. EMA rollout 15 (map 5, episode 6079, the same window as at 115k): 18 tics under 10 dB (min 6.5) between tics 207 and 224, recovered to 16.9 dB by 256, not channel 13; no channel-13 end state; all-channel end-out 9 (one above the 3 to 8 band). Live rollout 1 (map 2) 49 tics under -0.9 in channel 13, ending at 9.1 dB. Probe_v2 mean 2.6 / 3.3 / 1.2, control-MLP ratio 0.154 (highest yet). Directional and EMA seed 1 pending; the pairing rule waits on the directional.
- **2026-09-26 06:20 EDT: 120k complete: directional normal (EMA 0.828, live 0.852), EMA seed 1 +0.55 over copy-seed at 256; rates 50k to 120k: live 18.4 / 14.1 / 9.4 percent, EMA 5.5 / 1.8 / 1.5 percent.** EMA seed 1 at 4/32/64/128/256: 21.45 / 19.68 / 19.28 / 17.39 / 18.01 (copy-seed 17.46 at 256); two rollouts with a frame under 10 dB (maps 4 and 5), none ending under 12 dB or captured. Totals (`collapse_rates.md`): live 256 rollouts, (a) frame under 10 dB 47, (b) ends under 12 dB 34, (c) channel-13 end 24; EMA 272 rollouts, 15 / 5 / 4. Maps 4 and 5 carry nearly every EMA event. No flag. SD 3.5 at 123.1k, PixArt at 139k; 125k read about 07:00 EDT.
- **2026-09-26 05:40 EDT: SD 3.5 120k: EMA clean on seed 0 (+0.44 over copy-seed at 256), live shows three map-5 channel-13 excursions; TF bests continue.** TF h1 EMA 23.15 / 0.136 (+1.59 dB), h4 EMA 21.48 / 0.201 (+2.27); live h1 22.95 / 0.146, h4 21.24 / 0.222. Rollouts at 4/32/64/128/256: EMA 21.36 / 19.05 / 18.15 / 18.62 / 17.95 (no channel-13 end state, min frame 11.5), live 21.04 / 18.71 / 18.44 / 18.62 / 17.38 with three map-5 rollouts under -0.9 in channel 13 (one ending at -0.99 and 11.9 dB, one recovered, one with a 9.0 dB frame ending at 10.7 dB). EMA 256-tic series 75k to 120k: 18.54 / 17.94 / 17.44 / 17.56 / 17.55 / 17.60 / 16.86 / 18.13 / 17.55 / 17.95. Probe_v2 mean 1.5 / 2.3 / 0.7. Directional and EMA seed 1 follow; the table gains 120k. SD 3.5 at 121.3k, PixArt at 135.4k (135k periodic read running).
- **2026-09-26 04:10 EDT: 115k EMA seed 1 matches copy-seed at 256 tics but maps 4 and 5 fail again; the rate table now spans 50k to 115k (240 rollouts per weight set).** Seed-1 EMA 21.40 / 19.70 / 18.33 / 17.66 / 17.45 at 4/32/64/128/256 against its copy-seed 17.46; 3 of 16 below the line at 256 (maps 2, 4, 4), three rollouts with a frame under 10 dB (maps 4, 5, 5), none ending with channel 13 under -0.9 (the seed-0 rollout 15 ended at -0.897, inside the threshold). The EMA's closed-loop weak spot is maps 4 and 5 at long horizons. Rates 50k to 115k (50k has only the live seed-1 set: 10 of 16 with frames under 10 dB): live (a) frame under 10 dB 45 of 240 (18.8 percent), (b) ends under 12 dB 34 of 240 (14.2), (c) channel-13 end 23 of 240 (9.6); EMA (a) 13 of 240 (5.4), (b) 5 of 240 (2.1), (c) 4 of 240 (1.7). Every read from 120k adds 16 live and 32 EMA rollouts. SD 3.5 at 118.2k, PixArt at 128.9k.
- **2026-09-26 03:50 EDT: closed-loop failure rates, SD 3.5 55k to 115k, 224 rollouts per weight set: live 15.6 percent with a frame under 10 dB, EMA 4.5 percent; ending under 12 dB, live 11.2 percent, EMA 1.8 percent.** From every kept rollout npz (13 reads of 16 rollouts per weight set plus the seed-1 sets at 70k live and 105k EMA; `$D/tmp/steward/collapse_rates.md`, being extended back to 50k): live (a) any frame under 10 dB 35 of 224 (reads 55k, 65k, 70k both seeds, 80k, 85k, 95k, 100k, 110k), (b) under 12 dB at tic 256 25 of 224 (55k, 65k, 70k both seeds, 100k), (c) channel 13 under -0.9 at 256 22 of 224 (55k, 70k both seeds, 85k, 100k); EMA (a) 10 of 224 (55k, 60k, 70k, 75k, 95k, 105k both seeds, 115k), (b) 4 of 224 (60k, 70k, 105k seed 1, 115k), (c) 4 of 224 (60k, 85k, 105k). The 70k live collapse alone carries more than 20 of the live events. 115k directional normal (EMA 0.836, live 0.828), so no flag. From 115k the EMA seed-1 rollout runs automatically after every read (`stw7-emawait`), 32 EMA rollouts per read. SD 3.5 at 117.4k, PixArt at 127.3k, 295 GB free.
- **2026-09-26 03:30 EDT: SD 3.5 115k: TF bests again; one EMA rollout (map 5) reaches a 6.2 dB near-blank frame and ends at 9.9 dB, the most severe EMA event of the series; the stability claim becomes a rate.** TF h1 live 22.95 / 0.144, EMA 23.14 / 0.138 (+1.57 dB); h4 live 21.18 / 0.221, EMA 21.46 / 0.203 (+2.25). Rollouts at 4/32/64/128/256: EMA 21.42 / 19.20 / 17.96 / 17.68 / 17.55 (+0.03 over copy-seed), live 21.19 / 19.06 / 18.00 / 18.13 / 17.40. EMA 256-tic series 75k to 115k: 18.54 / 17.94 / 17.44 / 17.56 / 17.55 / 17.60 / 16.86 / 18.13 / 17.55. EMA rollout 15 (map 5, the same window family as the 105k seed-1 channel-15 failure): channel 13 under -0.9 for 20 tics between 116 and 248, ten channels out of range at the end, min frame 6.2 dB, end 9.9 dB; the live weights are clean at 115k (channel-13 min -0.89, min frame 10.5). Probe_v2 mean 4.7 / 4.9 / 2.7 (the odd-5k pattern again: 65k, 75k, 85k, 95k, 115k high; 60k, 70k, 90k, 100k, 110k low; 80k and 105k exceptions); directional pending. Reading: two of nine EMA reads since 75k (105k, 115k) carry a long-horizon shifted-state rollout on maps 4 or 5, so the paper reports a rate for each weight set (rollouts with a frame under 10 dB, rollouts ending under 12 dB, channel-13 end states, over all rollouts run) rather than "the EMA never falls in"; decision: the EMA seed-1 rollout joins every read from 115k on (32 EMA rollouts per read), and the steward builds the cumulative rate table from every kept npz since 55k (`$D/tmp/steward/collapse_rates.md`).
- **2026-09-26 02:00 EDT: the evaluation-map encode and the distance chain finished; the SD 3.5 latent space agrees with the other two; all three spaces frozen.** `ENCODE_EVAL_MAPS_OK` at 00:07 EDT (both spaces, all 210 episodes), `DISTANCE_CHAIN_OK` at 01:39. SD 3.5 latent-space distances (16 channels, same episodes and tics, motion arm): the map ranking agrees with the SD 1.x space (Spearman 0.90 over the 30 primary maps; Kendall tau with the primary 0.76; pixels 0.70), the same checks pass (i, iii, v) and fail (ii, iv), floor band 0.022 to 0.093, bootstrap attenuation 0.076; the U-Net's one-tic gain has partial Spearman -0.73 on the SD 3.5 distance (pixels -0.66, SD 1.x -0.73). Frozen: `results/distance_study/distances_sd35.json`, its per-episode CSV and bootstrap, and the refreshed `figure_unet_h1/stats.json` with all three spaces. The verdict conditions are unchanged (only `distance_validation` false). GPU 3 now carries only the reads. SD 3.5 115k read in progress under the new cache env; SD 3.5 at 115.6k, PixArt at 123.8k (120k periodic read ok); disk 296 GB, /home 9.9 GB, both flat over the last hour.
- **2026-09-26 00:20 EDT: SD 3.5 110k: the EMA is clean again (256-tic 18.13, +0.61 over copy-seed, best since 75k), so the 105k softness reads as checkpoint-to-checkpoint variance, not a trend; TF bests continue.** TF h1 live 22.91 / 0.145, EMA 23.10 / 0.139 (+1.53 dB); h4 live 21.20 / 0.221, EMA 21.44 / 0.205 (+2.23). Rollouts at 4/32/64/128/256: EMA 21.42 / 18.87 / 18.30 / 18.15 / 18.13, live 21.04 / 18.73 / 18.40 / 18.75 / 18.27, both above copy-seed at every horizon. EMA series 75k to 110k at 256 tics: 18.54 / 17.94 / 17.44 / 17.56 / 17.55 / 17.60 / 16.86 / 18.13; EMA channel-13 end count 105k 2, 110k 0 (min -0.64); EMA frames under 10 dB: none (lowest 12.4); all-channel end-out 7 (band 3 to 8). Live: one rollout (map 5) under -0.9 for tics 207 to 240 (min -1.27, one 9.6 dB frame) and recovered. Probe_v2 mean 1.1 / 1.0 / 1.1 (moderate rise from 0.2); directional running. Paper reading: the EMA-clear claim holds at 110k with the 105k read as the recorded counterexample (2 of 16 on seed 0 and 1 of 16 on seed 1 ending in shifted states, none blank). SD 3.5 at 112k, PixArt at 116.6k (1.0 to 1.3 updates/s), disk 285 GB free, /home 11.6 GB.
- **2026-09-26 00:10 EDT: researcher dossier extended through Sep 25 and republished for Keerthana.** `docs/RESEARCHER_DOSSIER.md` gains 3.13 (the U-Net final, SD 3.5 to 105k with the collapse mechanism, probe episodes, directional check, PixArt; 8,343 words), 3.14 (the map-distance study with the per-map table and full statistics; 4,619), 4.11 (Sep 24 to 25 decisions; 2,714), 5.9 (distance-versus-generalization and persistence-baseline literature; 1,244), and a reading order plus a "what Keerthana can pick up" table in "How to use the sources" and 8.3; the source-check corrections of 23:15 are applied; every number traces to a log entry or a frozen file (the writer recomputed the partial Spearman -0.734, the pixel rank agreement 0.863, the eight failing maps and the 1.36 dB gap between the decoded-copy and raw persistence references from the frozen files). `docs/researcher_dossier.html` rebuilt (60 tables, 21 figures, including the EMA one-tic PSNR curve of both rows and the frozen distance figure); merged at `058300b`, header dated at `680323c` and `67dc386`. Published as a private artifact at https://claude.ai/artifact/5wRk6m5J3xFbkZzDYrXgNw (the Sep 23 artifact is in the other account and unreachable from this session); Rohan shares it from the page's Share menu. Process note: two writers briefly shared the branch after main assumed the first had died; the second's fast-forward wiped the first's uncommitted drafts (recovered from its scratchpad); rule from now on, one writer per branch and a commit per section.
- **2026-09-25 23:15 EDT: the day's SSH "hangs" were this Mac sleeping; keep-awake requested; dossier corrections.** Steward 7 traced the four stale-master incidents (18:22, 19:45, 20:48, 21:15 EDT) to the laptop entering maintenance sleep (`pmset -g log`): local timers freeze, the multiplexed master's TCP session goes stale, and the first call after each wake hangs or returns 255; Spiderman and the CMU edge were never at fault, and the server-side automation (queue, arrivals, directional, mirrors, all in tmux) never stopped. Main requested the desktop app's keep-awake for the session (idle sleep only); the steward runs `caffeinate -s`, which works only on AC, and the Mac is on battery, so overnight reports arrive in bursts after wakes. Recommendation to Rohan: `ServerAliveInterval 15`, `ServerAliveCountMax 2` in the multiplexed Host block. Corrections from the dossier writer's source check: SD 3.5 passed the U-Net's final LPIPS at 40k and both final numbers at 50k (not both at 40k); the one-tic CI is [-0.84, -0.32]; the directional figure for SD 3.5 is 84 percent (100k EMA 0.844); check (ii) fails on eight campaign maps, not nine; PixArt's 200k lands early Sunday, not Saturday. Section 0 and the weekly update are corrected accordingly.
- **2026-09-25 23:00 EDT: the all-channel end-state count is too noisy to carry the stability claim; the per-read EMA series is fixed; host load back to normal.** Calibration over 55k to 105k (rollouts whose last frame has any channel mean outside the ground-truth 0.5 to 99.5 percentile band, "any / at least three channels", live | EMA): 55k 8/8 | 6/5, 60k 5/2 | 7/5, 65k 4/2 | 5/3, 70k 12/12 | 7/4, 75k 5/3 | 7/5, 80k 8/3 | 5/3, 85k 8/4 | 8/3, 90k 8/3 | 3/1, 95k 8/3 | 4/3, 100k 4/3 | 4/3, 105k 6/4 | 8/5: the EMA sits at 3 to 8 in every read, only the 70k live collapse (12/12) stands out, so the band catches ordinary scene drift. The series that carries the claim from 110k, per read and both weight sets: 256-tic PSNR against copy-seed, channel-13 end count (under -0.9) and minimum, rollouts with any frame under 10 dB, with the all-channel count as context (`$D/tmp/steward/stw7_endstate.py`). Host load fell to 17 at about 22:45; SD 3.5 back at 0.50 to 0.54 updates/s, PixArt 0.9 to 1.36; the 110k read is running and its snapshot is mirrored.
- **2026-09-25 22:40 EDT: the 105k EMA long-horizon failures are per-channel mean shifts with RMS in scale, in channels 13, 15 and 8; the "absorbing state" is a family, not one channel.** Seed-1 EMA rollout 8 (map 5, episode 6011, 7.1 dB at 256): RMS 1.00 to 1.17 (data 1.00 to 1.59), channel 15's per-frame mean inside its data range through tic 192 then down to 0.38 (range 1.00 to 1.94) by 256, after an earlier 100-tic excursion that recovered; rollout 7 (map 4) shifts in channel 13, rollout 10 (map 4) in channel 8. So the paper's stability claim must be tracked on every channel: from 110k each read reports, for both weight sets, the number of rollouts ending with any latent channel outside its data range (`stw6_latstats.py` on the kept npz), plus the channel-13 end count and minimum, beside the 256-tic PSNR. JSON `steward_105000/latstats_ema_seed1.json`.
- **2026-09-25 22:15 EDT: the 105k EMA weakness past 128 tics reproduces on seed 1; other users' jobs are starving both trainers; the 300 GB ladder step was taken.** 105k EMA seed-1 rollouts (different windows): 21.26 / 19.93 / 19.23 / 17.31 / 16.49 at 4/32/64/128/256 against their own copy-seed 19.11 / 18.07 / 17.94 / 17.50 / 17.46, so the EMA is under copy-seed at 256 on both seeds (-0.66 dB seed 0, -0.97 seed 1); 12 of 16 crossed, 3 below at 256 (maps 4, 5, 4), three rollouts with frames under 10 dB; one rollout sat in the channel-13 zone for 77 tics and recovered, none ends there; rollout 8's 7.1 dB end state is not the channel-13 signature and its latent statistics are being read. Reading: the 105k EMA is weaker past about 128 tics while teacher-forced quality and the directional are at their best; maps 4 and 5 carry most of it; 110k decides whether it is a trend. If it is, the paper's claim becomes "the EMA delays the closed-loop failure" and the released SD 3.5 checkpoint should be chosen by the rollout series, not by step count. Host: load 70 on 64 cores from hengjinz's `sam6d_pilot` (four processes) and `large_head_features` plus ruixuanl's trainer; SD 3.5 at 0.32 updates/s (from 0.53), PixArt at 0.6 (from 0.95); our chain is at nice 19 and not in the top list; if it persists SD 3.5's 200k slips toward Sep 28 midday. Disk: 292 GB free at 22:04 EDT (22 GB/h), so the ladder's first step ran: `_aborted_safe_to_delete` (39 GB: the Sep 19 aborted 134-episode recording, the stale 0:60 arenas_678 latents, the seen_old28 sidecars, all regenerable and used by nothing, per its README) deleted by main at 22:15; the 150 GB step (the Hub-verified `hf_release_arnold` and `raw_arnold`) stands unless Rohan runs it first.
- **2026-09-25 21:00 EDT: SD 3.5 105k: best teacher-forced numbers yet, but the EMA 256-tic rollout fell 0.66 dB under copy-seed with two of 16 EMA rollouts ending in the channel-13 excursion zone (first time for the EMA); seed-1 EMA rerun ordered.** TF h1 EMA 23.07 / 0.141 (+1.51 dB), h4 EMA 21.41 / 0.208 (+2.20). Rollouts at 4/32/64/128/256: EMA 21.43 / 18.85 / 18.06 / 18.36 / 16.86, live 20.84 / 19.07 / 18.60 / 19.08 / 17.52, copy-seed 17.52 at 256; EMA 256-tic series 75k to 105k 18.54 / 17.94 / 17.44 / 17.56 / 17.55 / 17.60 / 16.86. EMA channel 13: rollout 5 (map 4, the recurring hard window) under -0.9 from tic 108, ending at -1.12 and 13.4 dB; rollout 2 (map 5) under -0.9 from tic 175, ending at -0.97 and 12.2 dB; no blank frames. Live clean (min -0.37). EMA rollouts ending under -0.9 per read 70k to 105k: 0 0 0 1 0 0 0 2; EMA channel-13 minimum per read -1.12 / -0.94 / -0.54 / -1.16 / -1.08 / -0.58 / -1.01 / -1.29 (correction to the 02:30 entry: EMA rollouts dipped under -1 before, at 70k, and recovered by 256; "at or above -0.44" referred to the two reference windows only). Probe_v2 mean 0.16 / 0.18 / 0.18 (lowest since 60k); directional EMA 0.836, live 0.820. Not a flag by the pairing rule; the new item is the EMA ending state. A seed-1 EMA rollout at 105k (about 35 min on GPU 3) runs to separate two hard windows from drift; 110k and 115k decide. From 110k every read reports the EMA channel-13 end-state count and minimum, since the paper's "EMA stays clear" claim rests on the series. SD 3.5 at 107.3k, PixArt at 106.8k (105k periodic read ok), 330 GB free.
- **2026-09-25 19:00 EDT: SD 3.5 100k complete: EMA bests again, EMA rollouts above copy-seed at every horizon, directional at its best; one live rollout ends at the edge of the absorbing state; PixArt recovering to full rate.** TF EMA h1 23.06 / 0.142 (+1.49 dB), h4 21.39 / 0.210 (+2.18). Rollouts at 4/32/64/128/256: EMA 21.36 / 19.00 / 18.35 / 17.95 / 17.60 (+0.09 over copy-seed at 256; series 75k to 100k 18.54 / 17.94 / 17.44 / 17.56 / 17.55 / 17.60), live 21.06 / 18.46 / 18.36 / 17.90 / 17.36. Channel 13: live rollout 15 (map 5) under -0.9 from tic 183 and at -1.04 with a 10.2 dB frame at 256, the closest approach to the fixed point (-1.4 to -1.5) since 70k; EMA min -1.01 in the recurring map-4 window, recovered to 19.3 dB. Probe_v2 mean 0.19 / 0.57 / 0.20; directional EMA 0.844 (motion 0.94), live 0.836 (0.96), the best of the series. No flag. Also today: a stale multiplexed SSH master hung the steward's polls at 18:22 EDT for 15 minutes; closed with `ssh -O exit`, one fresh connection succeeded (not an edge block). SD 3.5 at 103.5k (val 0.0879), PixArt at 99.5k at 1.24 to 1.38 updates/s (the other user's GPU 1 job eased), disk 336 GB, /home 14.2 GB.
- **2026-09-25 18:40 EDT: SD 3.5 95k and 100k: teacher-forced bests continue; a probe_v2 jump at 95k coincided with brief live channel-13 excursions but the directional held; PixArt lost no labels since the yield rule; ETAs Sep 27 21:30 (SD 3.5) and Sep 27 00:50 (PixArt).** 95k TF h1 live 22.83 / 0.150, EMA 23.04 / 0.142 (+1.47 dB); h4 live 21.10 / 0.230, EMA 21.35 / 0.212 (+2.14). Rollouts at 4/32/64/128/256: live 21.14 / 19.00 / 17.18 / 18.27 / 17.72, EMA 21.26 / 18.77 / 18.18 / 18.54 / 17.55 (flat since 85k), copy-seed 17.52 at 256. Live channel 13 min -1.26 with three rollouts under -1.0 (one 4.5 dB near-blank frame at tics 80 to 83 that recovered to 21.5 dB; one rollout ending at -0.89 and 13.3 dB); EMA min -0.58, clear. Probe max 51 / 46 / 22, probe_v2 mean 7.2 / 6.0 / 3.6 (highest yet), control-MLP ratio 0.146; directional EMA 0.836 (motion 0.94), live 0.828: the watch pairing (probe jump with live excursions) occurred, the arbiter shows no drop, so no flag; the live weights touched the absorbing state and recovered. 100k (in progress): TF EMA h1 23.06 / 0.142 (+1.49), h4 21.39 / 0.210 (+2.18), live rollout 17.36 at 256 (0.16 under copy-seed); probe_v2 mean 0.19 / 0.57 / 0.20, ratio 0.088. Steward observation for after the runs: probe_v2 has been high at the 5k-odd reads (65k, 75k, 85k, 95k) and low at the 10k reads (60k, 70k, 90k, 100k; 80k the exception), both probed on recovery files. PixArt periodic reads 75k to 95k all ok (70k labels recovered by hand); EMA h1 gain +0.62 to +0.71 and h4 +1.73 to +1.86 from 70k to 95k; val 0.1610 at 97k. Rates: SD 3.5 0.527 updates/s (200k about Sep 27 21:30 EDT), PixArt 0.93 (66 percent of normal, GPU 1 still shared; 200k about Sep 27 00:50 EDT). Disk 336 GB free at 16 GB/h; /home 15 GB free.
- **2026-09-25 18:20 EDT: U-Net per-map scoring complete at both horizons; four-tic partial Spearman -0.80 [-0.88, -0.46] over 30 maps; steward 7 lost to the session limit at 13:45 and resumed at 18:15; its automation carried the 95k and 100k reads.** Four-tic gains over persistence: validation maps +1.3 to +2.3 dB; near maps and the seeded arenas -0.3 to +0.9; campaign maps 24 / 19 / 26 / 28 / 25 / 18 at -2.6 / -2.6 / -3.1 / -2.0 / -1.8 / -1.8; partial Spearman -0.795, CI [-0.875, -0.458], p 1e-4 (one tic on the same 30 maps: -0.734 [-0.84, -0.32]); verdict conditions all pass except `distance_validation`. The h4 scores are frozen beside the h1 ones (`results/distance_study/scores/...`) with the refreshed `figure_unet_h1/stats.json`. Directional at 90k and 95k: EMA 0.836 / 0.836, live 0.828 / 0.828 (ref 0.914), steady. Steward 7 was killed by the Claude session limit at about 13:45 EDT (reset 14:30) and resumed at 18:15 by message; `stw6-arrive`, `stw6-queue` and `stw7-dirwait` kept reading (95k full read and directional done, 100k read running). SD 3.5 at 102k, PixArt at 97.5k, disk 337 GB free (about 15 GB/h since 13:30; the ladder's 300 GB step not reached), GPU 1 still shared.
- **2026-09-25 14:00 EDT: the distance result replicates in pixel space and at four tics; pixel distances frozen.** Pixel-space distances (same episodes and tics as the SD 1.x clouds, motion arm primary): the map ranking agrees with the SD 1.x space (Spearman 0.86 over the 30 primary maps, Kendall tau 0.70), the same checks pass (i, iii, v) and fail (ii, iv), and the U-Net's one-tic gain has partial Spearman -0.66 on the pixel distance, so the `pixel_space` condition of the verdict passes; only `distance_validation` remains false. Four-tic gain (24 maps scored so far): partial Spearman -0.79 [-0.89, -0.38], p 1e-4. Frozen in the repo: `results/distance_study/distances_pixels.json`, its per-episode CSV and bootstrap, and the refreshed `figure_unet_h1/stats.json`. The SD 3.5 latent space waits on its encode (48 of 130 unseen2 episodes; the encoder is the lowest GPU 3 tenant and OOMs when the card is full, which its wrapper retries).
- **2026-09-25 13:45 EDT: SD 3.5 90k: teacher-forced bests at both horizons, both weight sets above copy-seed in rollouts, the EMA 256-tic decline stopped, probe excursion subsided.** TF h1 live 22.78 / 0.149, EMA 23.00 / 0.144 (+1.43 dB); h4 live 21.02 / 0.228, EMA 21.32 / 0.214 (+2.11 dB). Rollouts at 4/32/64/128/256: live 21.38 / 18.78 / 18.35 / 18.51 / 17.71, EMA 21.17 / 19.05 / 18.05 / 17.96 / 17.56 (copy-seed 17.52 at 256, so +0.04), no frame under 10 dB on either weight set; EMA 256-tic series 18.54 / 17.94 / 17.44 / 17.56 at 75k to 90k. Channel 13: live min -0.96 (recovered), EMA min -1.08 in the same map-4 window that dipped at 85k, under -0.9 for tics 92 to 157 and recovered to -0.52, so one hard window rather than drift toward the fixed point. Probe max 4.3 / 4.5 / 4.2, probe_v2 mean 0.63 / 0.78 / 0.63 (85k 2.4 to 3.4, 70k 0.24 to 0.51): the 75k to 85k excursion has subsided; control-MLP ratio 0.082. Directional (EMA and live) follows; snap_0090000 mirrored. Disk 409 GB free (about 40 GB/h); SD 3.5 at 93.8k, PixArt at 79.8k at 0.8 updates/s (GPU 1 still shared).
- **2026-09-25 13:25 EDT: the /sata2 fill rate fell back to background (35 to 40 GB/h) at 13:05; the deletion ladder stays armed but is no longer imminent; PixArt slowed to about 0.8 updates/s by another user's job on GPU 1.** Free 420 GB at 13:17 EDT; at the background rate 300 GB comes about 16:15 EDT, and the ladder (RC 13:15) still applies at 300 and 150 GB unless Rohan runs his deletions first. The PixArt 70k and 75k holds are released, its probe_v2 series stopped (`stw6-arrive-pixart` killed; restart with `bash $D/tmp/steward/stw6_arrive_pixart.sh 80000` if wanted), the SD 3.5 90k hold kept. Throughput: SD 3.5 back to 0.47 to 0.54 updates/s; PixArt at 0.66 to 0.85 (about 57 percent of 1.42) from GPU 1 sharing with hengjinz's `extract.py`, load 19. ETA slips if the sharing persists: PixArt 200k from Sep 26 13:00 EDT to about Sep 27 evening (42 h for the remaining 121k steps at 0.8/s), SD 3.5 200k from Sep 27 19:00 to about Sep 28 01:00 EDT; both still before the Sep 30 deadline, and the Sunday-evening draft carries SD 3.5 numbers as provisional.
- **2026-09-25 13:15 EDT: /sata2 is filling at about 185 GB/h from another user's job; a failed checkpoint save kills a run; Rohan paged; deletion ladder set.** Free space 536 GB at 12:40 EDT, 450 at 13:01; none of our processes writes (per-process write counters 0 over a 30 s probe); onset matches hengjinz's `large_head_features.data prepare` at 12:42. Projection at that rate: 300 GB about 13:50, 200 about 14:20, zero about 15:25 EDT. `train_wm.py` prunes before writing (keep_last 2) but raises if the local write fails (no `--remote-results`), and best.pt is rewritten at every improving validation, so a full disk kills either run at its next save: SD 3.5 95k (37 GB, about 14:10 to 14:25 EDT, its prune frees the 85k file first), PixArt 80k (10 GB, about 13:25 to 13:45; its 70k prune freed nothing while the steward's holds pinned it, so the PixArt 70k and 75k holds are released and their probe_v2 passes dropped), SD 3.5 snap_0100000 (9.5 GB, about 15:40). The four deletions Rohan reserved for himself (hf_release_arnold 202 GB and raw_arnold 202 GB, both verified on the Hub; latents_arnold_sd35 41 GB; _aborted_safe_to_delete 39 GB) are unused by every live process and script (steward audit). Rohan paged by push with the commands. Ladder if he does not answer: at 300 GB free main deletes `_aborted_safe_to_delete` (agent scratch); at 150 GB main deletes `hf_release_arnold` and `raw_arnold`; the steward deletes nothing and reports every 15 minutes.
- **2026-09-25 12:55 EDT: two Spiderman host issues for Rohan: another user's job on GPU 1 is slowing PixArt, and /home is at 19 GB free.** From about 12:40 EDT hengjinz runs `extract.py --gpu 1` on PixArt's card (GPU 1 at 100 percent) plus a 4-core CPU job; PixArt fell from 1.40 to 0.67 to 0.84 updates/s and SD 3.5 from 0.52 to 0.40 to 0.44 (host load 55 on 64 cores; our chain is capped at 16 threads, nice 19). If it lasts, PixArt's 200k slips from Sep 26 13:00 EDT by roughly the hours it runs, and SD 3.5's Sep 27 19:00 by a fraction of that; asking the other user to move is Rohan's call. /home (3.4 TB shared volume with the conda envs, ~/.cache and the W&B config) is 100 percent used, 19 GB free; our share is 822 GB of older data (~/data 498 GB, ~/perseve 258 GB, .cache 23 GB, miniconda3 24 GB) and freeing it is Rohan's call; the Doom runs write to /sata2 (502 GB free) with the HF cache and TMPDIR there, and the steward checks that nothing of ours writes under ~ during the runs. Weekly update to Changliu drafted at `.claude/analyses/weekly-update-2026-09-25.md` (`d5bc486`).
- **2026-09-25 12:00 EDT: pre-registered test on all 30 maps, U-Net 200k EMA, one tic: partial Spearman -0.73 [-0.84, -0.33], permutation p 1e-4; verdict gate still "does not support" on the distance-validation checks; scores and figure frozen.** Primary (outcome mean(psnr_raw - persist_psnr_raw), covariate persistence PSNR): -0.734, bootstrap CI [-0.842, -0.325], p 0.0001; raw Spearman -0.44 [-0.70, -0.02]; raw-PSNR Spearman -0.37; leave-one-map-out 30 of 30 same sign (-0.70 to -0.77); within arenas (17 maps) -0.57 [-0.85, -0.02] p 0.017; within campaign (13) -0.38 [-0.79, +0.37] p 0.20; LPIPS gain -0.59 [-0.81, -0.21] p 0.001; episode-within-map rho 0.09 (342 episodes, p 0.22), so the relation lives between maps, not between episodes of one map. Conditions: CI excludes zero, leave-one-out, both clusters negative, every row: pass; pixel space: pending (chain); distance validation: False (checks ii and iv, RC 09:40 and 11:30), hence the pre-declared "does not support"; the gate question stays with Rohan and Astra. Frozen in the repo: `results/distance_study/scores/040-unet-nexttic_snap_0200000_ema_ddim10/*_h1/` (metrics, score keys, provenance) and `results/distance_study/figure_unet_h1/` (stats.json, distance_table.md, figures). The h4 pass runs now under the GPU 3 guard.
- **2026-09-25 11:45 EDT: GPU 3 tenancy order fixed after PixArt's own periodic reads lost labels to OOM.** Steward 7 found eval_0065000 lost both h4 labels and eval_0070000 lost tf_live_h1, tf_ema_h4 and the probe (the trainer's periodic reads have no guard and no retry) while the card held the SD 3.5 encoder (19.6 GB), the scorer (14.8), another user (4.6) and a steward read. Priority from now: PixArt periodic reads > SD 3.5 steward reads > distance scorer > evaluation-map encoder. Implemented: encoder wrapper v3 (`$D/tmp/encode_eval_maps_retry.sh`) also pauses while any `results_spiderman/*/eval_*/child.pid` is alive; the scorer gained a `MAP_GUARD` hook (`ca03bad`, script run before every map; a guard failure fails only that map) and runs with `$D/tmp/gpu3_guard.sh` (waits for 14 GB free and no periodic read); the steward recovers the lost 65k and 70k labels from the trainer's kept files. Seeded maps 12 to 15 scored at h1 (the scorer had resumed on its own); campaign 29 is the last h1 map, then the h4 pass.
- **2026-09-25 11:30 EDT: pre-registered test, U-Net 200k, one tic, 25 of 30 maps: partial Spearman -0.76 [-0.86, -0.36], permutation p 1e-4, but the pre-declared verdict is "does not support" because the distance validation gate (checks ii and iv) failed; and against true persistence the U-Net is below copy-last-frame on most unseen maps.** `paper/make_distance_figure.py` on the frozen SD 1.x distances and the scorer's h1 outputs (`$O/figure/`: stats.json, distance_table.md, distance_gain.png). Outcome per map = mean(psnr_raw - persist_psnr_raw), covariate = persistence PSNR (motion proxy). Primary: partial Spearman -0.76, bootstrap CI [-0.86, -0.36] (10,000 draws), permutation p 0.0001; raw Spearman -0.47 [-0.75, -0.01]; leave-one-map-out all 25 same sign (-0.72 to -0.79); within arenas (13 maps) -0.70 [-0.92, -0.17] p 0.008, within campaign (12) -0.38 [-0.82, +0.38] p 0.21; LPIPS gain -0.54 [-0.80, -0.08]; episode-within-map rho 0.08 (p 0.25, 316 episodes). Every verdict condition passes except `distance_validation`, which is the distances file's `checks.all_pass` and is False because (ii) subset agreement and (iv) same-WAD-nearer failed (RC 09:40); the script therefore prints "does not support" as pre-declared. Open for Rohan and Astra: whether (iv), an assumption about the maps rather than a check of the measurement, and (ii), a subset-level tolerance stricter than the bootstrap precision the test uses, belong in the gate; I did not change the gate after seeing the data. The gains themselves, against the raw-frame persistence: validation maps +0.5 to +1.8 dB; near unseen arenas 17 and 16 +0.53 and +0.77, seeded 11 +0.35; most other unseen maps at or below zero: arena 8 -0.92, arena 6 -0.31, arena 7 +0.05, seeded 1 / 9 / 10 -0.23 / -0.06 / -0.36, campaign 20 / 32 / 28 / 24 / 18 / 19 / 25 / 31 / 22 / 26 / 30 / 23 = -0.13 / -0.68 / -1.83 / -2.44 / -1.02 / -2.35 / -2.40 / -0.19 / +0.03 / -3.98 / -0.75 / +0.17; LPIPS gain negative on every unseen map (-0.06 to -0.21). So at one tic on unseen maps the U-Net row mostly loses to copy-last-frame on both metrics, most on the campaign maps whose seeded footage is nearly static (persistence 23 to 27 dB, 1.0 to 1.4 lives per episode; whether the agent is stuck there needs a look at the frames). The 09:50 first look (Spearman -0.88, gains +0.1 to +2.4) used `copy_psnr_raw` and is superseded. Missing at h1: seeded 12 to 15 and campaign 29, which the scorer retries when GPU 3 frees; the statistics rerun on all 30 then, and on SD 3.5 at its final 200k.
- **2026-09-25 10:40 EDT: the re-encoded `seen` latents equal the old ones, so the frozen SD 1.x distances stand; seeded map 1 is rescored.** Old (7f7d0b1, Sep 21) versus new (190c125) latents of ep_00000 and ep_00003: same shape and dtype, correlation 0.99999, mean relative difference 0.2 percent (bf16 nondeterminism, same VAE), so only the sidecars were wrong and the distances frozen at `01de829` are kept; the chain now skips the SD 1.x block and runs pixels then SD 3.5, thread-capped. The old sidecars' narrow-string episodes passed the dtype check with the wrong button semantics, so seeded map 1's first score (gain 0.84 dB) fed the model wrong control tokens: its outputs were removed and the scorer's next attempt rescores it with the re-encoded sidecars, together with maps 9 to 15. Steward: both trainers back to rate (SD 3.5 0.51 to 0.55, PixArt 1.39 to 1.47 updates/s, load 17); 85k directional EMA correct_frac 0.828 (70k 0.805, 75k 0.8125), so the 85k probe_v2 rise does not pair with a directional drop; the live 85k directional reruns when GPU 3 has room, and from 90k the directional runs after every full read automatically; the steward fixed its guard to count only the yielding encoder as free memory (the re-encode does not yield). U-Net scoring: 22 of 30 maps at h1, h4 pass under way, 8 maps pending retry.
- **2026-09-25 10:20 EDT: the pixel-space distance job saturated Spiderman's 64 cores and halved both trainers' throughput for about 50 minutes; killed and relaunched thread-capped.** Steward 7 caught it: `distance_study.py distances --space pixels` ran 64 BLAS threads (load average 128 on 64 cores), SD 3.5 fell from 0.50 to about 0.27 updates/s (GPU 2 starved on the data side) and PixArt from 1.38 to about 0.9 between 13:26 and 14:17 UTC; the earlier SD 1.x distances step (11:20 to 12:55 UTC) will have done the same unnoticed. Fix: the chain (`$D/tmp/distance_chain.sh` v2) now exports `OMP/MKL/OPENBLAS_NUM_THREADS=16` and runs at nice 19, waits for the `seen` re-encode marker, and reruns from the SD 1.x clouds (so the seen maps get their re-encoded latents), then pixels, then SD 3.5 after its encode pass. Rule from now on: any CPU-heavy study job on Spiderman is thread-capped at 16 and niced; the steward watches wall time per 50 steps and reports a trainer under 90 percent of its rate. SD 3.5's 200k time slips by about 25 minutes from this.
- **2026-09-25 10:10 EDT: 28 of the 60 `seen` evaluation episodes carried Sep 21 sidecars from an older encoder; moved aside and re-encoding.** The scorer failed on the seeded maps 9 to 15 (`doom_data.check_sidecar_buttons_dtype`: buttons column up to <U36, wider than the 19-button executed control). Audit: `latents_arnold_eval_pertic/seen/ep_00000..00027` were written on Sep 21 by encoder commit 7f7d0b1 (ENCODE_LOG) with buttons stored as Arnold's raw request strings of varying width (even <U9 rows are a different button semantics), while the 32 episodes written today and every unseen and unseen2 episode are <U19 from the pinned encoder 190c125; the Sep 24 encode skipped the 28 because they existed. The 56 files are in `$D/_aborted_safe_to_delete/seen_old28_sep21/` (README inside) and `$D/tmp/reencode_seen.sh` (tmux `reenc-seen`, log `$D/logs/reencode_seen.log`, marker `RE_ENCODE_SEEN_OK`) re-encodes them with the pinned encoder on GPU 3 behind a 22 GB guard. Consequences: the SD 1.x clouds of the seen maps (1, 9 to 15, and the replications of 2 to 8) used the old latents, so after the re-encode one old and new latent file are compared; if they differ the chain reruns from the SD 1.x clouds and the distances are re-frozen before any seen map is scored (the 16 maps in the first look are unaffected except seeded map 1, whose latents may change); the scorer retries the seven failed maps on its next attempt. The two `seen` maps scored so far under the old sidecars (map 1 succeeded, its buttons happened to be narrow) will be rescored with RESCORE=1 if the latents differ.
- **2026-09-25 10:05 EDT: SD 3.5 85k: teacher-forced bests again, rollouts softer at 256 tics on both weight sets, channel 13 excursions without capture.** TF h1 live 22.83 / 0.157, EMA 22.96 / 0.146 (+1.39 dB); h4 live 21.16 / 0.233, EMA 21.30 / 0.216 (+2.09 dB). Rollouts at 4/32/64/128/256: live 21.28 / 18.42 / 17.66 / 17.45 / 16.99, EMA 21.02 / 19.05 / 18.44 / 18.10 / 17.44 (0.08 dB under copy-seed at 256), copy-seed 18.93 / 17.47 / 17.60 / 17.80 / 17.52. EMA 256-tic trend over three reads: 18.54 (75k), 17.94 (80k), 17.44 (85k), while TF keeps improving. Channel 13: live min -1.19 with five rollouts under -0.9 for 4 to 30 tics (four recovered), EMA one rollout under -0.9 from tic 104 ending at -1.00 and 13.3 dB, the first EMA excursion of that size (70k and 80k EMA never went under -0.54); nothing captured at the -1.4 or -2.6 fixed points. Probe_v2 mean 2.4 / 3.4 / 2.4 (up from 80k). Not a flag by the rule (no collapse, no directional read yet); the steward runs the directional check on the held 85k weights before release, and reports 90k's 256-tic point with onset tics if the downward trend continues. SD 3.5 at 87.8k (val 0.0897), PixArt at 65.2k, 552 GB free.
- **2026-09-25 09:50 EDT: first look at the distance study (SUPERSEDED at 11:30: this look used `copy_psnr_raw`, which decodes the last latent and carries the decoder's reconstruction error, so every gain below is about 1.4 dB too high; the pre-registered outcome uses `persist_psnr_raw`, the raw last frame).** U-Net 200k EMA, 256 windows per map, raw-frame PSNR minus copy-last-frame on the same windows, joined to the frozen SD 1.x nearest-training-map distance: validation maps (D 0.03 to 0.07) gain 1.90 to 2.43 dB; arena 8 (0.115) 1.03, arena 17 (0.130) 1.03, campaign map 20 (0.140) 1.15, arena 16 (0.164) 1.32, seeded map 1 (0.179) 0.84, campaign 24 / 18 / 19 (0.18) 0.24 / 0.72 / 0.71, arena 6 (0.188) 0.67, campaign 22 (0.220) 0.71, arena 7 (0.282) 0.81, campaign 23 (0.371) 0.09. Spearman of gain on D -0.88 over the 16 primary maps scored so far, -0.64 for raw PSNR, +0.67 for LPIPS, and -0.14 for the copy-last PSNR itself, so the relation is in what the model adds and not in map difficulty; restricted to the 12 out-of-distribution maps it is about -0.7. This is not yet the pre-registered test (partial Spearman controlling for motion over all 30 maps with bootstrap), which runs when the h1 pass completes (14 maps to go, then h4). Literature memo `.claude/analyses/distance-study-literature-2026-09-25.md` (Opus reader, `414e6ee`): no prior work correlates train-to-domain distance with a generative world model's quality across held-out scenes; templates are OTDD (Alvarez-Melis and Fusi 2020, Spearman -0.59 to -0.85 over 11 to 16 domain pairs), Deng and Zheng 2021 (Fréchet distance versus accuracy, -0.91) and Blitzer et al. 2007; distance measures have also lost (Guillory et al. 2021), so a null framing is precedented; copy-last-frame baselines were routine in 2014 to 2019 video prediction (Ranzato, Mathieu, Finn, PredNet, ContextVP, SDC-Net, Villegas 2019 where it beat every model on Human3.6M) and are absent from 2024 to 2026 world models (GameNGen, DIAMOND, Genie, NWM, Vista, GAIA, Matrix-Game); Genie's delta-tPSNR has our difference-of-PSNRs form; recommended figure: one labelled marker per map with rho, CI and p in the panel, as OTDD.
- **2026-09-25 09:20 EDT: SD 3.5 80k: new bests, both weight sets above copy-seed in rollouts, no collapse.** TF h1 live 22.70 / 0.156, EMA 22.92 / 0.148 (+1.35 dB); h4 live 20.90 / 0.239, EMA 21.25 / 0.219 (+2.04 dB). Rollouts at 4/32/64/128/256: live 20.75 / 18.67 / 17.77 / 18.13 / 18.26, EMA 21.15 / 18.94 / 18.43 / 18.65 / 17.94, copy-seed 18.93 / 17.47 / 17.60 / 17.80 / 17.52; no rollout under 10 dB; channel 13 stays inside its data range on both weight sets (live min -0.73, EMA -0.54, fixed points were -1.4 and -2.6). Probe max 17.5 / 8.6 / 16.9, probe_v2 mean 2.2 / 1.0 / 1.5 (down from 75k, still 3 to 6x the 70k mean); no flag. 85k TF already better again (EMA h1 22.96 / 0.146, h4 21.30 / 0.216), rollouts in progress. SD 3.5 at 86.5k (val 0.0897, new low), PixArt at 61.9k (val 0.1685), its probe_v2 flat at 45k to 55k. U-Net scoring: 3.5 minutes per map at h1 (5.0 sampling frames/s), so h1 over 30 maps takes about 1.8 hours and h4 follows. GPU 3 carries four tenants (a PixArt read, an SD 3.5 read, the scorer, another user's 4.6 GB job).
- **2026-09-25 09:40 EDT: SD 1.x map distances computed and frozen (`01de829`); U-Net 200k per-map scoring started on GPU 3; steward 7 launched.** The chain (`distance-clouds`) finished the SD 1.x space: 37 map points (30 primary, 7 replications of maps 2 to 8 from the seeded corpus), motion arm primary (check v: |Spearman(D, n_eff)| 0.02). Floor band (train-versus-train, 40 single-subset clouds) 0.022 to 0.093; the four validation maps sit inside it (check i pass: 0.026 to 0.069), the two reference draws rank the maps alike (check iii, Spearman 0.996), and the bootstrap attenuation ratio is 0.089 (map-level D is precise enough that a correlation with D is attenuated under 1 percent). Nearest-training-map D, motion arm: arenas_678 map 8 0.115, map 6 0.188, map 7 0.282; unseen arenas 17 0.130 and 16 0.164; the seeded "seen" maps 1 and 9 to 15 (training maps of the April model, not of the next-tic rows) 0.127 to 0.180; campaign maps 18 to 32 from 0.140 (map 20) to 0.371 (map 23), with 30 at 0.338, 26 at 0.273, 29 at 0.235. Two checks fail and are recorded as findings, not measurement faults: (iv) same-WAD arenas are not all nearer than every campaign map (max same-WAD 0.282 for map 7 against min campaign 0.140; means 0.195 versus 0.218), so "same WAD" is not a proxy for closeness in latent space; (ii) the 4-episode disjoint subsets of eight campaign maps disagree by a median 11 to 29 percent (18: 0.29, 20: 0.20, 26: 0.20, 29: 0.20, 24: 0.20), which is subset-level noise that the 10-subset mean and the bootstrap already absorb (validation maps are exempt inside the floor band; every replication of maps 6 to 8 agrees within 4 percent). The pixel space is being computed now (clouds written, distances next), SD 3.5 after its encode pass (36 of 210 episodes, about 10 hours at the shared-card pace). Decisions taken under "continue with everything" (reversible, scores can be discarded): the pre-registered primary test stays as the memo declares it (partial Spearman of gain over persistence on nearest-map D controlling for motion, 30 primary maps, 200-redraw bootstrap, leave-one-map-out); the SD 1.x distances were frozen in the repo before any per-map score existed; the per-map scoring of the U-Net 200k EMA (final weights, so no selection is possible) runs now on GPU 3 (`$D/tmp/score_unet_wrap.sh`, tmux `distance-score`, waits for 14 GB free, retries failed maps; 30 maps at h1 then h4, about 1 to 2 hours; output `$D/results_spiderman/distance_study/040-unet-nexttic/snap_0200000_ema_ddim10/`); nothing is booked in the seal, and Rohan can still rule the sealed-corpora question the other way. SD 3.5 is scored only at its final 200k. Steward 6 went silent after 04:50 EDT (its automation `stw6-arrive` kept reading: 80k done, 85k in progress, both unreported); steward 7 launched at 09:35 with the same brief plus the new rules (probe_v2 on every read, directional check from 90k on, the scorer sharing GPU 3).
- **2026-09-25 05:00 EDT: at 75k real control swaps are normal; the probe jump is a broad response to the off-manifold all-bits-flipped token.** Directional check at 75k (64 windows per direction, ref_frac 0.914, raw 0.898): EMA correct_frac 0.8125 (left 0.797, right 0.828; recorded median shift +17.8 / -18.8 px against ground truth +18.2 / -19.1; swapped -18.6 / +21.2; motion ratio 0.941), live 0.859 (0.844 / 0.875; +19.4 / -19.0, swapped -19.6 / +22.0; motion ratio 0.920). Against 70k EMA (0.805, 0.938) unchanged. Probe v2 (`caf58bb`, all 19 newest-control bits flipped at t=500, seeds 0/1/2, max / mean / p99): 60k 2.3 / 0.14 / 0.86 (typical of three seeds), 70k 3.2 / 0.36 / 1.5, 75k 71.0 63.9 17.2 / 6.9 7.6 3.2 / 64.5 55.6 14.3, so at 75k the whole difference field moves (mean 15 to 30x higher than at 70k), not one element. Recorded interpretation: the flipped token is off the data manifold, so this is an extrapolation property of the control embedding, watched and not acted on while real swaps, TF and rollouts are normal; the probe v2 pass joins every read from 80k on, and a future pairing of a probe jump with a directional drop or a live collapse gets flagged. 75k hold released (kept by the trainer until 85k); 80k read auto-queued about 05:30 EDT.
- **2026-09-25 04:30 EDT: SD 3.5 75k: best teacher-forced and best EMA rollouts yet, live collapse did not recur; the probe max-sensitivity is a tail statistic.** TF h1 live 22.73 / 0.154, EMA 22.88 / 0.150 (+1.31 dB over persistence); h4 live 20.96 / 0.238, EMA 21.19 / 0.223 (+1.98 dB). Rollouts at 4/32/64/128/256: live 21.30 / 18.55 / 17.43 / 17.85 / 17.63 (none under 10 dB), EMA 20.92 / 18.77 / 18.02 / 18.31 / 18.54 (above copy-seed 18.93 / 17.47 / 17.60 / 17.80 / 17.52 at every horizon past 4 tics, the best EMA rollout of any read); live LPIPS at 256 0.611, EMA 0.542, copy-seed 0.520. Live collapse history is intermittent, not progressive: 50k yes, 55k to 65k no, 70k yes on two seeds, 75k no. Probe (seeds 0/1/2): 75k 71.0 / 63.9 / 17.2 against 3.0 / 3.4 / 3.2 at 70k and 9.1 / 8.3 / 8.5 at 65k, update ratios and probe loss ordinary. Read of `smoke_probe.py`: that number is the MAX absolute element of the v-prediction difference when all 19 bits of the newest control are flipped at t=500 on one batch, an out-of-distribution token and a one-element tail statistic, so it says the response to an impossible control is heavy-tailed, not that real controls act 20x more strongly; `caf58bb` adds the mean and 99th-percentile fields, and the steward reruns the probe on 65k/70k/75k and runs the directional check on 75k EMA and live, which measures real swaps. PixArt at 39k (val 0.1758, monotone), periodic reads 5k to 35k ok (30k: live h1 +0.33 dB, h4 +1.30 dB, EMA h1 +0.14 dB); SD 3.5 at 77.7k (val 0.0913 low); snapshots 10k to 70k mirrored; `stw6-arrive` now holds and queues each 5k checkpoint automatically; the encoder is on the 4-channel unseen pass; 581 GB free.
- **2026-09-25 02:30 EDT: the SD 3.5 70k live collapse reproduces on seed 1 and is a single latent channel captured by a fixed point, not a scale blow-up.** Seed-1 live rollouts: 20.57 / 15.62 / 14.68 / 12.96 / 10.95 at 4/32/64/128/256 tics (seed 0: 20.72 / 15.20 / 15.29 / 13.69 / 11.11); 15 of 16 crossed the EMA-minus-3-dB line, 10 of 16 under 10 dB at 256 tics, across maps 2 to 5 (50k had 8 of 16 on seed 1). Latent statistics (`stw6_latstats.py`, `steward_70000/latstats_*.json`): the RMS of the predicted latents stays inside the ground-truth range (0.98 to 1.19) in almost every collapsed rollout, so the latent neither explodes nor vanishes; instead SD 3.5 latent channel 13, whose per-frame mean sits at about +0.45 in data (range -0.085 to 0.70), drops to one of two fixed values shared across windows and maps: -1.39 to -1.50 in 17 of 22 collapsed rollouts over both seeds, -2.60 to -2.73 in 4 (the ones with RMS 1.35 to 1.48). Those are the blank frames at 5.8 to 10 dB. On seed 0 channel 13 leaves its range at the PSNR drop (tics 16/16, 88/90, 137/137, 30/31, 80/85); on seed 1 six rollouts drop at tic 1 while channel 13 leaves later (tics 2 to 21, or 160 to 208), so the channel capture is the end state and not always the trigger. EMA references on the same windows keep channel 13 at or above -0.44 and end near +0.4; one healthy live rollout dipped below -1 at tic 32 and recovered. Reading for the paper: with the live weights, closed-loop generation has an absorbing state in one latent channel that the noise augmentation (0.7, 10 buckets) does not cover, and the EMA is what keeps the released model out of it; whether the EMA stays clear at 75k+ is the next read. Also: `snap_0070000.pt` mirrored and verified; PixArt 25k periodic read ok (live 21.79 / 0.229 at h1, 20.50 / 0.311 at h4; EMA nearly warmed at 21.48); SD 3.5 at 73.3k (val 0.0924), PixArt at 27.1k (val 0.1815), 587 GB free.
- **2026-09-25 02:10 EDT: the directional check validates on real frames; both rows turn the way the control says and neither learned persistence.** `directional_check.py`, EMA weights, 64 windows per direction, about 3 minutes each on GPU 3. Estimator check first: the ground-truth next frame moves the way the control implies in 0.91 to 0.92 of windows (decoded) and 0.90 (raw frames) for both rows, so the shift estimator and the left-turn-positive sign convention hold. SD 3.5 70k EMA: correct_frac 0.805 (left 0.797, right 0.813); recorded-control median shift +17.8 px left / -18.3 px right against +18.2 px ground truth; swapped control reverses it (-18.8 / +21.1); the predicted sign matches the control in 0.90 of windows; closed-loop 4-tic motion ratio 0.938 (per step 0.972 / 0.927 / 0.937 / 0.917). U-Net 200k EMA: correct_frac 0.867 (0.859 / 0.875); median shift +19.1 / -19.1 px, swapped -19.8 / +22.1; sign match 0.92; motion ratio 0.893 (0.964 / 0.876 / 0.867 / 0.864). Persistence scores 0 on both by construction. Reading: the U-Net reverses under the swap a little more often (0.87 versus 0.80); SD 3.5 keeps more of the true motion over four tics (0.94 versus 0.89); neither collapsed to persistence, which answers review 7.2. JSONs `directional_0070000_ema.json` and `directional_0200000_ema.json` sit beside the checkpoints. Steward queue fix: its 25 GB guard now counts the encoder's GPU 3 memory as free for SD 3.5 reads (the encoder yields once the read session exists), so the guard and the yielding encoder no longer wait on each other; the 70k seed-1 live rerun and the latent-statistics read of the collapsed rollouts follow.
- **2026-09-25 01:40 EDT: SD 3.5 70k: best teacher-forced numbers yet, live rollouts collapsed again while EMA held.** TF h1 live 22.63 / 0.161, EMA 22.83 / 0.152 (persistence 21.57 / 0.203, EMA +1.27 dB); h4 live 20.60 / 0.256, EMA 21.15 / 0.226 (+1.94 dB). Rollouts at 4/32/64/128/256 tics: live 20.72 / 15.20 / 15.29 / 13.69 / 11.11 (LPIPS 0.70 at 256, decoded PSNR 11.2: the latents leave the data range), EMA 20.71 / 18.85 / 18.42 / 17.89 / 16.84, copy-seed 18.93 / 17.47 / 17.60 / 17.80 / 17.52. Per rollout, live: 15 of 16 crossed the EMA-minus-3-dB line, 11 below it at 256 tics, 11 ending between 5.8 and 10.1 dB (blank frames), onsets from tic 1 to 137 across maps 2 to 5; EMA 13 crossed, 0 stayed down, min 9.7. Pattern over the full reads: live collapsed at 50k (8.98 at 256), recovered at 55k to 65k (17.2 to 17.8), collapsed again at 70k; val loss unaffected (72k 0.0917, a new low). Probe (seeds 0/1/2): sensitivity 3.00 / 3.44 / 3.24, so the 65k jump (8.3 to 9.1) did not persist; control-MLP update ratio 0.095. Reading: the live weights pass through periods where closed-loop generation is unstable while teacher-forced quality keeps improving, and the EMA (0.9999, fp32) filters them out; the steward reruns the 70k live rollout with seed 1 after the directional check and reads the per-tic latent norms of the collapsed rollouts (exploding versus vanishing). PixArt 20k periodic read ok: live 21.76 / 0.237 at h1 (+0.19 dB), 20.34 / 0.320 at h4; sensitivity 1.56; at 24k, val 0.1836.
- **2026-09-25 01:25 EDT: the distance-study pipeline is on main and running on Spiderman; per-map scoring waits on Rohan.** Worker delivered (suite 1,512): `distance_study.py` subcommands `clouds` (SD 1.x draws the frames, 50 seeded episodes per training map from ids 0:2000, 250 frames per episode stratified by motion decile, a disjoint second draw as the floor; SD 3.5 and pixels read the same episodes and tics via `--frames-from`), `splits` (one split file per primary map), `distances` (fast sorted-reference 1-D W2, equal to the committed function to 8e-14; map D = mean over ten 4-episode subsets of the nearest-training-map distance; floor band from 40 single-subset clouds; checks i to v with the motion arm primary unless check v fails; best training-map mixture by pattern search; 200-redraw bootstrap), `scripts/spiderman/score_distance_maps.sh` (resumable per-map `eval_tf` reads, flock, score key and provenance per map, refuses the test corpus and checkpoints without EMA), `paper/make_distance_figure.py` (partial Spearman controlling for motion with bootstrap CI and permutation p, leave-one-map-out, within-cluster, per-row and per-space reads, section 5 verdict, appendix table, light and dark figures), memo section 7 with the run order. Not implemented: the episode-level mixed model (needs statsmodels), the decoder-free copy-last latent ratio, stratification by control regime. My change: check (ii) no longer judges maps inside the floor band (there D is draw noise; the toy study saw 6 to 20 percent pair differences on the validation maps). Runs: the smoke run (two episodes per map, 100 directions) passed on real data in `$D/tmp/distance_smoke` (`SMOKE_OK`, 17 maps, primary arm motion); the chained CPU job `distance-clouds` (`$D/tmp/distance_chain.sh`, log `$D/logs/distance_chain.log`, checkout `$D/repo_distance` at main) waits for the encode's `SD15_EVAL_MAPS_DONE`, then runs SD 1.x clouds, splits and distances, the pixel space, and after `SD35_EVAL_MAPS_DONE` the SD 3.5 space; markers `DISTANCES_<space>_DONE`, `DISTANCE_CHAIN_OK`. Encode at unseen2 99/130, seen 28/60, unseen 0/20 in the 4-channel pass (about 1.5 h once the 70k read releases GPU 3), SD 3.5 pass about 5 h after. Waiting on Rohan before any per-map score is read: (1) agree the pre-declared primary test (partial Spearman of gain over persistence on nearest-map D controlling for motion, over the about 30 primary maps, 200-redraw bootstrap CI, leave-one-map-out); (2) the sealed corpora rule: `after_nexttic.sh` treats arenas_678, seen, unseen and unseen2 as sealed, and the scorer reads them once with a fixed checkpoint without booking anything in `$R/sealed`, so either book these reads or declare fixed-checkpoint distance reads exempt; (3) the GPU 3 slot for scoring (U-Net 200k EMA at h1 and h4 first, about 1 h). Steward: SD 3.5 70k read and the first directional run (`stw6-sd35-dir70k`) on GPU 3; PixArt at 24k, SD 3.5 at 72k, disk 611 GB free.
- **2026-09-25 00:45 EDT: the directional check is on main (`999f6b2`); the evaluation-map encode now yields to steward reads.** `directional_check.py` (worker, 22 tests, suite 1,464): draws turning windows from the val split (newest executed control holds exactly one of TURN_LEFT/TURN_RIGHT and no strafe; next four tics inside one life), predicts each twice from the same noise with the two turn bits swapped in the newest control only, decodes, and measures the horizontal shift of each frame against the decoded last context frame (normalised cross-correlation over ±32 px on rows 48 to 120 of a central crop, sub-pixel by parabola; left turn = scene slides right = positive shift; shifts under 1 px count as no motion). Reports `correct_frac` (sign flips under the swap), `ref_frac` (the ground-truth next frame moves the way the control says: the estimator and sign check, read first), the raw-frame reference with `--parquet-dir`, and the closed-loop 4-tic motion ratio of review 7.2 on the same windows (persistence scores 0). Summary line `DIRECTIONAL_CHECK <run> <step> correct_frac=<x> ref_frac=<y> weights=<live|ema>`. Not yet run on real frames: the sign convention is unproven until `ref_frac` is read; the steward runs it on SD 3.5 70k EMA and U-Net 200k EMA from `$D/repo_tools` after the 70k read. The requested-action row is refused (no control token to swap). GPU 3: the encode wrapper v2 (`$D/tmp/encode_eval_maps_retry.sh`) starts only with 25 GB free and no `stw*-sd35-*` tmux session, kills its encoder when one appears and resumes after (skip-existing), so steward reads no longer wait on it; the SD 3.5 70k read (TF on the snapshot, three-seed probe) started 00:25 alongside PixArt's 20k read, 15.9 + 13.5 GB, due about 01:25. PixArt past 20k, SD 3.5 past 70k.
- **2026-09-24 23:30 EDT: SD 3.5's control sensitivity rose 3.8x between 60k and 65k, reproduced on three seeds; the distance-study design is on main and its pipeline is being built; the directional check is being written.** SD 3.5 65k read: EMA 22.77 / 0.154 at h1 (+1.21 dB, best of any row) and 21.07 / 0.231 at h4; rollouts EMA 20.64 / 18.77 / 18.39 / 17.97 / 17.54 at 4/32/64/128/256 tics (above copy-seed at 256). Newest-control sensitivity (`smoke_probe.py`, seeds 0/1/2): 60k 2.28 / 2.21 / 2.21, 65k 9.13 / 8.30 / 8.48, 55k 2.05; the control-MLP update ratio 0.081 at 60k to 0.155 at 65k, other ratios unchanged, probe loss identical across checkpoints per seed, val flat at 0.0926, no gradient spikes; so between 60k and 65k the model began responding about four times more strongly to the newest control while teacher-forced quality improved. Whether the response has the right sign needs the review's directional check (swap TURN_LEFT and TURN_RIGHT in the newest control, measure the horizontal shift of the predicted frame against the ground truth), which a worker is implementing now as `directional_check.py` with the 7.2 motion ratio; the steward reruns the probe with three seeds at 70k. Distance study: memo `.claude/analyses/distance-study-design-2026-09-24.md` and the `distance_study.py` core (weighted sliced Wasserstein, motion weights, pooling, nearest-training-map reference; 17 tests) merged at `b3d87bc` (suite 1,443); recommendations: nearest-training-map distance rather than the pooled corpus, SD 1.x pooled latents as the primary space with pixels and SD 3.5 latents as checks, Rohan's motion weighting, no time coordinate, gain over each map's own persistence reference as the outcome, one pre-declared partial-Spearman primary test over 30 maps with bootstrap and leave-one-map-out; a worker builds the `clouds`/`splits`/`distances` subcommands, `scripts/spiderman/score_distance_maps.sh` and `paper/make_distance_figure.py`. PixArt 10k periodic read ok (live 21.52 / 0.282 at h1, level with the U-Net at 10k); PixArt at 12.9k, SD 3.5 at 68k; the evaluation-map encode at 20 of 130 under its memory guard. Pending Rohan: agree the primary test; GPU 3 for the scoring pass tomorrow; the GPU 2 plan after Sep 27.
- **2026-09-24 22:50 EDT: Superman finished both 2000:6000 sets; the lab-server dashboard refreshed and back on an hourly cron.** Superman's second orchestrator logged `ORCH_DONE` at 21:30 EDT: the SD 3.5 2000:6000 phase completed (4,000 episodes, row-count audit 0 mismatches) after the 4-channel phase at 03:00; both sets live under `$D/ext_2000_6000/` on Spiderman and are uploaded to `sd15/arenas_2000_6000` and `sd35/arenas_2000_6000` on the Hub by the uploader (pruned locally as they verify); every Superman card is free again, its disk at 155 GB. The 6,000-episode row is therefore ready to launch on GPU 2 after SD 3.5 stops on Sep 27 (re-download of the 4-channel set from the Hub, about 250 GB, plus a hard-linked 6,000-episode directory and a gate run), pending Rohan. Lab-server dashboard: `tools/server_usage.py` now reads the next-tic runs and the dense corpora; a fresh snapshot with directory sizes was written to the artifact database (`usage/latest`, `history/1790304241`), and a session cron refreshes it hourly at :23 (sizes once a day at 03:23). PixArt at 11.2k (val 0.196), SD 3.5 at 67.5k (val 0.0926); the evaluation-map encode runs on GPU 3 under its memory guard.
- **2026-09-24 22:20 EDT: the map-distance generalization study is go (Rohan); design memo and encode started.** Question (Changliu's suggestion): measure the distributional distance between each evaluation map's footage and the training footage and correlate it with the model's quality drop on that map, reusing Rohan's NVIDIA method (tests as weighted clouds of per-frame latents, motion-weighted frames, sliced Wasserstein). Astra is out of credits, so an Opus design worker writes `.claude/analyses/distance-study-design-2026-09-24.md` (what transfers from the driving setting, the distance definition with weighting and time coordinate decided, the reference cloud, three feature spaces: pixels, VAE latents, the model's own context features; outcome variables and confounds including the per-map persistence reference; statistics for about 22 map points and about 300 episode points; alternatives; the figure; implementation plan) plus a CPU prototype `distance_study.py` with tests; Astra reviews it when back on Sep 26. Data: per-tic latents exist for maps 2 to 5 (val) and 6 to 8; the seeded evaluation corpus `raw_arnold_eval` (per-tic raw: seen 60 episodes on the original 17 maps, unseen 20 on arenas 16 and 17, unseen2 130 on 13 campaign maps) had only a 28-episode fragment encoded, moved to `_aborted_safe_to_delete/`; `enc-eval-maps` (log `$D/logs/encode_eval_maps.log`) now encodes all 210 episodes per tic in both spaces on GPU 3 with the pinned encoder (`$D/repo` at 190c125, same canonical table and settings as the training corpora), 4-channel first (about 3 hours) then SD 3.5 (about 5), sharing the card with the reads. PixArt's first periodic read (5k, 22 minutes, status ok): live 20.91 / 0.305 at h1 and 19.10 / 0.410 at h4, EMA still warming; its evaluators logged natively to `041-pixart-nexttic-eval` in the run's group, so only the trainer's init was hit by the fork bug.
- **2026-09-24 21:55 EDT: the native W&B init hang is root-caused, fixed and verified live (`11540db` on main).** Cause (worker, reproduced with the real SDK 0.30.0 offline): inside `wandb.init` the SDK starts subprocesses on the calling thread (`git rev-parse` in `wandb/sdk/lib/gitlib.py:57` via `wandb_init.py:324` and `:933`, `uname -p` from `platform.platform()` in `wandb_settings.py:1628`, the `wandb-core` service in `service_process.py:149`) and waits for end-of-file on each pipe; the trainer's main thread forks its persistent DataLoader workers at the same moment, a worker forked inside that window inherits the pipe's write end and never closes it (it never execs), so the read never sees end-of-file and init hangs for the life of the run. The live signature (core log "accepting connections", "new connection created", then nothing; no run directory; no "View run" line) is reproduced exactly by forcing a fork during `uname -p`. Fix: `RunLogger` calls `wandb.setup()` and `platform.platform(aliased=True)` on the caller's thread in its constructor, before the trainer forks (0.24 s, no network), opens runs with `disable_git=True`, and installs a backstop audit hook that refuses any subprocess started on the logger thread; a real-SDK subprocess test forks eight workers during init and passes in 0.8 s where the old code hung 60 s. Suite 1,424 (1 skip: the real-SDK test where wandb is absent). Verified on Spiderman in the doom env with the real SDK online: a RunLogger under eight forking DataLoader workers created its run, logged 16 rows, closed in 1.7 s (check run deleted). The live PixArt run keeps its stuck logger thread (harmless; its curves come from the sidecar `041-pixart-nexttic-tail`, its periodic reads use zero workers and are unaffected); every launch from now on runs with the fix. U-Net light backfill complete: the 5k read series is unbroken from 5k to 200k (EMA h1 +0.79 dB at 110k to +0.92 at 200k). SD 3.5 65k read and PixArt's first periodic read overlap on GPU 3 now.
- **2026-09-24 21:05 EDT: the PixArt trainer's native W&B init hung; the sidecar carries its curves; GPU 3 reads may overlap.** On the live PixArt run the trainer created `.wandb/`, its `wandb-core` service started and accepted one connection, and the Python side never sent the init record (no run directory, no "View run" line, no run on the server after eight minutes); training is unaffected because the logger runs on a daemon thread, and the same SDK works from the sidecar's main-thread init. Every `wandb_log` test used a stub, so the real SDK's init on a worker thread was never exercised: a testing gap, now being root-caused by a worker against the real SDK offline (thread versus main-thread init, import order, DataLoader workers, redirected stdio, `wandb.setup`), with a real-SDK subprocess test to follow. Fallback in place: `tools/wandb_tail.py` now also tails the trainer's own `eval_<step>/` reads (`624c773`) and runs as tmux `wb-pixart` under the id `041-pixart-nexttic-tail`. The trainer's first periodic read on GPU 3 is due about 21:35; its `status.json` is the check. GPU 3 policy (steward): SD 3.5 full reads every 5k may overlap PixArt's periodic reads on the 49 GB card, guarded by 25 GB free at start and one steward read at a time; the U-Net light backfill fills gaps. SD 3.5 at 62k, val 0.0934, 60k read in progress.
- **2026-09-24 20:35 EDT: PixArt-alpha row LAUNCHED on GPU 1 (`041-pixart-nexttic`, commit `f5386f1`), the first row on the new code: native W&B logging and off-card periodic reads.** Launcher and gates generalised (worker, suite 1,423): `dit` backbone entry, `RUN_NAME` override with the certificate keyed by run name (live entries `unet` and `sd35` at `35258f3` untouched and verified byte-equal through a gate run), `DIT_GPU`/`PIXART_GPU` and per-backbone micro-batch, `ACTION_HISTORY` printed in the launch line; caveat recorded: the ImageNet DiT averages its control tokens (no cross-attention), so it cannot see their order, Rohan's call before any DiT row. PixArt gates from `repo_launch2` 20:13 to 20:31 EDT, all passed: audits 0 mismatches, latent alignment, fit 1.141 updates/s at 26.8 GB (27.7 reserved, no checkpointing), 300-step smoke (9.4 GB recovery checkpoint, 2.4 GB snapshot), probes, resume, four readbacks. Launched 20:32 with the printed `GATES_LAUNCH pixart` line (`MB=32 WORKERS=12 STEPS=200000 ACTION_HISTORY=32 EVAL_EVERY=5000 EVAL_DEVICE=cuda:3`), 628 M parameters, `--action-inject token`, 2,000 training and 100 validation episodes, 1.37 updates/s at step 100, 28.8 GB; config records `wandb: true`, run `041-pixart-nexttic`, periodic reads every 5k on cuda:3 into `eval_<step>/`. Expected 200k about Sep 26 13:00 EDT. Rohan's decisions today: PixArt-alpha (Chen et al., ICLR 2024) over DiT for the third row because it is a text-to-image DiT like the other two backbones and was the best transformer of the first run; the requested-action ablation dropped (the requested-versus-executed difference is mostly a recorder artefact); pending: the distance study and the GPU 2 plan after SD 3.5 stops.
- **2026-09-24 18:40 EDT: U-Net final read at 200k; decisions: PixArt-alpha is the third row on GPU 1, the requested-action ablation is dropped.** U-Net 200k (512 val windows, stock decoder): EMA 22.49 / 0.177 at h1 (+0.92 dB over 21.57 / 0.203) and 21.33 / 0.224 at h4 (+2.11), its best, flat since 185k; live 22.29 / 0.186 and 20.97 / 0.244. EMA rollouts above copy-seed at every horizon (21.28 / 19.35 / 18.24 / 18.28 / 17.81 at 4/32/64/128/256 tics; +0.29 at 256), live 20.75 / 18.61 / 17.84 / 18.16 / 15.87. Per rollout: live has 4 of 16 below the 3 dB line at 256 (two under 8 dB, none blank), EMA 2 of 16 (lowest 13.6 dB). Across the full reads 160k to 200k the live 256-tic point varies by 3.4 dB checkpoint to checkpoint and the EMA by 0.9 dB. Sweep at 200k, live h1, linear / trailing: 4 steps 22.35 / 0.272 vs 22.37 / 0.245, 8 steps 22.34 / 0.199 vs 22.32 / 0.193, 16 steps 22.20 / 0.171, 50 steps 22.04 / 0.162; LPIPS beats the reference from 8 steps on. Rohan's decisions: the requested-versus-executed control difference is 89 percent identical, almost all of the rest dropped weapon-select requests (a recorder defect, label noise in the condition) and only 0.15 percent true overrides, so the ablation would likely be null and is dropped; the finding stays as data hygiene plus the audit. Third row: PixArt-alpha (Chen et al., ICLR 2024, `PixArt-alpha/PixArt-XL-2-512x512`, run `041-pixart-nexttic`) on GPU 1 to 200k at the matched recipe, gated on `repo_launch2` with `EVAL_EVERY=5000 EVAL_DEVICE=cuda:3`, pending the launcher and gates generalisation in progress; the 6,000-episode U-Net row is the candidate for GPU 2 after SD 3.5 stops Sep 27 19:00; the distance study on GPU 3 awaits Rohan's yes.
- **2026-09-24 17:45 EDT: the U-Net stopped at 200k (val 0.1509); SD 3.5 55k is the best teacher-forced read of either row and its 50k live collapse did not recur.** U-Net: stopped 17:09 EDT after step 200150 by the waiter, snap_0200000.pt and 0200000.pt on disk, no skips; 190k EMA 22.47 / 0.178 (h1) and 21.31 / 0.225 (h4), 195k EMA 22.48 / 0.177; its 200k read with the sweep runs now. SD 3.5 at 55k: live 22.58 / 0.165 and EMA 22.64 / 0.159 at h1 (+1.07 dB, ahead of the U-Net's best +0.92 and its LPIPS 0.177), live 20.86 / 0.253 and EMA 20.99 / 0.239 at h4 (U-Net still leads there, +2.10 vs +1.77); rollouts live 20.45 / 18.89 / 18.47 / 18.09 / 17.21 and EMA 20.42 / 18.46 / 18.08 / 18.23 / 16.82, no rollout under 8 dB, none absorbed. So the blank-frame collapse was specific to the 50k live weights; the reading now is checkpoint-to-checkpoint variance of the live weights (SD 3.5 50k badly, U-Net 180k mildly) that no EMA checkpoint has shown, not a monotone trend. `wandb==0.30.0` installed in the doom env (torch 2.14, diffusers 0.31, numpy 2.0.2 unchanged, pip check clean); `$D/repo_launch2` cloned at main for the next launch (repo_launch stays pinned for SD 3.5); a worker is adding a `dit` backbone entry, a `RUN_NAME` override and `ACTION_HISTORY` as a certified knob to the launcher and gates. GPU 1 idle pending Rohan's choice between the requested-action ablation and the DiT row.
- **2026-09-24 16:20 EDT: decision (Rohan): the U-Net stops at 200k; 200k is the matched step count for all rows; SD 3.5 runs to 200k on Sep 27 19:00 EDT.** The U-Net is saturated: EMA 22.42 to 22.46 dB / 0.182 to 0.179 at h1 from 150k to 180k, val in a 0.153 to 0.155 band since 151k. A server-side waiter stops `train-unet-nexttic` once `snap_0200000.pt`, `0200000.pt` and step 200100 are on disk (about 17:10), recording the reason in resumes.log; the steward then runs the 200k full read with the sweep and per-rollout analysis and mirrors it; GPU 1 is then free for the third row (DiT-XL/2 or the requested-action ablation, `ACTION_HISTORY=0` in the launcher, Rohan's choice pending) and the 6,000-episode row goes to GPU 2 after SD 3.5 stops. Paper timeline (Rohan): draft to Changliu Saturday night Sep 26 with numbers marked provisional; final tables Sep 28 to 29; submission Sep 30 AoE.
- **2026-09-24 15:55 EDT: the live SD 3.5 weights at 50k have an absorbing blank-frame failure under autoregression that the EMA weights do not; the U-Net's live weights show a milder form at 180k.** Reproduced with seed 1 (per-rollout scoring, `$D/tmp/steward/stw5_perroll.py`, mean checked against drift.json): live raw PSNR 20.16 / 17.93 / 15.30 / 14.60 / 10.13 dB at 4/32/64/128/256 tics (seed 0: 20.74 / 17.24 / 14.71 / 13.38 / 8.98) against the EMA's 20.45 / 18.51 / 18.15 / 17.66 / 17.82. Of 16 live rollouts, 8 end at 2.3 to 3.9 dB at tic 256, a blank or saturated image, with onsets spread from tic 4 (one) through 36, 43, 95, 135, 225 and two past 128; once a rollout reaches about 3 dB it never recovers; the collapses spread over maps 2 to 5. Seven of 16 cross 3 dB below the EMA mean and never return. The EMA weights at the same step produce no collapse and their best LPIPS so far. U-Net at 180k: EMA rollout its best (+0.60 dB over copy-seed at 256, LPIPS level with it) while the live rollout fell to 14.75 dB at 256 (18.18 at 170k); its seed-1 rerun with the same per-rollout analysis is running. Teacher-forced reads are unaffected (SD 3.5 50k EMA 22.56 / 0.163 at h1, U-Net 180k EMA 22.46 / 0.179). Paper: report EMA rollouts, and state the live-versus-EMA stability result with the per-rollout counts; rollout arrays are now kept on every full read. Candidate research follow-up: whether a short rollout-consistency post-training removes the fixed point for the live weights.
- **2026-09-24 15:15 EDT: SD 3.5 50k: best teacher-forced read of either row, but the live-weight rollout diverged while the EMA rollout is its best; SD 3.5 is compute-bound.** SD 3.5 at 50k (512 val windows, 10 steps): live 22.51 / 0.170 and EMA 22.56 / 0.163 at h1 (+0.99 dB), live 20.77 / 0.265 and EMA 20.88 / 0.245 at h4. Rollouts 16x256 seed 0: EMA 20.45 / 18.51 / 18.15 / 17.66 / 17.82 dB at 4/32/64/128/256 tics (above copy-seed at 256, best LPIPS so far); live 20.74 / 17.24 / 14.71 / 13.38 / 8.98 with the decoded latents collapsing the same way, so the live latents themselves diverge from about 32 tics (at 45k the live rollout was 17.00 at 256). No NaN, no skips, val still falling (0.0950 at 52k), so no hold rule applies. Decision: rerun the 50k live rollout with seed 1 keeping the rollout arrays to see how many of the 16 diverge and from which tic, keep the arrays for the 55k read, and treat "the EMA stabilises autoregression where the live weights do not" as a candidate finding for the paper. Throughput check on GPU 2: 100 percent utilisation sampled over 12 s, 0.53 updates/s median, CPU 93 percent idle, no I/O wait, data disk 13 percent busy, so the run is compute-bound (gradient checkpointing costs about a third) and no disk or evaluation move would speed it up; the only faster options change the certified run. U-Net 100k backfill read completed the sweep series (50k, 100k, 150k): pattern stable, 4-step PSNR saturated by 100k, later gains at 16 and 50 steps; U-Net 160k to 175k reads flat at EMA 22.45 / 0.180 (h1) and 21.27 / 0.228 (h4), live rollout above copy-seed at every horizon at 170k.
- **2026-09-24 11:30 EDT: SD 3.5 40k read: at 40k it matches the U-Net's 150k one-tic PSNR with better LPIPS.** SD 3.5 at 40k (512 val windows, its own decoder, 10 steps): live 22.37 / 0.173 and EMA 22.33 / 0.173 at h1 (+0.80 dB over 21.57 / 0.203; the EMA has caught up with live), live 20.67 / 0.264 at h4 (+1.46); rollouts beat copy-seed through 128 tics and fall below at 256 (live 17.12 vs 17.52). Against the U-Net at 150k (EMA 22.42 / 0.182 at h1, 21.24 / 0.232 at h4): SD 3.5 leads on one-tic LPIPS at a quarter of the steps, the U-Net leads at four tics and at 256-tic rollouts. Probe sensitivity 1.98. U-Net at 160k, val flat at 0.154. Plan under discussion with Rohan (nothing started): stop the U-Net at 200k (about 17:00 today) as the matched mark, SD 3.5 runs to 200k at Sep 27 19:00 EDT, a third row on GPU 1 (DiT-XL/2 or the requested-action ablation), the 6,000-episode row on GPU 2 after Sep 27, and a generalization-versus-distributional-distance study (sliced Wasserstein between per-map latent clouds, Rohan's NVIDIA method) scored on GPU 3 between reads; draft to Changliu Saturday night with provisional numbers.
- **2026-09-24 10:30 EDT: U-Net 150k read and sweep; all missing snapshots mirrored.** U-Net at 150k (512 val windows, stock decoder, 10 steps): live 22.27 / 0.197 and EMA 22.42 / 0.182 at h1 (+0.70 and +0.86 dB over the 21.57 / 0.203 reference); live 20.96 / 0.257 and EMA 21.24 / 0.232 at h4 (+1.74 and +2.03 over 19.21 / 0.354). EMA rollouts beat copy-seed in PSNR through 128 tics (18.92 vs 17.80) and trail by 0.16 dB at 256 (17.36 vs 17.52), same shape in LPIPS. Probe sensitivity 2.80. Sweep at 150k, h1 live, linear / trailing: 4 steps 22.28 / 0.285 vs 22.31 / 0.258; 8 steps 22.30 / 0.210 vs 22.29 / 0.204; 16 steps 22.18 / 0.180; 50 steps 22.02 / 0.168; every cell 0.15 to 0.3 dB and 0.016 to 0.03 LPIPS better than at 50k; LPIPS beats the reference from 16 steps on (8 with trailing). Val flat at 0.1543 from 154k to 157k. SD 3.5 at 43.5k, val 0.0972. Hub: U-Net snapshots 10k to 150k and SD 3.5 10k to 40k all verified.
- **2026-09-24 09:45 EDT: overnight: both rows healthy; the 4-channel 2000:6000 set is complete and on the Hub; a steward gap of 7.5 hours left reads to backfill.** U-Net at 154.9k (1.80 updates/s, val 0.1544 at 154k), SD 3.5 at 42.7k (0.535 updates/s, val 0.0973 at 42k); no skips, excursions, relaunches or tracebacks. The steward of the night was killed by the harness watchdog at about 01:50 (a silent ten-minute wait); its replacement did not come up until 09:33, so no reads or mirrors ran between the SD 3.5 25k read (01:18) and then, and the U-Net 105k to 135k recovery checkpoints were pruned; the snapshots 100k to 150k and SD 3.5 30k and 40k exist. Decision: mirror the eight missing snapshots first (no GPU), full reads only at U-Net 100k and 150k (both with the sampler sweep) and at every SD 3.5 5k, light reads for 110k to 145k, new checkpoints before the backfill; stewards now wait in loops that print every four minutes so the watchdog never fires again. Last reads before the gap: U-Net 90k EMA 22.30 / 0.191 (h1) and 21.14 / 0.242 (h4), 95k light; SD 3.5 25k. Superman: the 4-channel 2000:6000 phase finished 03:00 EDT (4,000 episodes, row-count audit 0 mismatches) and every one of them is uploaded to `sd15/arenas_2000_6000` and pruned locally; the SD 3.5 2000:6000 phase started 03:01 on six cards, 2,540 episodes queued at 09:40 (about 12 hours), 1,300 already uploaded to `sd35/arenas_2000_6000`. Disk: /sata2 680 GB free, Superman 149 GB.
- **2026-09-23 21:30 EDT: evening reads; the U-Net's EMA clears the persistence reference on both metrics at both horizons and on rollouts to 256 tics.** U-Net EMA at h1 (512 val windows, stock decoder, 10 steps): 22.08 / 0.205 at 50k, 22.12 / 0.202 at 55k, 22.17 / 0.200 at 60k, 22.20 / 0.198 at 65k, 22.22 / 0.197 at 70k, 22.25 / 0.195 at 75k (reference 21.57 / 0.203); h4 EMA 20.99 / 0.251 at 70k (reference 19.21 / 0.354). At 70k the EMA rollout is at or above copy-seed PSNR at every horizon to 256 tics (17.71 vs 17.52 at 256) and beats it on LPIPS out to 128 tics; live rollouts still swing between reads. Val 0.1648 at 79k, falling slowly after a flat stretch at 64k to 66k. SD 3.5: live 21.91 / 0.211 at h1 and 20.32 / 0.317 at h4 at 15k (past the U-Net's 20k numbers), EMA still warming (20.4 dB at 15k), val 0.1048 at 20k against a 0.118 band, 0.50 updates/s; its 20k read and Hub mirror run now. Hub: U-Net snapshots 10k to 70k and SD 3.5 10k verified. /sata2 766 GB free. Steward cadence holds with the light reads at the odd 5k.
- **2026-09-23 19:45 EDT: native W&B logging and periodic evaluation merged to main (`c1f8fc8`, suite 1,381); Astra's credits exhausted until Sep 26.** Four rounds (branches `rohan/native-wandb-logging` and `-r2`, 21 files, 3,067 lines with tests): `wandb_log.RunLogger` on by default in `train_wm.py` (`--no-wandb` only for the gates' fit, smoke and resume), every W&B call on a daemon thread behind a bounded drop-oldest queue, bounded close that detaches the SDK teardown on any abandoned run including an init that completes after close gave up; evaluators log to `<run>-eval` in the run's group under a per-run flock writer lock with a `<run>-eval-<pid>` fallback; `periodic_eval.py` gives the trainer `--eval-every N` (a multiple of the checkpoint cadence) launching one detached wrapper per multiple on a hard link of the checkpoint (removed by an EXIT trap or on a failed spawn), on `--eval-device`, with per-read status.json, TERM forwarded to the evaluator's whole process group with a bound, `--num-workers 0` on every periodic `eval_tf` (new flag), and `eval_finished` reported on the trainer's next non-blocking look. Astra confirmed rounds one to three by reproduction and found round four's two items; its Codex account hit its usage limit before the round-four verdict (resets Sep 26 13:28 EDT), so the round-four diffs were reviewed by main against the two reproductions (both closed by construction) and the branch merged on the suite. Before the NEXT launch: `pip install wandb==0.30.0` into `~/miniconda3/envs/doom` and `~/wanenc` on Spiderman once nothing runs in them, re-run the gates (the certified command gains the eval flags), and confirm on a rendered run the exit-hook detach against the real SDK, `set -m` process groups under bash 5, the `-eval` overlay in the group, and a resume continuing the same W&B run. The two live rows keep their pinned checkout (`35258f3`) and sidecars. U-Net 55k light read: EMA 22.12 / 0.202 at h1 (+0.56 dB), light reads take five minutes.
- **2026-09-23 19:00 EDT: U-Net 45k and 50k reads; the U-Net beats the persistence reference on LPIPS as well as PSNR at 16 and 50 sampling steps; SD 3.5 10k leads the U-Net on LPIPS at the same step.** U-Net (512 val windows, stock decoder): 45k live 21.97 / 0.218 and EMA 22.03 / 0.208 at h1, 20.68 / 0.286 and 20.72 / 0.269 at h4; 50k live 21.97 / 0.215, EMA 22.08 / 0.205 (h1), 20.62 / 0.286 and 20.81 / 0.263 (h4), that is EMA +0.51 and +1.59 dB over the one- and four-tic references. Sampler sweep at 50k, h1 live, linear spacing: 4 steps 22.03 / 0.317, 8 steps 22.01 / 0.231, 16 steps 21.86 / 0.196, 50 steps 21.65 / 0.184 (reference 21.57 / 0.203); trailing spacing identical from 8 steps on and better only at 4 (0.285); at 10k the 16- and 50-step LPIPS were 0.239 and 0.221, so the perceptual gain from 10k to 50k is about 0.04 at every step count. Rollouts 16x256 at 50k: live at or above copy-seed in PSNR to 128 tics (17.83 vs 17.80) and level at 256 (17.35 vs 17.52); the EMA rollout's LPIPS beats copy-seed at 32 and 64 tics (0.413 vs 0.487, 0.484 vs 0.505), a first. Validation loss is plateauing: 0.1707 at 50k, 0.1672 at 64k to 66k (band 0.21 at 20k). SD 3.5: 5k live 21.30 / 0.259 (h1), 19.73 / 0.381 (h4); 10k live 21.60 / 0.220 and 19.88 / 0.335 against the U-Net's 21.49 / 0.264 and 20.22 / 0.347 at 10k; its EMA is warming up (15.6 dB at 10k) as the U-Net's did; throughput rose to 0.50 updates/s; val 0.1074 at 15k (band 0.118 at 20k); its 10k snapshot and config are on the Hub. Steward cadence: U-Net full reads at 10k snapshots, light one-tic reads at the odd 5k checkpoints (a full read takes about one checkpoint interval), SD 3.5 full every 5k, sweeps at 50k and 100k. Native W&B change: round three (bounded finish always attempted, link removed on failed spawn, wrapper forwards TERM to its evaluator) merged locally (suite 1,374); Astra found two more (a hook registered after abandonment can still hang exit; eval_tf hardcodes two DataLoader workers) now in round four. Rebuilt in the current account after the restart: advisor deck https://claude.ai/artifact/3pfCCpKUCniVR8bVXnweSZ (self-contained page), dossier https://claude.ai/artifact/MgNo2ABT3iL8TsZNf6a2DP.
- **2026-09-23 16:30 EDT: session restart; steward relaunched with GPU 3 reads; U-Net 30k and 40k reads; native W&B change in round three.** The main chat restarted about 14:00 and every subagent died with it (the run steward at 13:33, before its GPU 3 reads began; the W&B worker after committing round two). Both trainings, both W&B sidecars, the uploader and Superman's phase 2 were unaffected. A new steward (`run-steward-2`) caught up: U-Net 30k (live h1 21.83 / 0.226, h4 20.52 / 0.301; EMA caught up with live) and 40k (live 21.88 / 0.218 and 20.44 / 0.295; EMA now ahead at 21.95 / 0.212 and 20.64 / 0.274, that is 0.38 and 1.43 dB over the one- and four-tic persistence references; probe sensitivity 3.10; rollouts at 256 tics swing 17.8 / 15.4 / 16.4 dB across 20k / 30k / 40k, the noise of 16 rollouts); U-Net val 0.1713 at 48k, flattening; SD 3.5 val 0.1115 at 10k (band 0.128), its 5k and 10k reads next on GPU 3. Native W&B: round two finished by a second worker (branch `rohan/native-wandb-logging-r2`, suite 1,359: EXIT-trap cleanup of the read's hard link and partial copies, stale status.json cleared on re-read); Astra's earlier thread was lost with the restart, a new one (`01a0cfef-a663-7700-a545-7377d51911ef`) re-reviewed and returned NO-GO on three items (log failure skipping the bounded finish and leaving the SDK exit hook; a failed spawn leaking the checkpoint link; SIGTERM to the wrapper orphaning its evaluator child), now in round three; held off main until cleared. Superman: one ssh refused with "permission denied" at 15:00 while Rohan's own ssh succeeded; a single retry worked (a dropped multiplexed connection); six cards on the 4-channel 2000:6000 phase, 2,508 episodes queued at 15:10, 2,800 already uploaded to `sd15/arenas_2000_6000` and pruned locally.
- **2026-09-23 13:15 EDT: dossier brought up to date and republished; advisor deck built; open-weights contribution noted; native W&B change held for round two.** `docs/RESEARCHER_DOSSIER.md` gained sections 3.12 (the launches and the first curves) and 4.10 (the decisions of the launch night), sourced from the day's server printouts; `docs/researcher_dossier.html` was rebuilt to match (the Sep 22 text fixes it had missed, the two new sections, a new Figure 13 of the U-Net's live-h1 reads, renumbered tables and figures; rendered and checked in both themes) and is published as a new artifact, https://claude.ai/artifact/WakX91sbRWNj9XtBH5hPcH, because the earlier artifact link no longer exists. Advisor deck for the meeting with Changliu: https://claude.ai/artifact/JveEnVhZjGQ7XezZcShjsC (first run, two data lessons, the fix, the dataset, the runs with their freeze estimates, contributions, a rollout appendix); at Rohan's request the deck names no persistence floor anywhere and the first-run chart shows PSNR and LPIPS on seen and unseen maps with GameNGen's before/after-decoder-tune lines (29.43/0.249 and 22.36/0.295, its own footage). Freeze estimates on the deck: U-Net reaches its 400k end about 02:00 EDT Sep 26 at 1.65 updates/s (12.8 M examples); SD 3.5 about 105k steps at 09:00 EDT Sep 26 at 0.41 updates/s (3.4 M). Candidate contribution (Rohan's question): "the strongest publicly available Doom world-model weights on the first public multi-map benchmark": public Doom weights today are only the two GameNGen reproductions (Stiegler's 20k/60k SD 1.4 checkpoints and VAE; Taketani's decoder and diffusion weights), both single-scenario; GameNGen, MultiGen and PlayGen release no Doom weights, DIAMOND's are Atari and CS:GO, Oasis is Minecraft. Condition: score both reproductions' checkpoints on our test and unseen sets with a documented adapter (context, padding, action vocabulary differ), planned as a row on GPU 3 after the freeze unless Rohan wants it earlier. Native W&B logging (`rohan/native-wandb-logging`, suite 1,337 on the merge): Astra NO-GO on four defects (blocking W&B calls in the trainer process, a checkpoint pruned under a detached read, two writers on `<run>-eval`, a failing read exiting zero); fixes in flight, held off main until cleared; the two live runs keep their pinned checkout and sidecars.
- **2026-09-23 11:30 EDT (Rohan back): steward evaluations move to GPU 3; native W&B logging on by default is being built; /home filled up.** Rohan: use idle GPU 3 intermittently for the steward's reads so neither training card loses throughput (a full read had cost about 2,500 updates on the shared card); the full read set now runs at every 5k checkpoint and snapshot, the sampler sweep at 10k and 50k; GPU 3 is checked idle before each read and never held between reads. U-Net 10k read: rollouts beat copy-seed through 128 tics (20.1 vs 18.9 dB at 4 tics, 17.9 vs 17.8 at 128) and tie at 256, LPIPS still worse than copy-seed past 32 tics; the paired 4/8/16/50-step sweep is a clean perception-distortion trade (4 steps 21.6 dB / LPIPS 0.35 to 0.38, 50 steps 21.1 / 0.22; spacing only matters at 4 steps), so the paper protocol must declare one step count and the 10-step readbacks flatter PSNR against 50 steps. U-Net at 20k: val loss 0.184 (band 0.21), step 22.3k at 11:10; SD 3.5 at 3.6k, 0.43 updates/s. Rohan's rule extended: W&B logging is ON BY DEFAULT in the trainer (`--no-wandb` only for the gates' fit, smoke and resume); evaluators log to a grouped `<run>-eval` run (W&B forbids two writers on one id); the sidecar `tools/wandb_tail.py` stays only for runs pinned before this lands (worker `wandb-native` in flight; `wandb` must be installed in the doom and wanenc envs before the next launch, deferred while runs are live). Spiderman /home (3.4 TB, shared) hit 0 bytes free at about 10:50: the runs write only to /sata2 and were unaffected; the steward's 20k upload failed once in the xet cache and succeeded with `HF_XET_CACHE` on /sata2; pruned our regenerable caches (18 GB); the uploader restarted from `repo_tools` with its caches on /sata2 and, after `e708b92`, reads the 2000:6000 sets from `ext_2000_6000` and uploads them to `sd15/arenas_2000_6000` and `sd35/arenas_2000_6000` (own Hub folders, so Superman's shard logs never overwrite the 0:2000 sets'); the remaining /home usage is Rohan's other projects (`~/data` 498 GB, `~/perseve` 258 GB) and other users.
- **2026-09-23 09:10 EDT: SD 3.5 next-tic row LAUNCHED behind its gates (`042-sd35-nexttic`, Spiderman GPU 2, commit `35258f3`); both rows stream to Weights & Biases.** Superman finished the SD 3.5 0:2000 corpus at 08:24 (row-count audits: train 2,000 episodes 10,070,779 rows, val 503,864, test 503,620, unseen 295,984, all 0 mismatches). `run_gates.sh sd35 sd35` 08:25 to 09:03 EDT, all passed: val audit 0 mismatches, training audit 0 in 1,000,000 rows, inventory ok, latent alignment on the seven Superman shards cross-host (train shards 10 to 16: MAE 6.8e-4 to 9.5e-4, p99 0.0068 to 0.0117, 69 to 77 percent bit-identical, margins 4.96 to 7.27 dB; val shards 10 to 15: margins 4.85 to 6.85 dB), fit 0.377 updates/s at 36.8 GB allocated (38.2 reserved) with gradient checkpointing, 300-step smoke (35 GB recovery checkpoint, 8.9 GB snapshot), probes, resume, four readbacks. Launched 13:04:38 UTC with the printed `GATES_LAUNCH sd35` line (`PY_SD35=~/wanenc/bin/python MB=32 WORKERS=12 STEPS=400000`), certified; 2,000 training and 100 validation episodes; GPU 2 at 39.5 GB. U-Net at the same time: step 11.8k, val loss 0.192 at 11k (band 0.225 at 10k); 10k read live h1 21.49 dB / LPIPS 0.264 against persistence 21.57 / 0.203 (0.07 dB under the floor, from 0.42 under at 5k), live h4 20.22 against 19.21 (1.0 dB over); rollouts at 5k below copy-seed beyond 64 tics (16.2 vs 17.5 dB at 256), early. Steward reads cost about 2,500 updates each on the shared card, so the cadence is the full set at 10k snapshots and a light live-h1 read at the 5k checkpoints. New rule (Rohan): every run streams live to W&B; `tools/wandb_tail.py` sidecars (tmux `wb-unet`, `wb-sd35`, env `~/wbtail`, checkout `$D/repo_tools`) replay `log.jsonl` and every steward JSON to project `doomdit-nexttic` (entity rohannaga04-rapideye). Disk: `results_smoke` and `results_preflight` (about 130 GB, mine) deleted; the rest of the freeable list is Rohan's (`hf_release_arnold`, `_aborted_safe_to_delete`, `latents_arnold_sd35`, `raw_arnold`). Open from the review, not implemented: the directional check and the rollout motion ratio.
- **2026-09-23 06:30 EDT: U-Net next-tic row LAUNCHED behind the gates (`040-unet-nexttic`, Spiderman GPU 1, commit `35258f3`).** `gates.sh` (sd15, unet) 06:12 to 06:28 EDT, all passed (`$D/GATES.txt`, results `$D/logs/gates_results_20260923T101211-*.jsonl`, certificate `$D/GATES_CERT.json`): val sidecar audit 0 mismatches in 503,864 rows; training audit 2,000 episodes, 0 mismatches in 1,000,000 rows; inventory 2,000 ids, rows and tics equal to the recordings; val inventory and split file ok; latent alignment train shards 00/01/03/05/07/08 all bit-identical re-encode (MAE 0) with margins 3.21 to 4.52 dB, val 2.91 dB; emitted windows ok; yaw gate aligned; fit 1.14 updates/s at 23.3 GB (23.9 reserved) with accumulation 1; 300-step smoke wrote a 13 GB recovery checkpoint and a 3.3 GB snapshot; probes ok; 10-update resume restored optimizer, scheduler, EMA and RNG; readbacks live and EMA at horizons 1 and 4 ok. Launch: the printed `GATES_LAUNCH unet` line (`cd $D/repo_launch && DOOM_ROOT=$D RUN_REPO=$D/repo_launch PY_UNET=~/miniconda3/envs/doom/bin/python MB=32 WORKERS=12 STEPS=400000 TRAIN_IDS=0:2000 VAL_IDS=6000:6100 bash scripts/spiderman/launch_nexttic.sh 1 unet`), recorded in `$D/logs/resumes.log` at 10:28:41 UTC as certified; tmux `train-unet-nexttic`; start event with 2,000 training and 100 validation episodes; GPU 1 at 24.8 GB. Recipe as planned: batch 32, no accumulation, lr 5e-5 after 2k warmup, EMA 0.9999, context 32 tics with 32 executed-control tokens, v-prediction, noise augmentation 0.7 over 10 buckets, public SD 1.4 warm start, validation every 1k on 1,024 windows, recovery every 5k, snapshots every 10k. Run steward `run-steward-unet` (opus-monitor) follows `.claude/analyses/run-steward-brief-2026-09-23.md`. SD 3.5 next: corpus about 08:05 EDT, then `run_gates.sh sd35 sd35` on GPU 2 and the printed launch.
- **2026-09-23 06:30 EDT: sd15 gates running for the U-Net; SD 3.5 validation alignment passes cross-host; Superman's 2000:6000 encode redirected to a separate tree.** The 4-channel corpus completed at 05:22 (tail shards 07 and 08 merged, no duplicates), normalisation rewrote 250 of 2,000 sidecars (the old-encoder episodes with unexecuted switch columns), the 100-episode raw audit found 0 mismatches over 504,921 rows. `bash $D/tmp/run_gates.sh sd15 unet` started 06:12 EDT from `repo_launch` at `35258f3` (log `$D/logs/gates_unet_20260923T1012.log`): val audit 0 mismatches, training audit, inventory, latent alignment of train and val passed, emitted windows running. The SD 3.5 validation alignment rerun on the free GPU 2 passes: six shards, MAE 8e-4 to 9e-4, p99 0.007 to 0.012, 69 to 73 percent bit-identical (Superman A4000 against Spiderman A6000), margins 6.6 to 6.9 dB. Found and fixed before it bit: `gate_certificate.encoder_records` pins every `encode_meta_*.json` in the training directory, and Superman's phase 2 would have written the 2000:6000 shards into the same `arenas/` directories, so every later resume would have refused; the first orchestrator was stopped (supervisor only, its seven workers keep running) and `superman_encode_orch2.sh` (a patched copy, log `enc-orch2.log`) waits for those workers with the 08:38/08:45 drain, re-lists and audits phase 1, then encodes 2000:6000 under `$D/ext_2000_6000/latents_arnold_dense_pertic{,_sd35}/arenas`; the 0:2000 directories stay frozen for the paper rows and are merged later, deliberately. SD 3.5 train queue 472 at 06:15, done about 08:05. The run steward's brief is `.claude/analyses/run-steward-brief-2026-09-23.md`.
- **2026-09-23 04:40 EDT: round four merged to main (`35258f3`, suite 1,251 passed); Astra: GO for launching behind the gates and GO for paper numbers scored from `repo_launch`.** Round four (`paper/fixtures/test_nexttic_defects4.py`): a failed revocation is retried under the gates' interpreters and never deletes another backbone's entry (gate 0 stops with the file untouched if none can revoke); every GPU step of `gates.sh` runs under `CUDA_VISIBLE_DEVICES=<card>` addressing cuda:0 (the readbacks had defaulted to GPU 0, another user's card); `after_nexttic.sh` honours `RUN_REPO` for its cd and all helper paths, takes `PY_UNET`/`PY_SD35`, and records checkout, commit and interpreter (`1132afe` adds the interpreter to `decoder_provenance.txt`, Astra's last non-blocking note). Astra accepted the 2 dB margin together with MAE <= 5e-3, p99 <= 2e-2 and complete shard coverage, noting these are sampled checks, not proof. Its verdict is CPU-level: the real GPU gates still have to pass. Spiderman `repo_launch` is at `35258f3`; the gate invocation is in the runbook's gate section.
- **2026-09-23 04:05 EDT: round three merged to main (`1747d46`, suite 1,240); Astra's fourth pass found three narrower defects (in fix); the validation-side gates pre-run on both spaces; the shifted-control margin floor set to 2 dB from the real measurement.** Round three (worker, test-first, `paper/fixtures/test_nexttic_defects3.py`): evaluator `flock` in `after_nexttic.sh`; one latent contract per space across train and val and equal to the backbone's (`check_latent_alignment.py --space/--contract-peer`, values read from `doomdit_utils`, `backbones`, `verify_sd35`); one production-argument set feeding fit, smoke, resume and certificate; `PY_UNET`/`PY_SD35`; `RUN_REPO`; a `GATES_LAUNCH` command printed at GATES_GO and verified under `env -i`; per-backbone revocation with per-run results files. Astra confirmed its earlier attacks fail correctly and found: a failed revocation still deleted the whole certificate (`gates.sh` revoke_mine), gate 5 readbacks ran on GPU 0 (no device selection; GPU 0 is another user's), and `after_nexttic.sh` ignores `RUN_REPO` (cd `$D/repo`, helper paths); all three confirmed in the code and handed back (round four). Pre-run of the val-side gates from `repo_launch` at 1747d46 with the Spiderman knobs: sd15 val audit 0 mismatches, val inventory ok, yaw gate aligned (exit 0), latent alignment re-encode 100% bit-identical (MAE 0) but margin 2.91 dB under the 3 dB floor; sd35 val audit 0 mismatches, inventory ok, yaw gate aligned, latent alignment OOM because GPU 2 was shared with a tail encoder (22.7 GB for the SD 3.5 decode; rerun on the free card at gate time). Decision (Fable, to be reviewed by Astra): the margin is bounded by per-tic motion (true alignment 25.46 dB, +-1 controls 22.55/22.53, +-4 controls 20.6/20.5; copy floor at one tic 21.6 dB), so the 3 dB floor sat above what slow Doom footage can give; `MIN_SHIFT_MARGIN_DB` is 2.0 (`1d9442d`), rerun passes. Tail encoders at 40 of 168/169 at 04:00, about 06:00 for TAIL_MERGED.
- **2026-09-23 03:00 EDT: the pre-smoke found and fixed a launch blocker; every post-smoke gate step passes on the new code; shard 1's remainder split over two cards.** The 300-step U-Net pre-smoke from `repo_launch` on the complete even-id half (ids 0:1000, 1,000 episodes) died at dataset construction with `OSError: Too many open files`: `TicWindowDataset` keeps one memory map per episode and Spiderman's soft limit is 1,024 (hard 1,048,576), so 2,000 training episodes could never have loaded and the real gate would have failed at 05:30. Fix `1313e22`: `doom_data.ensure_open_file_budget` raises the soft limit to the episode count plus headroom in all three dataset constructors before the first map; forked loader workers inherit it. Rerun: 300 steps at 1.55 updates/s, peak 23.3 GB, val loss 0.62 / 0.39 / 0.31 at 100 / 200 / 300, recovery checkpoint, snapshot and best.pt written; `smoke_probe.py` SMOKE_PROBE_OK; `eval_tf.py --tic-stride 1` readback on 64 val windows: live h1 18.48 dB PSNR, LPIPS 0.632 (persistence 21.92), EMA h4 10.14 dB (the EMA is still the pretrained start at 300 steps); the 10-update resume restored optimizer, scheduler, EMA and RNG and ended at 310. Split files: `latents_arnold_dense_pertic_eval/split_arenas_678.json` still named ids 0:60 while the directory held 60:120 (moved aside as `.ids0_60.old.json`, regenerated with `--refuse-worker-first`), and the SD 3.5 eval directory had none (val, test, arenas_678 published now); the canonical control table is copied to `raw_arnold_dense/` where the gates read it. Throughput: enc-train1 had 337 odd ids left at 70 episodes/h (done 07:40), so it was stopped (`tmux kill-session`, no orphan) and the remainder runs as `enc-tailA` (ids 1327..1661 on GPU 2) and `enc-tailB` (1663..1999 on GPU 1) from the same `$D/repo` code into staging directories; `enc-tail-merge` moves them into `arenas/` as shards 07 and 08 and writes `TAIL_MERGED` (expected about 05:20); the steward normalises and audits after the marker. Superman SD 3.5 train queue 1,324 left, about 07:50.
- **2026-09-23 02:55 EDT: round two merged to main (`f525782`, suite 1,215 passed); Astra's third pass: NO-GO again on three narrower defects, fixes in flight; first fit check on the new code.** Astra re-ran its six attacks against `d2d6260..ebe1bec` and all now fail correctly (foreign or changed certificate, missing shard log, NaN sample, RESCORE after a rollout failure, swapped decoder, changed split contents); 161 tests pass on its side. Remaining: `after_nexttic.sh:275` mistakes a live evaluator for an interrupted one (two concurrent invocations both scored test TF; fix: an exclusive process lock separate from the seal); `gates.sh:349` compares latent contracts within a directory only, never train against val or against the backbone's expected contract (synthetic train scale 1 versus val scale 2 passed); `gates.sh:243,409` start the smoke without the resolved micro-batch, workers and operator EXTRA, so the smoke and the certificate can come from different arguments (reproduced: smoke lr 5e-5 with 12 workers versus certified lr 0.1 with 4). Astra's operational note: the certificate can match a direct Spiderman launch only if DOOM_ROOT, interpreter, WORKERS, LAUNCH_STEPS=STEPS and MB_UNET/MB_SD35=MB are set identically on both sides. A worker is closing the three defects test-first plus per-backbone interpreters (`PY_UNET`/`PY_SD35`; the `doom` env has diffusers 0.31, too old for SD 3.5, `wanenc` has 0.40), a `RUN_REPO` knob (an encoder still runs from `$D/repo`, so the gates and launch come from a second checkout `$D/repo_launch`, cloned at `f525782`) and a printed operator command at GATES_GO. Overnight state at 02:30: Spiderman shard 1 at 640/1000 (93 f/s, exit about 05:30), `arenas_678` 60:120 done, GPU 1 free; Superman seven cards at 50 f/s each, SD 3.5 evals done, train queue 1,392 of 1,836 left at 266 episodes/h (done about 07:30, before the 08:45 drain); uploader idle-waiting; Spiderman /sata2 1.29 TB free, Superman 148 GB free. U-Net fit check from `repo_launch` on GPU 1 (synthetic windows, micro-batch 32, no checkpointing): 1.18 updates/s, peak 23.3 GB allocated, 23.9 GB reserved of 48 GB, so the recipe's fixed global batch of 32 leaves the card half empty and the fill-the-card lever is spent (a larger micro-batch would change the recipe). A 300-step pre-smoke on the complete shard 0 (ids 0:1000) runs next on GPU 1 to catch code-path failures before the real gates.
- **2026-09-23 00:50 EDT: Astra's review of the fixes: NO-GO on both the gates and the scoring script until eight defects are closed (worker resumed).** Confirmed by Astra with reproductions: the gate certificate accepted a changed recipe and an SD 3.5-only certificate for a U-Net launch (`launch_nexttic.sh:180`); the latent-alignment gate let missing shard logs hide shifted latents, passed mixed scaling contracts and sampled NaNs (`check_latent_alignment.py:65,191,213`) and its bit-identity floor rejects harmless rounding (one fp16 ULP gave MAE 2.4e-4, 0% identical; `:215`), so the criterion becomes MAE plus shifted-control separation with a cross-host-calibrated tolerance (Superman A4000/cuDNN 9.10 versus Spiderman A6000/cuDNN 9.24, measured tonight on a 4-channel episode: 67% bit-identical, RMS 2.5e-3, p99 0.011, decode PSNR within 0.001 dB); the cluster gates implemented only part of review 7.1 (`gates.sh:299`); `after_nexttic.sh` could recompute a sealed test stage under RESCORE after a rollout failure (`:427`), pinned only the checkpoint at selection so a newly installed decoder would replace the selected one (`:422`), and keyed caches on the split pathname (`:206`); plus `decoder_mse.sh` default ids, a stale 0:60 in the design note and GameNGen wording in a trainer help string. Verified correct: H1 (map balance, history, worker-first and mixed-directory rejection; the old 0:60 latents must be moved out of both `arenas_678` eval directories before the splits regenerate), the selection rule and pairing, mixed-stage refusal, checkpoint-keyed caches, commit refusal logging, H4 registry, H5/L1 text, bit-identical default sampling. Encoder provenance decision: the 4-channel 2000:6000 set (Superman) mixes environments with 0:2000 (Spiderman) at bf16-rounding level; accepted and documented in `PROVENANCE.md` per latents directory and the latents card; the paper rows on 0:2000 remain single-environment in both spaces. Space: an uploader streams the 2000:6000 latents to `RohanNaga/doom-dense-arnold-latents` and frees them locally after size verification. Progress 00:25 EDT: 4-channel shard 0 at 940 of 1,000, shard 1 at 490; unseen 60:120 done (60); SD 3.5 train 164 and eval 135 of 260 landed from Superman. Tagged: goal 1.
- **2026-09-23 00:20 EDT: review fixes merged (suite 1,147 passed); Superman SD 3.5 encode on seven cards; Astra reviewing the fixes.** Fixes (branch `rohan/review-fixes-0922`, eight commits): H1 unseen scoring subset is arenas_678 ids 60:120 everywhere with a history note in `dense_split.json` and a test rejecting worker-first ids; H2 `after_nexttic.sh` has a val-only `--select` step (rule, preregistered: highest raw PSNR among candidates whose LPIPS is within 0.01 of the best, 512 val windows, live and EMA paired) writing `selection.json`, test and unseen are sealed corpora scored once on the selected checkpoint (`test_scored_at`, `FORCE_TEST=1` to override), caches keyed by checkpoint identity, corpus episode sets validated against the split, mixed val-and-sealed `CORPORA` refused; H3 gates audit the training sidecars against raw and check latent-to-sidecar alignment, the launcher no longer pulls after the gates and refuses a checkout that differs from the gate-certified commit (`ALLOW_UNGATED=1` recorded); new `check_latent_alignment.py` (per-shard re-encode comparison with shifted negative controls); H4 decoder tunes sample training ids only and write `provenance.json`, a decoder registry marks the installed decoders provenance-unknown/all-maps, and an unseen-map claim with such a decoder is refused unless `UNSEEN_CLAIM=dynamics-only`; H5 paper and dossier text corrected; M1 `--timestep-spacing {linear,trailing,karras}` (default linear, bit-identical); L1 card and index corrections. Open decisions recorded for Rohan: the selection rule above; unseen claim is dynamics-only until a train-only decoder is tuned; the latent-alignment gate's bit-identity threshold when the gate host differs from the encoding host (SD 3.5 latents come from Superman A4000s, cuDNN 9.10; Spiderman has cuDNN 9.24), Astra asked for the replacement rule. Superman: seven SD 3.5 shards (GPUs 1 to 7) at 48 to 50 frames/s each, eval corpora first then train 0:2000, projected 06:15 to 07:55 EDT; Spiderman GPU 1 does the ep-6000 cross-host diff and the split publishing only (mixing rule: the 16-channel training corpus comes from one encoder environment unless the diff is bit-identical). Runbook for the morning: `.claude/analyses/launch-runbook-2026-09-23.md`. Tagged: goal 1.
- **2026-09-22 23:30 EDT: compute plan for the week (Rohan): lab servers only for now; Superman encodes overnight; the paper rows train on ids 0:2000; the rest of the training split is encoded for a later 6,000-episode row.** Decisions: stay on Spiderman and Superman (AWS kept as an option; the P-instance quota request is Rohan's, no commitment); Superman may use 7 of 8 GPUs until 09:00 EDT Sep 23 and 6 afterwards (GPUs 0 and 1 free), the 250 GB disk floor is waived (keep 60 GB); both next-tic rows launch on Spiderman GPUs 1 and 2 as soon as their gates pass, without waiting for Rohan; SD 3.5 per-tic latents (ids 0:2000 plus val, test, unseen 60:120) are encoded overnight by seven Superman A4000 shards streaming episodes from Spiderman (batch 64, about 50 frames/s per card, 12.5 to 13.8 GB peak; the allocator's OOM warnings are retries, not failures) plus Spiderman GPU 1 for the eval corpora, projected done 06:00 to 08:50; after that Superman encodes `arenas` ids 2000:6000 in both latent spaces on six cards (about 16 to 19 h per space, about 1.07 TB on Spiderman) so a separate 6,000-episode row can run later on a rented node or after the freeze; expanding the running rows' data mid-run was considered and rejected (a data schedule, not a fixed-size row; only worth it if data limitation shows, which it cannot below one pass). Hugging Face repos created: `RohanNaga/doom-dense-arnold-latents` (card written; 4-channel val and test latents uploading; training latents follow) and `RohanNaga/doomdit-nexttic` for weights, so nothing depends on Spiderman. Agents: `opus-worker` now runs at xhigh effort (usage no longer constrained); the review-fixes worker, the Superman encode worker and the night steward run through the night. Tagged: goal 1.
- **2026-09-22 22:45 EDT: independent review (Opus 5.5 reviewer paired with Astra, `docs/REVIEW_2026-09-22.md`): training GO, evaluation pipeline NO-GO until fixed; the frozen unseen subset was wrong.** Verdict: nothing found corrupts what the runs learn (row convention measured on real data: the control on row i predicts the turn between rows i and i+1 with accuracy 0.996 against 0.496 and 0.501 for the neighbours; deaths increment exactly at respawns; split balanced and disjoint; no repeated seeds; frames lossless 320x240 with HUD; conditioning offsets right in training, rollouts and the 4-tic horizon; suite 1,004 passed). Findings, high: H1 the declared unseen scoring subset arenas_678 ids 0:60 is 51 of 60 worker-first episodes (Superman ran 16 workers and the recording restarted three times), the only regime in which Arnold's weapon switches execute, which is 1.6% of training (ids 0 to 31) and absent from val and test, so map transfer would have been confounded with a control shift; replacement declared before any score exists: ids 60:120 (20 per map, zero worker-first). H2 `after_nexttic.sh` scored val, test and unseen in one pass with no val-only selection, rolled out on test even with CORPORA=val, cached by file existence, and scored whatever subset was encoded. H3 the gates never audited the training sidecars against raw or the latent-to-sidecar alignment, and the launcher ran `git pull` after the gates. H4 the planned dense decoder tune sampled val and test episodes; the installed tuned decoders saw maps 6 to 8. H5 `paper/main.tex` calls MSE+LPIPS 'as in GameNGen' (GameNGen is MSE only) and says 900M training frames (v2: a 70M-example subset); the dossier's 'opposite LPIPS pattern' sentence and the 'no modelling gap at equal data' reading are unsupported. M1 the few-step DDIM numbers depend on linear timestep spacing (at 4 steps two of four calls sit at SNR 0.011). Disagreements with Astra recorded in section 8 (unseen share 33% vs measured 51/60; clustered SE about 0.19 dB at 2,048 windows, not 0.087). Fixes in progress (worker `review-fixes`): new unseen ids, val-only selection then a sealed single test scoring, checkpoint-keyed caches, corpus-id validation, training-corpus audit and latent alignment gates, no post-gate pull, train-only decoder provenance, text corrections, `--timestep-spacing`. Encode of arenas_678 ids 60:120 queued behind shard 0 on GPU 1. Compute plan under discussion: lab-only (free; U-Net one pass, SD 3.5 0.4 pass by the freeze) versus an AWS p4d 8xA100 node (about $22/h, eight rows, SD 3.5 0.8 pass) using AWS credits; the P-instance quota is the gate. Tagged: goal 1.
- **2026-09-22 21:30 EDT: the whole dataset is public and verified; encodes continuing; agents moved to Opus 5.5.** Hugging Face `RohanNaga/doom-dense-arnold`: all batches landed (dense data 15:09 EDT, original corpora, indices and the card 15:52 EDT); final verification VERIFY_OK: all six folders present with matching names and sizes, 20 of 20 randomly sampled files md5-identical after download. Encodes: val, test and arenas_678 evaluation latents complete (100/100/60) with their split files; train shard 0 (old encoder, wide sidecars, normalised afterwards) at 740 of 1,000 at 21:05 EDT; the `enc-evals` chain had failed at its test publish step (12:52 EDT) because the test sidecars written by the old encoder were wide, so train shard 1 never started until relaunched at 21:07 EDT with the new encoder on GPU 2 (about 12 h); sidecars of test (57), arenas (610 more) normalised and the test split republished; steward `steward-tue-night` (Opus 5.5) watches both shards, normalises the arenas sidecars once more when both finish, then runs the sidecar audit. Lesson: a `git pull` under a running encoder leaves the old process writing the old format; publish steps of the new code then refuse its output. Dossier delivered (`docs/RESEARCHER_DOSSIER.md`, `docs/researcher_dossier.html`, private artifact). Agents: every Opus agent (`opus-worker`, `opus-reader`, `opus-monitor`, `codex-worker`), the `overnight-monitor` skill and the memory notes now name `claude-opus-5-5` at effort high (Rohan, Sep 22). No training has started; the GPU provider decision is still open. Tagged: goal 1.
- **2026-09-22 13:00 EDT: full-corpus button report; dataset batch 1 verified; researcher dossier delivered.** `buttons_report.py` over all 8,000 dense arenas episodes (40,285,059 rows): raw max width 2,507; 11.0% of rows wider than the 19 executed buttons (4,427,202); executed weapon-switch rows 3,286, all inside the first episode of each of the 32 workers (ids 0 to 31); unexecuted switch requests 4,424,593; zero episodes with within-episode growth; max inferred prior starts 249 (8,000 episodes over 32 workers), exactly as the Arnold `add_buttons` mechanism predicts. So Arnold's requested weapon switches executed in 32 of 8,000 episodes (0.4%); the executed control is `buttons[:19]` everywhere, and the requests stay recoverable from the sidecar. Dataset: batch 1 verified against the local copy (every uploaded `arenas` and `arenas_678` file present with matching size; 12 of 12 randomly sampled files md5-identical after download); batch 2 in progress (ids up to 5,749 landed at 12:50 EDT), throughput 117 to 123 MB/s. Encodes: val and test complete (100 each), arenas_678 eval in progress, train shard 0 at 210 of 1,000. Dossier: `docs/RESEARCHER_DOSSIER.md` (about 22,000 words, written by Astra from the log, analyses, code and the papers; reviewed) and a paper-style rendering `docs/researcher_dossier.html` published as a private artifact (18 figures, 33 tables). Tagged: goal 1.
- **2026-09-22 11:30 EDT: executed-control normalisation merged, reviewed and applied; batch 1 of the dataset is on Hugging Face.** Code (`190c125` and follow-ups on main; suite 1,006 passed): `transitions.EXECUTED_BUTTONS = 19`, `normalize_buttons` (`s[:19]` right-padded, validated binary on the first 19), `switch_request_indices`, `ControlWidths` reporting raw max width, rows over 19 and unexecuted switch rows as separate statistics; sidecars now carry `buttons` as fixed `<U19` plus `buttons_raw_len` and `switch_requested_index` (the unexecuted requests stay recoverable); `encode_parquet.py --normalize-sidecars --in-dir <raw> --out-dir <latents> --episode-ids A:B` repairs existing sidecars atomically after checking row count, tics and the normalised control against the raw recording (never reads frames, never touches latents); `check_action_alignment.py --audit` compares normalised to normalised; loaders refuse a wide sidecar naming the repair; `buttons_report.py` reports widths and switches and checks constant inferred k within an episode. Astra reviewed twice (found three repair-path gaps, fixed in `f653817`; then GO). Applied on Spiderman to every encoded sidecar (arenas 414 episodes, val 100, test 39 at the time): all now `<U19`, about 620 KB each. Report over the first 40 raw episodes: raw max width 27, 3.9% of rows over 19, 3,286 executed switch rows, 7,818 unexecuted, zero episodes with within-episode growth, max inferred starts 1 (early episodes; the full-corpus report is running). Dataset card corrected (`release/DATASET_CARD.md`, restaged for the final upload batch). Dataset: batch 1 (train 0:2000, val, test, arenas_678 0:60; 620 GB) landed 10:01 EDT at 123 MB/s; batches 2 and 3 uploading; original corpora, indices and the card follow; verification script fixed to treat not-yet-uploaded folders as pending. Encodes: val complete (100), test and train shard 0 continuing at 85 frames/s. Tagged: goal 1.
- **2026-09-22 09:45 EDT: `buttons` column defect found and understood; encoder is at the card's limit; cluster bundle built.** DEFECT: the raw `buttons` string is Arnold's requested control list (9 entries over the first 9 of the 19 `available_buttons`) optionally extended by its weapon-preference code to the mapping index of `SELECT_WEAPONj`; Arnold's `add_buttons` (`src/doom/actions.py:197-199`, `game.py:485`) appends the ten weapon names to the shared button list on EVERY `Game.start()`, so after k starts in one recorder process the index is 9 + 10k + j (strings of 112 to 2,506 characters on about 5% of rows; ViZDoom deduplicates its own list, so the engine keeps 19 buttons). ViZDoom 1.2.4 `setAction` (`src/lib/ViZDoomGame.cpp:151-158`, checked by Astra) reads only the first 19 entries and zero-fills the rest, so the EXECUTED control is `s[:19]` right-padded, and a select bit beyond index 18 was never executed; confirmed on data: rows with the bit inside 19 show an ammo change within 4 tics 20% of the time against a 4% baseline, rows with the bit beyond show the baseline. Behaviour artefact: in every episode after a worker's first, Arnold's weapon switches did not execute. Consequences before the fix: sidecars held a `<U2506` column (40 MB per episode, about 76 GB for 2,000), the strict width check would have refused every episode at launch, control tokens would have been 2,506 wide. FIX in progress (worker `buttons-fix`, Astra to review): `EXECUTED_BUTTONS = 19`, normalise everywhere (`control_matrix`, checks, audit, sidecar written as fixed `<U19`, a sidecar-normalisation pass for already-encoded episodes without re-encoding), canonical table's `[:9]` untouched, dataset card corrected. ENCODER: profiler measured the SD VAE forward at 96 frames/s on an A6000 (99.7% GPU-busy, memory-bound GroupNorm/elementwise), every other stage negligible; producer-thread prefetch shipped (97% of ceiling, byte-identical); no further gain on this card; H100 expected about 4x by memory bandwidth, so about 7 card-hours per latent space for 2,000 episodes. CLUSTER: `scripts/cluster/` (setup, dataset fetch with md5, encode across GPUs, gates, launch, status, README; 906 tests) merged into the session branch; rental shape agreed with the provider card: wide-and-short encode (8xH100, 1 to 2 h) then 2xH100 for 48 h, about $650; two A6000s on an NVLink pair (0+1 or 2+3) could host a third next-tic row (PixArt) for free. Batch size stays 32 for every run; multi-GPU means 2x16, never a larger batch. Upload at 123 MB/s (1 GbE NIC, saturated). Tagged: goal 1.
- **2026-09-22 09:15 EDT: Spiderman back (rebooted 23:59 EDT Sep 21; 503 GB RAM, so the encoder scan was not the cause; root disk now 2.2 GB free); dataset going public on Hugging Face; split confirmed; H100 rental planned.** What survived the outage: SD 3.5 final evaluation with the tuned decoder (2,048 windows): live 21.21/0.263 seen (copy 19.29), 19.24/0.422 unseen (copy 18.28), 21.17/0.402 unseen2 (copy 20.79); EMA 21.51/0.242, 19.37/0.395, 21.48/0.381; rollouts PSNR@64 17.77, IDM 0.49, FVD16/32 202/458. SD 3.5 is the best row: it matches the U-Net's seen margin over copy-last, beats it on unseen maps, and has the best LPIPS everywhere; live is the like-for-like row, EMA beside it. DDIM step sweep (U-Net and PixArt, 512 windows): 1/2/4 steps give about +1.15 dB PSNR over 50 steps with LPIPS 0.45 to 0.58 against 0.28 (blur wins PSNR); every PSNR must name its step count and carry LPIPS; our 4-step numbers are the fair comparison with GameNGen's 4-step 29.4 dB. MSE-only decoder tune failed with CUDA OOM on the shared GPU 3 (rerun pending). Dense per-tic encode reached 277 episodes before the reboot (three shards SIGKILLed early, two ran to 130 each at 43 frames/s on a shared card; 83 frames/s alone on a free card, too slow for an SD VAE, profiler running). The dense and 17-map canonical tables are identical, so no mask rebuild. Encode relaunched with the audited launcher: train shard 0/2 on GPU 1, evaluation corpora then shard 1/2 on GPU 2. DATASET: public repo `RohanNaga/doom-dense-arnold` (private quota is 100 GB; public 8.7 TB): `arenas/` 8,000 episodes 1.90 TB, `arenas_678/` 3,000 episodes 0.78 TB, `arnold/` 850 episodes 0.20 TB, `arnold_eval/{seen,unseen,unseen2}` 210 episodes 50 GB, plus `dense_split.json`, md5 manifests, per-episode `index_*.parquet`, `canonical_controls.json`, the card `release/DATASET_CARD.md` (licence cc-by-4.0, Rohan to confirm). Uploaded as-is (one parquet per episode is already the right sharding; PNG frames do not compress further); priority order training 0:2000 + val + test + arenas_678 0:60 (620 GB), then the rest, then the original corpora. Measured uplink 123 MB/s, the same with one or three streams, so about 1 Gbit/s is the wall: first batch about 10:15 EDT, all dense about 14:45, everything about 15:30. SPLIT confirmed by Rohan: train 0:6000, val 6000:7000, test 7000:8000, unseen = arenas_678; runs may use a prefix of train (0:2000 was a compute decision; on H100s Rohan may train on all 6,000 = 30.2M tics; 0:4000 = 20.1M). PLAN: rent an 8xH100 node for about five days (provider comparison card and a cluster launch bundle `scripts/cluster/` in progress); encode both latent spaces there from the public dataset, run the gates, train both rows to at least one pass. Tagged: goal 1.
- **2026-09-21 14:30 EDT: full pre-launch audit of code and recipe (main session plus Astra, thread `01a0c45a-82aa-7cd2-9a93-294a9aa81467`, `.claude/analyses/astra-prelaunch-audit-2026-09-21.md`); recipe stands unchanged; Spiderman still down.** RECIPE: every decided item kept (next tic, 32 tics of context, executed-button history tokens, noise augmentation uniform to 0.7 in 10 buckets, batch 32, fused AdamW lr 5e-5 constant after 2k warmup, wd 0, clip 1.0, EMA 0.9999, public pretrained start, train ids 0:2000, snapshots every 10k). Astra withdrew its one proposed change (a 10% exactly-clean mass in bucket 0: it would re-bucket finished rows and departs from GameNGen's scheme). Recorded decisions that cannot change after launch: the beta schedule stays linear 1e-4..0.02 with v-prediction although it does NOT match the warm starts (SD 1.4: LDM scaled-linear 0.00085..0.012, terminal alpha-bar 4.7e-3 against our 4.0e-5, 273 of our timesteps below its terminal noise level; SD 3.5: rectified flow, a larger mismatch); near-zero terminal SNR is the right pairing for v-prediction with pure-noise DDIM, the schedule is hard-wired across eight call sites and not stored in checkpoints, and five finished rows trained under it. Paper must state the schedule, both mismatches, that no schedule ablation exists, and must not claim GameNGen schedule replication. No observation dropout means observation CFG (GameNGen 1.5) is unavailable for these models. Evaluation rules: live and EMA from the SAME checkpoint, selected on val never test; reserve evaluation time before the freeze (the launcher trains until stopped); the unseen-map claim needs a decoder tuned on training-map frames only (our installed decoders saw all maps) or must be qualified as unseen by the dynamics model. OUTAGE MECHANISM FOUND (not proven): `encode_parquet.py` scanned `action`+`buttons` of EVERY episode in the input directory before honouring `--canonical`, about 10 GB per process, about 61 GB across the six shards launched at 22:35 Sep 20; sufficient to exhaust a loaded host, though the root disk at 321 MB free is an independent candidate. FIXES merged (suite 808 passed, 1 skipped): canonical table resolved before any read, streamed per episode, one shared table from train ids 0:2000 written atomically and passed to every shard and corpus, `--canonical-only`, `--rebuild-sidecar-masks` (no re-encode; checks action/buttons/deaths against raw); local checkpoints written to an open file then `os.replace` instead of a 37 GB byte buffer, byte-identical output; `TicWindowDataset` requires tic/deaths/map_id/buttons/is_decision/action with equal lengths and binary constant-width controls (the server's existing sidecars satisfy this); resume identity covers optimizer, wd, warmup, clip, EMA, seed, spike guard and a content fingerprint of the corpus (header plus three 64 KB latent windows plus the sidecar, path-independent); sidecar audit against raw parquet and complete-split publishing; `after_nexttic.sh` scores live and EMA from one numeric-step checkpoint, `CKPT=`/`STEP=` modes, RESCORE fixed, stage errors fatal; exact raw-tic hits and keyed context noise in the evaluators; rollouts scored against RAW frames with streamed clips and all valid IDM windows; decoder MSE cropped to the 240 real rows (6.25% of the old loss was gray padding) with decoder provenance recorded. The hand-launched server encode used the 17-map table; only `is_decision`/`chain_id` depend on it and training ignores both: on return build the dense table, diff, rebuild masks only if they differ. LAUNCH PROTOCOL on return: health (load, memory, disk, what survived); pull main; shared table; encode inventory and sidecar audit; alignment gate on val 6000:6100; batch-32 fit check (memory, updates/s, workers); 300-step smoke with two recovery saves and an `eval_tf.py --tic-stride 1` readback; launch; 30-minute watch in one multiplexed connection. Planning forecasts (not thresholds): U-Net val loss about 0.30 / 0.24 / 0.225 / 0.21 at 1k / 5k / 10k / 20k, SD 3.5 about 0.19 / 0.14 / 0.128 / 0.118; next-tic PSNR against the 21.6 dB floor about 22.3 (10k) and 22.9 (20k) for the U-Net, 21.9 and 22.5 for SD 3.5; long-budget U-Net forecast 24.8 dB (23.5 to 26.0). Stop rules: wrong control/split/VAE contract means restart; three consecutive or over 0.1% skipped updates means stop and inspect; val 15% above trend twice means restore the last recovery; 0.3 dB below persistence at 20k with under 0.1 dB improvement means hold and diagnose, never reseed until it looks good. If the server returns late, Astra and the main session recommend train ids 0:1000 for BOTH backbones (the 16-channel encode of 2,000 episodes costs about 42 h of the remaining budget); Rohan decides. Spiderman: banner timeouts turned into immediate connection resets by 11:36 EDT (login queue full); needs console access. Tagged: goal 1.
- **2026-09-21 01:30 EDT: next-tic runs decided (Rohan) and the code path built, reviewed twice and merged; Spiderman unreachable since 22:40 EDT Sep 20.** Decisions: predict the next TIC; SD 1.4 U-Net and SD 3.5 Medium (PixArt third if a slot opens); PUBLIC pretrained starting weights (purity); dense split by episode id over arenas 2 to 5: train 0:6000, val 6000:7000, test 7000:8000 (`release/dense_split.json`; ids cycle through the four maps), unseen maps = arenas 6 to 8; these runs train on ids 0:2000 (10.07M tics, identical for both backbones; at batch 32 one pass is about 315k steps, which the U-Net reaches in about 3.6 days and SD 3.5 does not before the freeze; more data cannot help below one pass, and 16-channel latents for 6,000 episodes (1.24 TB) do not fit the 1.6 TB free); evaluation corpora val 6000:6100, test 7000:7100, arenas_678 0:60; context 32 tics; action conditioning copied from GameNGen (one token per context tic through cross-attention) but on the EXECUTED button vector per tic (19 bits, MLP plus learned positions; Arnold's anti-stuck overrides make the requested action id a wrong label on those tics), SD 3.5 pooled slot from the newest control only; context-noise augmentation uniform up to 0.7 in 10 buckets; batch 32 without accumulation, fused AdamW lr 5e-5 constant after 2k warmup, clip 1.0, EMA 0.9999, snapshots every 10k, run to the freeze; decoder choice, sampler steps and evaluation protocol are Rohan's and deferred (none blocks training). ROW CONVENTION (verified `record_arnold.py:264-273`, found by Astra): our recorder stores a row THEN steps, so row i carries the control LEAVING frame i, the opposite of the GameNGen reproduction; the contract is context `latents[r-32:r]`, target `latents[r]`, controls `buttons[r-32:r]` with `buttons[r-1]` producing the target and `buttons[r]` never used. Built by an Opus worker in its own worktree, reviewed line by line by the main session and in four passes by Astra (thread `01a0c1d5-f413-72f3-838d-9e28df232189`, `.claude/analyses/astra-nexttic-2026-09-20.md`, design note `.claude/analyses/nexttic-design-2026-09-20.md`): `doom_data.TicWindowDataset` (validity = consecutive tics, constant deaths, constant map; never chain ids), `train_wm.py --tic-stride 1 --action-history 32 --episode-ids --val-latents-dir --dense-segment --init-from`, resume identity check with the episode list stored in every checkpoint, `eval_tf.py --tic-stride 1 --horizon-tics K` with the decoder-independent floor `persist_psnr_raw` (the old `copy_psnr_raw` decodes the last latent and is NOT a persistence floor), per-tic rollouts, `check_action_alignment.py` (yaw-gated, shifts -1/0/+1 on one row set, nonzero exit for misaligned or inconclusive), launchers `launch_nexttic.sh` / `after_nexttic.sh` / `encode_nexttic.sh` (DRY and DOOM_ROOT safe). Reviews caught, among others: a launcher default that meant gradient accumulation, an alignment gate that passed an injected off-by-one, checkpoints saving control width 0, a changed embedding init order that broke bit-identity of finished rows (restored, pinned by a test against the merge-base code), an encode launcher that died after its first successful corpus, split files written where the evaluator does not read. Suite 611 passed. Launch gates on real data, in order: button audit (sidecar equals raw parquet), alignment gate on val episodes, fit check (memory, updates/s, loader workers), smoke run, launch. Stopped tonight per Rohan: grid queue (ctx16 at 27.5k of 30k, 25k checkpoint kept) and the budget-curve cell (its unfinished trainer edits parked on branch `wip/budget-curve-snapshot-cadence`). Launched before the outage: six per-tic 4-channel encode shards (tmux enc-d0..5, GPUs 1 and 2, episodes 0:2000) and the SD 3.5 tuned-decoder re-evaluation on GPU 3; state unknown. OUTAGE: from about 22:40 EDT Spiderman accepts TCP on port 22 but never sends the SSH banner, from the Mac and from Superman alike (ping 0.2 ms; Superman healthy), so the machine's sshd cannot serve new sessions; cause NOT established (candidates: our six shards with about 60 decode threads, the root disk that had 321 MB free, memory pressure from any job); one check every 20 minutes, no other probing; first action on access is load, memory, disk, then shard count. A launcher test that executed `encode_pertic.sh` for real whenever pytest ran on the server started stray GPU encodes twice on Sep 20; launcher tests are now dry-run only with a guard test, and agents run no pytest on Spiderman. Rohan's GPU rule: GPUs 1, 2, 3 until 10:30 EDT Sep 21, then GPUs 1 and 2 only. Tagged: goal 1.
- **2026-09-20 21:45 EDT: the gap to GameNGen decomposed; dense data is done; next run predicts every tic (Rohan's direction).** Dense corpus complete and verified: arenas 2 to 5, 8,000 episodes, 40.3M tics, 1.73 TiB; arenas 6 to 8 (recorded on Superman, streamed), 3,000 episodes, 14.8M tics, 725 GB; md5 manifests written; `/sata2/data` 1.6 TB free. As training examples: 40.3M per tic, 10.1M at stride 4 (arenas 2 to 5). GameNGen Figure 13 read against floors: its 5k-step point (24.7 dB) sits about 1 dB above the per-tic persistence floor of the reproduction's footage (23.8 dB); its 1M-example curve peaks at 25.9 dB (floor + 2.1 dB) at 20k steps x batch 128 = 2.6M examples seen, then overfits; our rows at 1.05M examples and 2.9M examples seen sit at floor + 1.9 to 2.1 dB (floor 18.9 to 19.4 dB at stride 4). So at equal data there is no unexplained modelling gap. [Withdrawn 2026-09-22, review H5: the 23.8 dB floor is a reproduction-footage proxy with unresolved provenance, not a GameNGen evaluation baseline, and floor-subtracted PSNR does not control motion, stochasticity, spacing, decoder or sampler; neither this sentence nor the additive decomposition below goes in the paper.] The 8 dB headline gap is roughly: tic spacing about 2.7 dB of floor, footage about 2.3 dB of floor, decoder ceiling 3 to 4 dB (GameNGen's frozen-decoder ablation scores 22.4 dB against 29.4 to 32.6 dB with its MSE-only decoder fine-tune at batch 2,048 for 700k steps; ours lifts the ceiling only from 23.6 to 28.6 dB with 100k presentations and an LPIPS term), and the rest data x steps (its curves separate only past 100k steps; 1M to 70M examples is about 3.5 dB at 700k steps). Other recipe differences: DDIM 4 steps with CFG 1.5 on the context and context dropout 0.1 (ours DDIM 50, no CFG), Adafactor constant lr 2e-5 clip 1.0, context 64 tics = 1.8 s (its ablation: 32 vs 64 is 0.05 dB). GameNGen and MultiGen use no diffusion forcing or self-forcing; those are rollout methods for after the dense run. SD 3.5 row finished 90k; the automatic evaluation fell back to the stock decoder because `after_sd35.sh` tested for both weight files with one `ls` (fixed, `RESCORE=1` added); stock-decoder numbers kept in `stock_decoder_eval/` (seen 20.19/0.279, EMA 20.42/0.257; PSNR@64 17.50, FVD 210/468), tuned-decoder rerun in progress on GPU 3. Snapshot reads (512 windows, tuned decoder) 40k/50k/60k/70k seen 20.14/20.69/20.85/21.14, unseen 18.45/19.13/19.25/19.45. Running: `budget-curve` (PixArt 30k with snapshots every 2.5k on GPU 1, floor-subtracted curve against Figure 13), `recipe-levers` (DDIM step ablation; MSE-only decoder fine-tune on dense frames for the ceiling) on GPU 3 after the rerun; grid `ctx16` on GPU 2. Open for Rohan: compute plan for a 300k-step-class dense run (one A6000: PixArt about 11 days at batch 128; H100 rental tabled), noise augmentation on or off, grid trim, dm_simple. Tagged: goal 1.
- **2026-09-20 08:30 EDT: overnight queue results (agent `night-runner`, `results/night_2026-09-19/`, LOG.md there).** (1) Inference noise: both stored rows reproduced (U-Net PSNR@64 16.09 vs 16.03, PixArt 17.12 vs 17.18); `--infer-noise` 0.035 to 0.3 moves PSNR@64 by under 2 SE, IDM top-1 falls monotonically (U-Net 0.511 to 0.487, PixArt 0.493 to 0.464), FVD worsens; teacher-forced at context level 0.035 / 0.07 (paired, 512 windows) gives at most +0.10 dB on PixArt seen and significantly worse LPIPS on held-out maps. Verdict: clean context with bucket 0 stays; no headline number changes; rollout drift is not an inference-noise problem (copy-seed 17.82 dB @64 still beats both rows). (2) Grid cell `noaug` (PixArt, 30k, context-noise augmentation off) against `base30k`: seen 21.27/0.289 vs 20.98/0.311, unseen 19.51/0.410 vs 19.41/0.436, unseen2 21.67/0.385 vs 21.41/0.409, PSNR@64 17.92 vs 17.87, IDM 0.546 vs 0.478, FVD16/32 219/469 vs 186/379. So at this budget noise augmentation costs about 0.3 dB and 0.02 to 0.03 LPIPS teacher-forced, buys nothing in rollout PSNR or action fidelity, and buys FVD only; no prior paper reports this cost. (3) Dense `arenas` verified: 8,000 episodes, 2,000 per map, 40,285,059 tics, 1.73 TiB, md5 manifest written; floor at 4 tics per map 19.55 / 18.94 / 20.18 / 17.57. `arenas_678` at 2,704 of 3,000 at 08:00 (Superman slower than projected), floors 18.25 / 16.76 / 20.15. `verify_dense.py --min-tics` loosened to 4600: episode length is 5286 minus 39 tics per death (R2 1.0), so 4800 flagged healthy episodes. (4) Open GameNGen reproduction footage against ours, identical stock decoder: floor at 4 rows 21.23 vs 18.94 dB, stock ceiling 24.38 vs 23.64, headroom 3.14 vs 4.71; its action runs sit on a strict 4-tic grid (44,616 of 44,616). GameNGen's 29.43 exceeds the stock ceiling of that footage, so it needs a tuned decoder as well as next-tic scoring. (5) `deathmatch_simple` fixed: `--zdoom-bots` never reached Arnold because `functools.partial` keywords are overridden by Arnold's explicit `use_scripted_marines=True`; `forced_game` applies ours last; proof pair on the same seeds 9 deaths / 2 frags / health 5 to 100 against 0 / 0 / flat. Launcher gate left on pending Rohan. (6) Encoding sizes corrected: live latents are (4,32,40), so dense arenas every tic is 467 GiB / 169 card-hours, decision frames only 117 GiB / 42 h (`scripts/spiderman/encode_dense.sh`, MODE=decisions); per-tic latents of the original plus evaluation corpora are 61 GiB / 22 card-hours (`encode_parquet.py --every-tic`, `scripts/spiderman/encode_pertic.sh`; `is_decision` rows reproduce the stride-4 latents to 1.45e-05 mean abs when the encode batch matches the reference: 64 for training, 16 for evaluation, because bf16 cuDNN kernels depend on batch shape). Neither encode launched: GPU 1 is shared with `hengjinz`. SD 3.5 row at 78.2k of 90k, 0.515 updates/s. Open decisions for Rohan: training stride; keep or drop noise augmentation in the dense-corpus recipe; which encode to launch and where; record dm_simple. Tagged: goal 1.
- **2026-09-19 20:15 EDT: rollout-stability literature checked; one mismatch found in our own recipe.** Card `lit/rollout-stability-training.md`. Our context-noise augmentation matches GameNGen's stated numbers (max 0.7, 10 buckets, level given to the model), but (a) we roll out and evaluate with exactly clean context and bucket 0, while in training bucket 0 means a level drawn in [0, 0.07) (`diffusion_v.py` `noise_augment`, verified), so the model never saw exactly clean context; GameNGen calls the inference noise level a tunable and never states its value, Diffusion Forcing argues the clean fed-back frame is the out-of-distribution one, and we never swept it; (b) our 0.7 is on the variance-preserving scale while the reproduction's 0.7 is additive, so at most one matches GameNGen; (c) no paper reports whether noise augmentation costs teacher-forced PSNR, which our `noaug` grid cell measures. Overnight queue (agent `night-runner`, Spiderman GPU 1, results under `results/night_2026-09-19/`): rollout sweep of `--infer-noise` in {0, 0.035, 0.05, 0.1, 0.2, 0.3} and a teacher-forced check at context level {0, 0.035, 0.07} for the U-Net and PixArt rows; if the near-clean level beats 0.0 beyond the standard error, every headline number should be re-scored. Cheapest follow-ups ranked by the card: the sweep, history guidance (MultiGen's Doom recipe, no retraining), a true zero bucket. Tagged: goal 1.
- **2026-09-19 20:00 EDT: CORRECTION, frame spacing. The Sep 10 claim that GameNGen and its reproduction predict every 4th frame is wrong.** Evidence card `lit/frame-spacing-prior-work.md` (primary sources, quotes). The open reproduction stores and trains on CONSECUTIVE tics: `load_model_generate_dataset.py:132-133` sets `frame_skip = 1` ("capture all frames"), chooses an action every 4th tic and repeats it; `skipframe-4` names the action repeat, no frames are dropped; its training window is 10 consecutive tics (0.29 s). GameNGen states action repeat 4 (App. A.5) and never states a training stride; the best-supported reading is consecutive 35 Hz tics (App. A.6: "teacher-forced trajectories on 35FPS data"), and its frames-to-seconds conversions at 20 fps are internally inconsistent, so do not cite its context length in seconds. Consequence: its 29.43 dB is most likely a next-TIC teacher-forced number and is not comparable to our next-decision (4-tic) numbers; on our footage persistence alone is 21.3 dB at 1 tic against 18.8 at 4. Field pattern: RL world models (SimPLe, DIAMOND Atari, STORM) step per decision and discard skipped frames; neural game engines (GameNGen, MineWorld, Matrix-Game 2.0) predict every rendered frame with the action repeated; video-trained models subsample for sequence length (GAIA-1 6.25 Hz, WHAM and Genie 10 Hz). No game paper ablates spacing; NWM reports 1 fps better short-horizon and 4 fps better past 8 s; Vid2World's head-to-head tables are unread. The two persistence numbers in our notes do not conflict: 26.2 dB per tic (R1.3, Sep 9) was the Stiegler PPO footage, 21.3 dB is Arnold deathmatch footage. Actions: per-tic latents of the original and evaluation corpora are being encoded overnight into new directories so a matched stride 1 / 2 / 4 experiment can launch after Rohan approves the design; R1.3's rationale stands as our choice but its literature basis is withdrawn. Open decision for Rohan: training stride. Tagged: goal 1.
- **2026-09-19 19:30 EDT: every-tic dense recording live on both servers; train/test split deferred (Rohan's decisions).** Rohan rejected decision-only recording ("all recordings should be every single tick"; training per tic versus per decision is still to be debated) and said recording decides nothing about what trains or tests. Live: Spiderman tmux `rec-dense`, arenas 2,3,4,5, 8,000 episodes, corpus id `arnold-train-dense-v1`, `$D/raw_arnold_dense/arenas`, 32 workers at nice 10, about 670 episodes/h (31 to 34 tics/s per worker), finish about 07:00 EDT Sep 20, about 2.0 TB. Superman tmux `rec-arenas678` plus `xfer-arenas678`: arenas 6,7,8, 3,000 episodes, corpus id `arnold-dense-arenas678-v1`, 16 workers at nice 19 in a new 1.7 GB env `doomrec` (the `Doom` env has no ViZDoom), 194 episodes/h, each episode rsynced to Spiderman `$D/raw_arnold_dense/arenas_678` over Superman's existing key and removed locally behind a zero-byte marker (spool under 2.4 GB, Superman free disk 262 GB); finish about 06:30 EDT, about 0.8 TB. More Spiderman workers do not help: 32 physical cores, the machine saturates near 1,000 tics/s, and 20 workers on Superman gave the same total as 16. Measured and kept unused: `--decision-only` mode (2.0x faster, 4.05x fewer bytes) is a DIFFERENT draw from the game, because ViZDoom does not step identically under `make_action(b,4)` and four `make_action(b,1)` calls (divergence within 5 to 35 decisions even with no bots), so skip-4 data is not a subsample of per-tic data. Per-tic PNG level 1 records 1.76x faster for 17.3% more bytes and level 3 1.52x for 8.8%; the live jobs stay at level 6 (disk). `deathmatch_simple` is BLOCKED: under Arnold no opponent ever appears, with scripted marines or `--zdoom-bots`, and `pukename change_difficulty 5` is not the mechanism; the map works when driven from bare ViZDoom with the reproduction's bot arguments, so the gap is in Arnold's game setup (`SEGMENT=dm_simple` refuses without `DM_SIMPLE_OK=1`; `verify_dense.py` checks counts per map, tics per episode and writes the md5 manifest). Launcher `scripts/spiderman/record_dense.sh`, doc `release/DENSE_CORPUS.md`. Spiderman root (/) is 100% full and /home has 16 GB free: write only under `/sata2`. Tagged: goal 1.
- **2026-09-19 17:00 EDT: dense corpus redesigned; map selection fixed BEFORE any model result on it (Rohan's decision).** The first attempt (maps 3, 10, 12, 13) was stopped after 134 episodes because those maps were picked from per-map model scores, which is selecting on the outcome; the data stays unused in `$D/raw_arnold_dense4`. Evidence card `lit/doom-maps-prior-work.md`: no Doom world-model paper names a reusable map (GameNGen says only "5 different levels"; PlayGen names nothing; MultiGen uses 100 unreleased Obsidian-generated maps and one unnamed deathmatch map, with Arnold as its agent); the only named, shipped map is `deathmatch_simple.wad` from the two open GameNGen reproductions (2.47M public frames; note its "lvl5" is an ACS difficulty level on one map, not a map, and it stores every tic with actions repeated 4 tics, as we do). Decision: TRAIN on Arnold arenas 2, 3, 4, 5, the agent paper's own published training maps (Arnold README: `--map_ids_train "2,3,4,5"`, `--map_ids_test "6,7,8"`, verified on the server), plus `deathmatch_simple`; TEST on arenas 6, 7, 8, the agent paper's test maps, with the existing held-out arenas 16, 17 and the 13 curated maps as further transfer sets. Model-free map properties (from raw recordings, five episodes per map; explored 128-unit cells / 4-tic persistence PSNR): map 2 226 / 19.0, map 3 155 / 18.4, map 4 321 / 19.6, map 5 160 / 17.3; test maps 6 162 / 18.9, 7 89 / 16.2, 8 118 / 19.5. `deathmatch_simple` under Arnold as-is is empty: a full 150 s episode had 0 kills, 0 deaths, constant health and ammo, 80 cells, so the reproduction's ACS difficulty command is being added to the recorder. A worker is building a decision-only fast recording mode (native frame skip; per-tic recording saturates near 1,000 tics/s on the 64-core server: 36 tics/s per worker at 8 workers, 20 at 48) with a bit-identity test against the per-tic recorder; relaunch will use 32 workers to leave a third of the machine free. Tagged: goal 1.
- **2026-09-19 15:30 EDT: why GameNGen's PSNR is high, and the dense 4-map corpus (recording).** Measured on our raw seen evaluation frames, persistence PSNR by frame gap: 1 tic 21.3 dB, 2 tics 20.1, 4 tics (our decision spacing) 18.8, 8 tics 18.1; so the footage is violently dynamic even one tic apart. We predict one frame per 4-tic decision because Arnold decides every 4 tics and, per the Sep 10 decision, GameNGen and its open reproduction also use frame skip 4 (its 20 FPS is generation speed, not game-time spacing), so frame spacing does not explain the gap to its 29.4 dB; the remaining suspects are data density (about 100x more frames per map on about five levels), 30x more training, evaluation on the training levels, and calmer single-player footage. Rohan's decision: record a much larger corpus on few maps, separate from the original. `arnold-train-dense4-v1` in `$D/raw_arnold_dense4` (README there and `release/DENSE4_CORPUS.md`; recorder `scripts/spiderman/record_dense4.sh`): maps 3, 10, 12, 13, chosen from the per-map seen evaluation in which the U-Net and PixArt rank the 15 maps identically (map 10 easiest 23.8 dB / 0.198, maps 3 and 12 middle, map 13 hard 20.0 dB / 0.331; mean slightly easier than the 15-map mean); 8,000 episodes of 150 game-seconds (2,000 per map, 40x the original density, about 10.5M decision frames, about 2.2 TB), every tic lossless, same schema, seeded by (corpus id, episode id) so it is bit-for-bit reproducible, unlike the original training corpus. Pilot: one worker records about 36 tics/s (146 s per episode). Launched 15:05 EDT (14:18 server log) with 48 niced workers: 72 episodes in the first 7.6 minutes, load average 76 on 64 cores, training throughput unchanged (SD 3.5 row 0.51 updates/s, grid cell 1.25); projected about 14 hours, first 2,000 episodes by about 18:30 EDT. Planned uses: 4 maps at 1M frames against 17 maps at 1M frames under the same recipe and budget (density against diversity), then the 10x and 40x scale points; latent encoding (about 4.6 A6000-hours per 1M decision frames in the 4-channel space) is the real cost. Tagged: goal 1.
- **2026-09-19 14:30 EDT: early SD 3.5 read is not a win; brief for Changliu.** Teacher-forced evaluation of the 40k snapshot (`035-sd35-l32-aligned/early_40k`, 512 windows, tuned SD 3.5 decoder, raw frames): seen 20.15 / 0.308 (copy-last 19.01 on the same windows, ceiling 31.68 / 0.024, HUD 29.9), unseen arenas 18.45 / 0.430 (copy-last 18.32, ceiling 29.68 / 0.031). For scale: the U-Net at 90k is 21.36 / 0.270 and 19.14 / 0.446, and PixArt at only 30k is 20.98 / 0.311. So at 40k the 2.25B model in the better latent space is below the 4-channel rows on PSNR, level on seen LPIPS, and has the best unseen LPIPS of any row; it uses about 9% of the room between persistence and its ceiling where the U-Net and the video row use about 21%. Hypotheses to test, none yet verified: the 16-channel target is four times larger with the same data; SD 3.5 was pretrained with flow matching and we retrain it with the shared v-prediction DDPM objective (the video row kept its native flow objective); lr 5e-5 may be low for it; 40k is early. Final numbers Monday; the 20k/30k/40k snapshots give a budget curve. The paper's spine therefore should not depend on SD 3.5 winning. A four-page brief with four figures (row comparison, headroom by latent space, a seeded rollout filmstrip of all rows against ground truth, the first grid cells), three candidate angles and six questions was written for Changliu: `samples/professor_brief_2026-09-19/` (PDF, HTML, PNGs). Session note: the app is now signed in under the CMU account, which cannot write the earlier artifacts (owner-only republish; cross-organization data store closed), so the dashboard and progress doc are frozen until the owning account is used again; stewards skip dashboard writes. Tagged: goal 1.
- **2026-09-19 13:30 EDT: from-scratch grid cell; Opus weekly limit.** Grid cell `scratch` (PixArt architecture, random init, 30k updates) against `base30k`: held-out loss 0.2474 vs 0.2161; teacher-forced seen 20.77 / 0.349 vs 20.98 / 0.311, unseen 19.44 / 0.467 vs 19.41 / 0.436, unseen2 21.57 / 0.409 vs 21.41 / 0.409; rollout PSNR@64 17.60 vs 17.87, LPIPS@64 0.558 vs 0.559, IDM 0.473 vs 0.478; FVD16/32 345 / 893 vs 186 / 379. Reading: at this budget the pretrained start buys almost no PSNR and no action following; it buys perceptual quality (LPIPS 0.04 on seen, 0.03 on unseen) and above all temporal realism (FVD halves). A PSNR-only evaluation would have called pretraining useless. SD 3.5 row at 13:15 EDT: 42.9k of 90k, 0.51 updates/s, held-out loss 0.1155 to 0.1162 over 40k to 42k (flattening), zero skips, snapshots at 20k, 30k, 40k; ends about 15:00 EDT Sunday. Cell `noaug` is training. The Opus weekly limit was hit at about 10:00 (resets Sep 23 12:00 EDT), which killed the day steward after one tick; stewarding moved to a Sonnet agent on the same plan, continuous turn until Sunday 09:30. Tagged: goal 1.
- **2026-09-19 09:40 EDT: clean night; first recipe-grid cell.** All three jobs survived the night (16 steward ticks, no relaunches). SD 3.5 row 035: 34.9k of 90k, 0.48 updates/s, held-out loss monotone from 0.1255 (15k) to 0.1173 (34k), zero spike-guard skips, snapshots at 20k and 30k on disk. Grid cell `base30k` (PixArt, 30k updates, shared recipe): final held-out loss 0.2161; teacher-forced seen 20.98 / 0.311 (EMA 20.86 / 0.317), unseen 19.41 / 0.436, unseen2 21.41 / 0.409; rollout PSNR@64 17.87, LPIPS@64 0.559, IDM top-1 0.478 (real 0.847), FVD16/32 187 / 380. Against the full 90k PixArt row (seen 21.35 / 0.272, unseen 19.39 / 0.434, unseen2 21.29 / 0.411, PSNR@64 17.18, FVD 211 / 472): one third of the budget already matches it on both unseen corpora and beats it on rollout PSNR and FVD; the extra 60k updates buy only seen-map perceptual quality (LPIPS 0.311 to 0.272). EMA does not help at 30k (it helped at 90k). Cell `scratch` is at 29.3k with held-out loss 0.248 against the base's 0.216, so the pretrained start is worth about 0.03 at this budget; its evaluation follows. Each cell costs about 6.5 h of training plus 2.5 to 3 h of evaluation. Tagged: goal 1.
- **2026-09-18 22:00 EDT: snapshots for the SD 3.5 row; a self-inflicted 35-minute outage.** Rohan approved weights-only snapshots so the row can be evaluated at intermediate budgets: `train_wm.py --local-snapshots` keeps the compact bf16 snapshot (live and EMA, about 9 GB) in the results dir every `--snapshot-every` updates, and `launch_sd35.sh` now asks for one every 10k (20k, 30k, ... 90k). The finished 4-channel rows keep only 85k, 90k and best, so they have no intermediate points. To pick the flag up without losing progress, a one-shot waiter restarted the row at its 15k checkpoint; its `pgrep -f "train_wm.py --backbone sd35" | head -1` matched the tmux SERVER (whose arguments contain the first session's command) and killed every session at 21:21 EDT: the row, the grid queue and the evaluation waiter. The steward diagnosed it correctly, held off as instructed and escalated. At 21:56 the main session removed the stale socket and the faulty script, pulled main, and relaunched all three: the row resumed from `0015000.pt` with snapshots on, the grid's base cell from its 20k checkpoint, and the evaluation waiter. Cost: 35 minutes on both cards and about 3.5k grid updates. Rule added to CLAUDE.md: stop tmux jobs with `tmux kill-session`, never by pattern. Also today: 10 corpus samples and the loss plots are under `samples/`; the partner progress report was updated and exported as a PDF. Tagged: goal 1.
- **2026-09-18 16:35 EDT: row stays on one card; GPU 2 to the grid; 034 unseen2.** Two-card DDP at per-GPU 16 ran out of memory at the first optimizer step on GPU 2 (43 GB available beside another user's 4.3 GB job; the single-card measurement had that configuration at 39 GB before all-reduce buffers), so the SD 3.5 row 035 continues on GPU 3 alone: resumed from the 4k checkpoint at 15:12 EDT with optimizer, EMA and RNG state, spike guard armed (threshold 5, `--skip-grad-after 3000`, observed norms about 0.27), 0.46 updates/s, val 0.1364 at 4k, 6k at 16:30; 90k expected about Sun 19:30 EDT. `launch_sd35.sh` fixed to launch accelerate through `python -m accelerate.commands.launch` (the wanenc env has no CLI entry point). UniDiffuser 034 unseen2 (13 curated maps): 21.25 / 0.419, copy-last 20.92, so parity with the other 4-channel rows (DiT 21.43 / 0.413, U-Net 21.15 / 0.412, PixArt 21.29 / 0.411). The 16-channel evaluation corpora (`latents_arnold_eval_sd35/{seen,unseen,unseen2}` with split files) are encoded. Knob grid queue launched on GPU 2 at 16:30 in the order base30k, scratch, noaug, data-1/4, ctx8, ctx16, eps, lr1e-4, adaln, data-1/2, lr2.5e-5, data-1/8 (about 10 h per cell on one card, so the queue runs into Tuesday and will be trimmed to the cells that matter once the first results land). Tagged: goal 1.
- **2026-09-18 14:50 EDT: SD 3.5 decoder gate PASSES on every corpus; UniDiffuser evaluated; SD 3.5 row training.** Gate (`results_spiderman/vae_gate_sd35/metrics.json`, decoder-only tune of the SD 3.5 16-channel autoencoder, encoder frozen, MSE + 0.1 LPIPS, 100k presentations, versus our tuned SD 1.x decoder on the same frames, paired episode bootstrap): seen 31.68 / 0.0247 / HUD 34.60 vs 28.61 / 0.0509 / 31.79 (+3.06 dB, LPIPS halved, HUD +2.8); unseen 29.62 / 0.0311 / 33.63 vs 26.35 / 0.0699 / 30.36 (+3.27 dB); unseen2 31.94 / 0.0263 / 34.06 vs 28.89 / 0.0581 / 30.83 (+3.05 dB); development set 31.42 / 0.0246 / 34.73 vs 28.34 / 0.0508 / 32.09. All CIs exclude zero; the go criterion (+1 dB, LPIPS and HUD no worse) is passed by a factor of three. The stock 16-channel decoder alone is worse than our tuned SD decoder on PSNR (26.47) and HUD (20.4), so the decoder tune is essential, not optional. Tuned decoder saved at `$D/vae_decoder_sd35_lpips/vae`. UniDiffuser 034 (live weights; EMA in parentheses): seen 21.11 / 0.293 (21.34 / 0.281), unseen 19.25 / 0.454, HUD 29.2, rollout PSNR@64 17.00, LPIPS@64 0.551, FVD16/32 206 / 423, IDM top-1 0.472 (real 0.847). It lands between the DiT and PixArt on perceptual quality and below the U-Net on rollouts and FVD, so LAION-scale pretraining in the same 4-channel latent space did not move the row: the latent space, not the pretraining corpus, is the lever. unseen2 scoring for 034 still to run. SD 3.5 row 035: launched 12:40 EDT on GPU 3 at per-GPU batch 32 with checkpointing (sweep: 0.455 updates/s, 36.4 GB; batch 32 without checkpointing and batch 16 without checkpointing both OOM); the first launch died at step 0 because the spike guard threshold of 5 was applied from update 0 while post-inflation gradient norms are 12 to 21 (21 skips tripped the trainer's stop); relaunched with the guard off, and the trainer now has `--skip-grad-after` (guard inactive for the first N updates; launcher uses 3000). At 3.5k updates: loss 0.144, val 0.1400 at 3k (not comparable to the 4-channel rows' v-loss), 0.46 updates/s, gradient norm 0.3. Plan: at the 4k checkpoint, measure a two-card DDP fit check on GPUs 2 and 3; relaunch across both cards only if it beats about 0.6 updates/s (all-reduce of 2.25B parameters over PCIe may eat the gain), otherwise keep one card and use GPU 2 for the unseen2 scoring, the 16-channel evaluation-corpora encode and grid cells. Tagged: goal 1.
- **2026-09-18 09:45 EDT: SD 3.5 fast track; SSH block diagnosed; tabled options.** The 08:15 outage was not a VPN problem (we never use one): port 22 timed out to every CMU host while github.com:22 connected, so CMU's network edge was dropping SSH from our public IP, most likely for connection bursts from several agents plus retry loops; a phone hotspot restored access at once. `~/.ssh/config` now multiplexes both lab servers (one master per server, 8 h persist), and the rule is one server-touching agent at a time and no retry loops (CLAUDE.md). Rohan's direction: fast-track the SD 3.5 Medium row. Done: Flux decoder gate stopped at step 1100 of its rerun to free GPU 3 (its development-set result stands at 32.44 dB; the save failure was a non-contiguous channels-last tensor, fixed with a pack-verify-reload save and per-epoch checkpoints); SD 3.5 decoder gate running on GPU 2 beside the UniDiffuser evaluation; SD 3.5 corpus re-encode on GPU 3 at 78 frames/s over six shards (flat in batch size, card at 100%), training corpus due about 12:30 EDT, `split_fit.json` written; launcher `launch_sd35.sh` (single card or two-card accelerate at per-GPU 16, `--grad-ckpt`, `--skip-grad-norm 5` from step 0) and `after_sd35.sh` merged; the IDM cannot read 16-channel latents, so rollouts are decoded and re-encoded through the SD 1.x encoder (`--idm-reencode-vae`), and only this row's own `idm_real` is its reference; 140 tests pass on main. Plan: throughput sweep on GPU 3 at about 12:30, single-card launch about 13:00, two-card relaunch about 15:00 when the UniDiffuser evaluation ends, 90k by Saturday night, evaluation Sunday. Tabled by Rohan for now: rented H100/H200 time (priorities if taken up: second seed of the headline, the full recipe grid, Lumina-Image 2.0 as a second 16-channel backbone, SD 3.5 Large last) and Matrix-Game 2.0. SD 3.5 Large is not trainable here (about 8.1B parameters, 130 GB of optimizer state); the TensorRT repository is an inference export only. Tagged: goal 1.
- **2026-09-18 07:30 EDT: UniDiffuser finished; the 16-channel decoder tune is a large win on the development set but its weights were lost; link back.** The CMU VPN outage lasted 23:11 to 07:20 EDT; the server ran through it (uptime 17 days) but nothing could be launched, so both cards idled from about 01:00. 034-unidiffuser-l32-aligned reached 90k at about 00:50 EDT: final held-out v-loss 0.2048 at 90k (also the best), 10 spike-guard skips over the 60k updates since the restore (U-Net 0.2043, PixArt 0.2031, DiT 0.2139 for comparison). Its evaluation was launched by hand at 07:25 on GPU 2 (`after_run2.sh` with the wanenc python). Flux-VAE decoder tune (Lumina copy of the Flux autoencoder, 16 channels, encoder frozen, MSE + 0.1 LPIPS, 100k presentations at micro 16 x accum 2): development-set validation at the end of training PSNR 32.44 / HUD 35.65 / LPIPS 0.0180, against our tuned SD 1.x decoder's 28.34 / 32.09 / 0.0508 and the stock 16-channel decoder's 27.46 / 21.46 / 0.0298, so about +4.1 dB, +3.6 dB HUD and a 2.8x lower LPIPS before any paired scoring. The save step then failed (`tune exit 1`, only config.json written) and the scoring step crashed on the missing weights, so the tuned decoder must be retrained (about 2.5 h; frames cached) before the paired gate on seen/unseen/unseen2 can run; a worker is fixing the save, rerunning, then running the SD 3.5 gate. Also: the server repo pull was blocked by the previous agent's hand-copied files; being cleared. SD 3.5 real-weight gates passed on the server CPU (parity 0.0 against the 2.246B-parameter transformer; stock-decoder round trip 24.98 dB on one episode); the memory estimate for the row moved to 44 to 46 GB allocated at batch 32 with checkpointing, so the sweep's micro-16 accumulation fallback or 8-bit Adam may be needed (a Rohan decision: accumulation versus a recipe deviation). Tagged: goal 1.
- **2026-09-18 00:30 EDT: overnight builds landed; gate in progress; video row unseen numbers.** Three worker deliveries on the worktree branch, verified by the main session (81 fixture tests pass; `verify_sd35.py` all gates true): (1) `SD35WorldModel` (SD 3.5 Medium, 16-channel latents, `latent_channels` generalised across trainer, evaluators and loader; EMA keyed by parameter name; `encode_parquet.py --vae-id/--scaling-factor/--shift-factor`; `scripts/spiderman/fit_sd35.sh` sweep; `torch.compile` unsafe as written), CPU parity exact, real-weight parity and encoder round-trip queued behind the VPN outage; (2) the knob grid: `--train-fraction`, `--objective {v,eps}` read back by the evaluators (eps and v agree on x0 to 1e-4), PixArt `--warm-start none` and `--action-inject {token,adaln}`, `scripts/spiderman/launch_cell.sh` and `grid_queue.sh` (12 cells, 30k updates, evaluation chained; cells sit at 55% of the card by construction because micro-batch already equals the recipe's global batch and token count is independent of context length); (3) the VAE gate: `finetune_decoder.py` generalised to any autoencoder, `vae_gate_score.py` with a paired episode bootstrap, `scripts/spiderman/vae_gate.sh`. Gate facts so far: stock 16-channel (Flux/Lumina copy, Apache 2.0) decoder on the 2,000-frame development set 27.46 dB / LPIPS 0.030 / HUD 21.5 versus our tuned SD decoder 28.34 / 0.051 / 32.1; micro-batch 32 needs 45.3 GB of the card's 47.4 GB usable and OOMed twice, so the tune runs at micro 16 x accum 2 (recipe unchanged). Video row 050 unseen teacher-forced (512 windows, raw frames): PSNR 18.80 (sem 0.09) / LPIPS 0.328 / copy-last 17.75 / Wan ceiling 23.08 / 0.144. The CMU VPN dropped at 23:11 EDT; server jobs continue in tmux; the night shift retries. Tagged: goal 1.
- **2026-09-17 11:45 EDT: headline-backbone question, three evidence cards, Astra round 11 (NEW thread 01a0b255-2996-72e0-aa0a-b4cb618babd2; the 01a0aace thread is gone; brief `.claude/analyses/astra-brief-2026-09-17-backbone.md`).** Rohan's goal: the strongest backbone on the open Doom dataset, shown against prior Doom U-Nets; he allows a full re-encode. Cards (`lit/backbone-candidates-image.md`, `lit/backbone-candidates-video.md`, `lit/doom-baselines-2026-09-17.md`): no reproducible external Doom U-Net number exists (GameNGen released nothing, its 29.43 dB is seen-map teacher-forced; PlayGen 131M DiT at 128x128 reports 20.41 / 0.285 at horizon 32; MultiGen's reimplementation gives rollout LPIPS 0.442 with no architecture), so the claim is a system comparison against our matched SD 1.4 U-Net on the released benchmark. SD3's ablation puts the 16-channel VAE 3.5 dB above the 4-channel one generically, but our Doom-tuned 4-channel decoder already reaches 28.61 dB, so the 16-channel ceiling after the same fine-tune is the gate. Memory: fp32 master + AdamW at 16 B/param caps one A6000 near 2B (SD 3.5 Medium 2B about 36 GB at batch 32; Lumina-Image 2.0 2.6B needs the trainer's AdamW8bit); Cosmos-Predict2.5 2B sits on the Wan VAE our video row already bounds (24.83 dB stock), and no published decoder domain-tuning gain exists for video VAEs. Astra's proposal: SD 3.5 Medium (gated repo, community license) first, Lumina-Image 2.0 (Apache 2.0, Flux VAE) second, Cosmos/Matrix-Game last; gate = decoder-only fine-tune (encoder frozen, MSE + 0.1 LPIPS, 50k cached frames, about 3,125 updates at batch 32, under 8 card-hours) with go at +1 dB paired over our tuned decoder (about 29.6 dB seen, LPIPS and HUD no worse, bootstrap CI excluding zero), scored on the 320x240 crop; schedule GPU 3 gate Thu, encode Fri, headline row through Mon, eval Tue; GPU 2 UniDiffuser eval Fri then the grid; cut order U-Net continuation, redundant cells, then the headline if its path slips. Claim wording: a system comparison (VAE, size, pretraining, objective all change), never architectural superiority or superiority to GameNGen. Tagged: goal 1.
- **2026-09-17 09:45 EDT: video row 050 finished; first teacher-forced read; 034 recovering.** 050-skyreels-l8-flow reached 10k updates at 06:44 EDT (final val 0.1734, best 0.1731 at 9.5k). Teacher-forced evaluation with `eval_video.py --teacher-forced --steps 50` on 512 windows per corpus (batch 32, full-window causal decode; 0.11 windows/s, so the 2,048-window protocol would take 5 h per corpus and is deferred), seen corpus, against raw frames: PSNR 20.11 (sem 0.12) / LPIPS 0.305 (sem 0.004); copy-last 18.85; Wan-VAE ceiling 24.83 / 0.122; HUD PSNR 19.59. Against the decoded target: 21.44 / 0.270. Read: the row sits 1.2 dB below the U-Net and about at the DiT's LPIPS on raw frames, but its ceiling is 3.8 dB and 0.07 LPIPS lower than the SD VAE's (28.61 / 0.051), so the gap to its own ceiling (4.7 dB) is smaller than the U-Net's (7.2 dB); the Wan VAE's 4x temporal compression, not the transformer, bounds this row on per-frame metrics. Unseen corpus running; rollouts, FVD and IDM not yet run (sampler choices for rollouts, 50 vs 20 steps and ctx-stabilize 0 vs 20, are Rohan's call). Copy-last differs from the SD rows' 19.41 because it is measured on 512 windows through the Wan decoder. 034: after the 30k restore at lr 1e-5 with the spike guard, val 0.2134 (36k), 0.2134, 0.2130, 0.2127 (39k), below the pre-restore 0.2188 at 33k; zero skipped updates so far (the guard never fired; the steward's morning note about skips misread the `skipped_updates` counter). Tagged: goal 1.
- **2026-09-17 06:20 EDT: UniDiffuser second excursion at 34.6k and the second restore (Astra round 10, thread 01a0aace).** Same signature as the 8.5k event at half the learning rate: one update with pre-clip gradient norm 1894 (window mean 38.5, clip_frac 0.18), train loss 0.218 to 0.31 to 0.35 within 100 updates, val 0.2188 (33k) to 0.3154 (35k, flagged) recovering only to 0.2876 by 39k, and the mid-block `attn1.to_q` update ratio collapsing from 1.2e-4 to 1e-13 (frozen). Damage sits in val quartile 0, which is the LOWEST-noise quartile (t in 0..249; earlier notes calling it highest-noise were wrong). Astra's reading: `clip_grad_norm_` is applied before the step, so clipping bounds the gradient but not Adam's preconditioned update; attention-logit saturation (Zhai et al. 2023, entropy collapse) is the plausible mechanism, not proven. Decision (pre-agreed fallback plus containment): killed the run by pid at 39.5k, moved the post-spike `0035000.pt` and the full log to `excursion2/`, truncated log.jsonl to step 30000, restored `0030000.pt` with optimizer, EMA and RNG at lr 1e-5, and added `--skip-grad-norm 5` (new trainer flag, default off: skips the optimizer step, schedule and EMA when the pre-clip norm exceeds the threshold, PaLM-style; 5 is about 20x the healthy norm of 0.17 to 0.25 and above the benign 1.5 spikes). Launcher gains an `EXTRA` passthrough. Lost 9.5k updates (about 3 h); ends about Fri 04:00 EDT. If the attention update ratio collapses a third time, the rescue stops and the row is reported as unstable under the shared recipe. Two recipe deviations are now on this row (lr 1e-5, spike guard) and must be stated in the paper. Tagged: goal 1.
- **2026-09-17 00:40 EDT: Superman fit check ran after all (GPU 6 freed at 23:09; two cards left free).** With `--grad-ckpt` on for every config: DiT per-GPU batch 16 (accum 2) 0.498 updates/s, peak 12.29 GB allocated, 300 steps clean; U-Net OOMs at per-GPU 8 and 16 before the first step even with checkpointing (batch 4 from Sep 9 is the ceiling); PixArt failed to build (diffusers 0.37 pos-embed error, fixed in `backbones.py` cd89c89 but Superman's checkout predates it). Projection: DiT 90k on one A4000 50 h, 30k 17 h. Superman is therefore a DiT-only or 30k-cell machine until the U-Net batch-4 rate (0.30/s, 83 h per 90k) is accepted or PixArt is re-checked after a pull. Log `/home/rohan/Doom/logs/fitcheck_master.log`. Tagged: goal 1.
- **2026-09-17 00:10 EDT: Superman readiness check (Opus agent, no runs launched).** Env `Doom`: Python 3.14, torch 2.11 cu128, diffusers 0.37.1, no transformers (UniDiffuser cannot build there). Repo fast-forwarded to main; PixArt transformer and UniDiffuser unet downloaded to `/home/rohan/Doom/weights/hf`; disk 283 GB free (floor 250). Blockers found: (1) every launch needs `LD_LIBRARY_PATH=/home/rohan/miniconda3/envs/Doom/lib` (system libstdc++ lacks CXXABI_1.3.15 for timm/optree), (2) diffusers 0.33+ requires `output_type="pt"` in `get_2d_sincos_pos_embed` (fixed in `backbones.py` by signature inspection, works on 0.31 and 0.37), (3) `transformers` must be installed. Superman holds the Sep 9 unaligned latents (11 GB, no chain_id, so `--require-verified-transitions` is unusable); the aligned set is 11.5 GB and the swap is footprint-neutral but deletes Rohan's data, so it waits for his word. No fit check ran: only GPUs 4 and 5 were free all night and the leave-two-free rule held; a waiter (tmux `fitcheck`, deadline 02:30) runs the five configs if a card frees. Prior A4000 rates (0.30 U-Net, 0.50 DiT updates/s) put a 90k row at 50 to 83 h on one card, so Superman suits 30k-budget grid cells, not 90k rows. Two failed logins early (parallel ssh) were followed by 10-minute cooldowns. Tagged: goal 1.
- **2026-09-16 23:00 EDT: Rohan's reframing question and Astra round 9/9b (thread 01a0aace, brief `.claude/analyses/astra-brief-2026-09-16-methodology.md`, Astra's notes `astra-methodology-2026-09-16.md`).** Rohan asked whether the paper can be "the best methodology for a small-budget world model" and whether the U-Net's parameter count (860M vs DiT 675M) explains its edge. Parameter count: PixArt (611.6M, the smallest row) ties the U-Net on seen PSNR/LPIPS (21.35/0.272 vs 21.36/0.270) and beats the DiT (675M) on LPIPS, so size does not order the rows; capacity is not causally isolated (no larger adaLN DiT exists in the SD 1.x latent space); add a paired episode-bootstrap CI for PixArt minus U-Net. Framing: Astra will not sign "best methodology" or "initialisation matters most" (init, architecture, conditioning and size vary together across rows); it signs "start from a pretrained package that does well in a short adaptation pilot, measure the return from additional updates, report EMA and persistence-aware rollout metrics". Claude's decision-ordered recipe wording is adopted with those edits; the sentence "spend the remaining budget on updates not architecture" is dropped (never compared at equal card-hours). Agreed for Thursday, pending Rohan: (1) EMA as the reported weights for every SD row including both DiT seeds, evaluated on seen, unseen, unseen2 and rollouts (about 3 card-hours; live numbers kept as sensitivity); (2) inference-time context-noise sweep on the U-Net, live and EMA, levels {0, 0.035, 0.105} at DDIM 50 on 64 development rollouts, selection by rollout LPIPS at 32/64 with motion and IDM guardrails, confirmation on the full reporting set (`rollout_eval.py --infer-noise` exists; bug to fix first: `fixed_noise` draws from the global RNG so context noise perturbs the DDIM start noise; use a dedicated torch.Generator); (3) optional one training run on GPU 3 from Thu noon: resume the U-Net's 90k recovery checkpoint at lr 5e-5 without fresh warmup (scheduler is constant after warmup, `train_wm.py:182`) to 180k with checkpoints at 120k/150k/180k, giving a budget curve 90k/180k (the 2.5k sample-matched point used warmup 250 and is plotted as a separate short-budget recipe); Astra's caveat: this measures diminishing returns for the U-Net, not whether the DiT would catch up. Dropped for this deadline: REPA, Matrix-Game history probabilities, batch redesign, new architectures. Tagged: goal 1.
- **Sep 16 Astra round 9 (independent proposal; not launched):** practical recipe plus shared-recipe pretrained-package study, not best methodology or identified pretraining cause. PixArt nearly matches U-Net seen quality with about 29% fewer parameters; this rebuts a simple size ordering but does not isolate capacity. Prioritize EMA and a U-Net live/EMA x inference-context-noise development sweep (DDIM50 fixed, separate context/target RNG streams), then confirm on reporting rollouts; keep tuned results separate from the common recipe. DDIM step sweep third; defer REPA/new architectures/automatic 180k continuation. Equal 80k target presentations are sample-matched, not automatically compute-matched. Existing corrected-label 5k DiT context sweep is preliminary evidence, despite the brief saying no context ablation. Detailed proposal and sources: `.claude/analyses/astra-methodology-2026-09-16.md`.

- **2026-09-17 00:20 EDT: UniDiffuser row excursion at 8.5k, restored from 5k with the learning rate halved (recipe deviation).** Train loss 0.240 to 0.247 through 8.25k, then in the 8.25k to 8.5k window grad_norm mean 5.59, grad_norm_max 227.7, clip fraction 0.10, loss 0.354; afterwards gradients back to 0.2 with no clipping but loss stuck at 0.31 to 0.33 and held-out val 0.2426 (8k) to 0.3198 (9k, excursion flag; low-noise quartile 0.331 to 0.543). The mid-block attention query weight's update ratio fell from 2.3e-4 to 2.1e-11 after the spike, so that attention layer stopped updating. Astra (round 8) agreed on option A over letting it run, reseeding, or tighter clipping: restore the pre-spike step-5,000 recovery checkpoint (val 0.2519) with optimizer, EMA and RNG, and continue at lr 2.5e-5 with the same seed and clip 1.0. train_wm.py now honours a learning-rate override on resume (the restored optimizer state carries lr and initial_lr, both overridden, logged as "recipe deviation"); launch_unidiffuser.sh takes LR=. Relaunched 00:15 EDT: step 5,050 at lr 2.5e-5, 0.77 steps/s, so 90k lands about Friday 07:00 EDT. The degraded segment's log is kept as log_before_lr_halving.jsonl. Paper sentence (Astra's): "UniDiffuser exhibited a gradient excursion near update 8,500; we restored its update-5,000 recovery checkpoint and reduced the learning rate from 5e-5 to 2.5e-5 for subsequent training, retaining the original seed and other hyperparameters." Watch the next two validations (6k, 7k) for recurrence.
- **Sep 16 Astra round 8 (recommendation, not executed):** UniDiffuser 034 has a gradient spike near 8.5k, persistent validation degradation (0.2426 at 8k to 0.3198 at 9k), and a mid-attention to_q update-ratio collapse. Recommend option A: restore the full pre-excursion 5k recovery checkpoint (model/optimizer/scheduler and available RNG state), explicitly override and verify effective lr 2.5e-5 after resume, retain clip 1.0 and seed. Lower lr applies from resumed step 5k, not 8.5k; report rollback and failed compute. Check validation and attention updates at the next two scheduled validations; a tiny update ratio does not prove permanent layer death or identify QK saturation. Remaining 85k updates at 0.79/s is 29.9 h before overhead/evaluation. No remote action or launch in this review.

- **2026-09-16 21:40 EDT: compute-matched rows scored (80k samples = 2,500 updates of batch 32, warmup 250; seen corpus, 2,048 windows, live weights, raw pixels):** ImageNet DiT 19.20 dB / LPIPS 0.471 (below copy-last 19.41), U-Net 19.67 / 0.411, PixArt 19.89 / 0.412; held-out v-loss 0.2605 / 0.2438 / 0.2462. At the video row's sample budget the ImageNet DiT has not yet beaten persistence while the two web-scale-pretrained rows are 0.3 to 0.5 dB above it; this is the reference the video row (80k samples) is compared against. Live runs healthy: video row 2,750 (val 0.1845 at 2.5k), UniDiffuser 4,550 (val 0.2544 at 4k, falling).
- **2026-09-16 19:40 EDT: zero-shot transfer on 13 more unseen maps, compute-matched runs done, UniDiffuser row training.** unseen2 (13 curated Freedoom Phase 2 campaign maps 18 to 32 minus 21 and 27, 130 seeded episodes, 2,048 windows, live weights, raw pixels): DiT-s0 21.43 dB / LPIPS 0.413, DiT-s1 21.59 / 0.404, U-Net 21.15 / 0.412, PixArt 21.29 / 0.411; copy-last 20.92, VAE ceiling 28.89 / 0.058 (these maps are calmer than the deathmatch arenas: persistence scores 1.5 dB higher than on the seen corpus, and every model's margin over copy-last is small: DiT-s0 +0.51, DiT-s1 +0.67, U-Net +0.23, PixArt +0.37). Per map, DiT-s1 beats the U-Net on PSNR on 12 of 13 maps and DiT-s0 on 10 of 13; LPIPS is at parity (per-map wins mixed). Reading: the two-map hint holds on 13 further maps; the U-Net's seen-map LPIPS advantage (0.270 vs 0.311) disappears entirely off the training maps, and the transformers keep a slightly larger margin over persistence. Caveats for the paper: different design family (campaign levels, curated), decoder fine-tuned on seen-map frames (ceiling 28.89 here vs 28.61 seen), margins over persistence under 0.7 dB for every row, episode-level CIs still to compute. Compute-matched 2.5k-update rows (80k samples, warmup 250), final held-out v-loss: DiT 0.2605, U-Net 0.2438, PixArt 0.2462; their seen-map pixel evaluation is launched on GPU 1 (`eval_cm.sh`, marker CM_EVAL_DONE in logs/eval_cm.log) for the sample-matched comparison with the video row. UniDiffuser row 034 launched on GPU 2 after its fit check (batch 32 fits: 33.2 GB, 0.74 steps/s; 90k updates about 32 h, so Thursday night); at step 1,700 loss 0.275. Video row at 2,150, val 0.1864 at 2k.
- **2026-09-16 17:10 EDT: unseen2 corpus complete (130 episodes on 13 curated maps, encoded, split_unseen2.json with all 130 in val); zero-shot scoring launched** (`eval_unseen2.sh` on GPU 1: eval_tf.py, 2,048 windows, 50 steps, live best.pt, for 030, 031, 033, 032 in that order; outputs results_spiderman/<run>/eval_tf_unseen2, marker UNSEEN2_EVAL_DONE in logs/eval_unseen2.log; about 6 h). Compute-matched runs 060 and 061 finished, 062 running; UniDiffuser chain waits behind them on GPU 2. Video row at update 1750, val 0.1896 at 1500.
- **2026-09-16 16:00 EDT: release kit written (`release/`: dataset and model cards, manifest with 59 entries, resumable upload.py with md5 and git-sha manifests, runbook; WEIGHTS.md regenerated with placeholders; 46 VERIFY markers).** Two facts it surfaced that change wording everywhere: (1) the 850-episode training corpus is not tic-for-tic reproducible, since seeding entered record_arnold.py on Sep 10 after the corpus was recorded on Sep 8 to 9; only the three evaluation corpora carry the doomdit-episode-v1 seed record, so "reproducible" applies to the evaluation corpora and the paper must say so; (2) the training maps are Arnold's full_deathmatch.wad arenas MAP01 to 17 rendered with Freedoom assets, not Freedoom Phase 2's own MAP01 to 17, and the unseen2 maps are Freedoom Phase 2 campaign levels 18 to 32 loaded through the IWAD, curated. Also: hf_upload.py shards drop per-episode parquet metadata, so seeds ship in worker_*.jsonl under meta/provenance; the `kills` field is always 0 (monster count), use `frags`. Needs Rohan: Hugging Face namespace and repo names, whether to ship the curated WAD or only its manifest and build script, the legacy April checkpoint, final title.
- **2026-09-16 15:40 EDT: unseen2 corpus recording (13 curated Freedoom maps, 130 seeded episodes, corpus id doomdit-eval-unseen2-curated-v1).** Root causes measured, not inferred: Arnold's full_deathmatch.wad holds only MAP01 to 17, so maps 18 to 32 load from the single-player campaign IWAD, which spawns 6 to 325 monsters per map (training maps: zero) and whose health pickups push health above 109, overflowing Arnold's health embedding (11 buckets), while paired shotgun and super shotgun break its weapon-slot assert. Curated PWAD (`tmp/unseen2/full_deathmatch_unseen2.wad`, manifest alongside, to be moved to a durable path): 2,430 THINGS removed across 13 maps (2,058 monsters of 22 types, health bonus 258, super shotgun 56, soulsphere 23, megasphere 18, backpack 17, chainsaw 12); all base-map lumps byte-identical. Maps 21 and 27 dropped (agent pinned in a 32 to 96 unit box, near-static frames). Smoke 30/30 at full episode length on two seeds including maps 16 and 17 as regression. Recording 12 workers, zero failures, health range 100 to 100 confirmed; SD encode with --align-decisions and the training canonical controls chained on GPU 1; split_unseen2.json written after. Paper caveats: the new maps are single-player level designs, not deathmatch arenas, and the corpus definition is "deathmatch-curated" with the manifest released.
- **2026-09-16 15:10 EDT: red-team review (`.claude/analyses/red-team-2026-09-16.md`) and two fixes.** Its strongest points: (1) "exposure predicts quality" is not monotone in the paper's own numbers (PixArt at 25M images beats the U-Net at about 2B on held-out loss; under presentations rather than unique images the ImageNet DiT saw about 1.8B), so the claim must be about the pretraining source (class-conditional ImageNet vs text-conditioned web-scale) rather than a quantity law; (2) the abstract's "weakest start on every metric" is false (the DiT wins rollout PSNR@64, unseen PSNR, degradation); (3) the compute-matched 2.5k-update runs used warmup 2000 and would have measured warmup, fixed now to warmup 250 and relaunched; (4) the VAE decoder was fine-tuned on seen-map training frames and its ceiling drops 2.25 dB on unseen maps (28.61 to 26.36), so transfer must be reported next to the ceiling's own degradation; (5) Freedoom MAP18 to 32 are single-player levels, a different design family from the 17 deathmatch arenas, which the transfer section must state; (6) the persistence finding wants an inference-time context-noise sweep (bucket 0 was never tuned; 0.6 card-days) before it is called a mechanism. Recommended spend of the remaining card-days: LR forks, context-noise sweep, UniDiffuser to 90k, unseen-map rollouts, a second U-Net seed. Its rewritten abstract is in the file. Framing adjustment adopted: headline becomes "the pretraining source, not the architecture, decides small-budget Doom world-model quality; the standard class-conditional ImageNet DiT is the weakest start on perceptual quality, and the same skeleton with text-image pretraining matches the SD U-Net", with the transfer and persistence findings carrying their caveats.
- **2026-09-16 14:30 EDT: PixArt evaluated; motion audit answers the stability question.** PixArt-alpha (033, identical recipe), live weights unless marked: seen 21.35 dB / LPIPS 0.272 (EMA 21.60 / 0.255), unseen 19.39 / 0.434 (best unseen LPIPS of any row; per map 16: 20.64 / 0.424, 17: 19.98 / 0.414), rollouts psnr@8/16/32/64 19.47 / 18.66 / 18.03 / 17.18, lpips@8/64 0.442 / 0.584, IDM top-1 0.492, FVD16/32 211 / 472, held-out v-loss 0.2031 (best 0.2030). Reading: the same DiT-XL/2 skeleton with 25M image-text pretraining matches the LAION U-Net on seen maps (21.35 vs 21.36 dB, 0.272 vs 0.270 LPIPS) and beats every row on unseen-map LPIPS, so the ImageNet DiT's deficit is its warm start, not its architecture; this is the exposure finding in its cleanest form. Motion audit (`motion_audit.py`, 256x64 rollouts, frame-difference energy and Farneback flow of predictions vs real continuations, episode bootstrap): the DiTs under-move. Flow-magnitude ratio prediction/real at h8/16/32/64: DiT-s0 0.75 / 0.71 / 0.68 / 0.58, DiT-s1 0.78 / 0.74 / 0.75 / 0.73, U-Net 0.85 / 0.81 / 0.78 / 0.75 (all CIs below 1; DiT-s0's 64-step CI [0.53, 0.62] is well below the U-Net's [0.69, 0.81]); frame-difference ratio DiT-s0 0.83 / 0.82 / 0.87 / 0.75, DiT-s1 0.77 / 0.79 / 0.81 / 0.76, U-Net 0.89 / 0.91 / 0.97 / 0.95; at h64 59% of DiT-s0 rollouts (51% seed 1, 35% U-Net) sit closer to the seed frame than the real frame does. Verdict: the DiT's horizon-64 PSNR lead is largely reduced motion, not more accurate dynamics; the paper uses the "moves less" wording (Version B) and drops any stability claim. Compute-matched runs: 060-dit-cm2500 at 1100 of 2500 on GPU 2 (1.4 steps/s); 061 and 062 follow. Video row at 1350 (val 0.1985 at 500, 0.1928 at 1000). Agents killed by the noon rate limit were resumed: UniDiffuser implementer, figures and tables, red team, unseen2 corpus (curation now also strips monsters to match maps 1 to 17, whose curated versions spawn none), release preparation, and the four LEGO readers.
- **2026-09-16 11:55 EDT: end of the unlimited-usage window; what is in flight.** Doom: UniDiffuser v1 row implementer (Fable, PixArt gates) for a fourth exposure point in the SD latent space; paper skeleton under paper/ with the agreed framing and a "lessons for small-budget world models" section; figure and table generators (paper/make_figures.py, make_tables.py, sync_results.sh); red-team review of every claim (.claude/analyses/red-team-2026-09-16.md); release preparation under release/ (dataset and model cards, upload script with md5 manifests); compute-matched 2.5k-update runs of the image rows chained behind PixArt's evaluation on GPU 2 (tmux cm-chain); motion audit on GPU 1; the 13-map curated unseen corpus (maps 18 to 32 minus 21 and 27, super shotgun, chainsaw and backpack THINGS stripped because Arnold's inventory assertions crash on stock maps; manifest recorded) once fail2ban lifts for the recording agent. Spiderman's SSH refusal this morning was fail2ban on our IP from a scripted login with a mangled password, not a reboot: video row alive at step 350 (0.16 updates/s), PixArt evaluation past teacher-forced seen and unseen. New direction from Rohan and Changliu (fall goal 3): fine-tune a video model on demonstrations of LEGO assembly (human hands from phone video, or the lab's robot arm with proprioceptive actions; undecided) for text or action-conditioned assembly video generation as synthetic training data; four Fable researchers are writing evidence cards to .claude/analyses/lit/lego-*.md (generated video as robot data with DreamGen/GR00T-Dreams as the precedent; post-training recipes and memory on 4x A6000; action and brick-state extraction from videos, human vs robot executor; datasets and evaluation protocols). My read: the video model is the easy half; the contribution is a labeller and an evaluation that grade generated assembly videos against the stud lattice, and capture (two calibrated cameras, known build sequences) decides tractability. Not a September project; the Doom paper owns the compute until Sep 30.
- **2026-09-16 Astra round 7 (goal1; recommendation):** independently ranked benchmark plus persistence diagnosis, pretrained-package comparison, then broader unseen-map transfer. Selected motion/action-stratified analysis of existing rollouts as the highest-value additional experiment; no launch. Direct primary-source checks confirm PlayGen already uses Doom DiT and ActAcc/ProbDiff, GameNGen reports 900M generated frames/700k updates (older 70M notes conflict), and MultiGen already covers memory/editable layouts/multiplayer. Cross-paper LPIPS equality is not performance parity; shared recipes do not isolate architecture/pretraining or equal compute; SkyReels has 36x fewer target presentations. Recorded design, limits, claim and title in `.claude/analyses/astra-contribution-2026-09-16.md`.

- **2026-09-16 11:20 EDT: video row hung at its first validation, relaunched at 1.9x throughput.** Run 050 stalled at update 500: a validation data-loader worker died with ENOSPC under /tmp (root filesystem full) and the main process waited on it forever. Cause of the recurrence: tmux starts commands in the tmux server's environment, so the launcher's exported TMPDIR never reached the training process; all launchers now put TMPDIR inside the tmux command string. Throughput probes on the free card (L=8, batch 8): 8 windows in one pass with gradient checkpointing 0.167 updates/s at 26.7 GB; 2 or 4 windows without checkpointing run out of memory on 48 GB (full attention over 2,880 tokens across 30 layers). Relaunched from scratch (no checkpoint existed) with per-gpu-batch 8, no accumulation, checkpointing on: 10k updates in about 17 h, so Wed morning. Plan: move to GPUs 2+3 (4 windows each, one pass) when PixArt's evaluation frees GPU 2 tonight. Rohan's point stands: the fit-check configuration (microbatch 1, accumulation 8) was never retuned for a free card.
- **2026-09-16 10:00 EDT: rollout audit (Astra's round-6 checks) and a PixArt crash.** `rollout_audit.py` (Opus, GPU 1) on the three finished runs: (a) all three scored identical trajectory ids, seeds, actions and teacher-forced window sets (md5-identical). (b) Blur control: Gaussian blur never raises PSNR for any model at horizon 8 or 64 (U-Net h64 16.02 at sigma 0, 16.04 at 0.5, 15.78 at 4; DiT-s0 18.30 to 17.78), so the DiTs' horizon-64 PSNR lead (2.28 dB over seed 0, 1.66 over seed 1, episode-bootstrap CIs exclude zero) is not smoothing. But copy-seed (the seed frame repeated) scores 17.86 dB / LPIPS 0.510 at h64, so every model sits at or below persistence perceptually there (LPIPS 0.55 to 0.57); the DiTs stay near persistence while the U-Net drifts further from it. "Holds PSNR longer" may mean "moves less"; a motion-magnitude check is the next test. (c) Late drop 32 to 64: medians -0.03 / -0.54 / -1.20 dB (DiT-s0 / DiT-s1 / U-Net), 25 / 36 / 43% of rollouts drop more than 2 dB, trimming the worst 10% shifts every mean by 0.6 to 0.7 dB: heavy tails for all, not a few bad clips. (d) Episode-level bootstrap 95% CIs: seen PSNR DiT-s0 21.06 [20.74, 21.38] vs U-Net 21.36 [21.01, 21.73] overlap; seen LPIPS 0.311 [0.301, 0.320] vs 0.270 [0.261, 0.280] separate; unseen PSNR DiT-s0 19.52 [19.32, 19.73], DiT-s1 19.59 [19.40, 19.80], U-Net 19.14 [18.90, 19.39] separate but on only 2 held-out maps; unseen LPIPS 0.450 / 0.442 / 0.446 mixed; rollout IDM top-1 CIs all overlap (0.49 / 0.48 / 0.51). Seen-to-unseen degradation: DiT 1.54 dB, U-Net 2.23 dB. PixArt hung at step 89,000 when a DataLoader worker hit ENOSPC creating its multiprocessing files under /tmp (root filesystem full, other users); killed and resumed from the 85k recovery checkpoint with TMPDIR routed to the data drive, which every Spiderman launcher and waiter now exports. Orchestrator stopped at Rohan's request; the session crons (dashboard tick, hourly steward) are back. Video row 050 at 0.077 updates/s alone on GPU 3, 10k updates due about Thu evening.
- **2026-09-16 10:30 EDT: Astra round 6 on the paired results.** Headline as agreed does not survive as stated; its replacement: "Under a shared adaptation recipe, the SD-initialized U-Net achieves lower validation loss, lower seen-map perceptual error, and better rollout FVD than the ImageNet-initialized DiT. Across two DiT seeds, however, the transformer retains higher long-horizon PSNR and achieves higher unseen-map PSNR, revealing a metric-dependent tradeoff rather than a consistent backbone advantage." Two qualifiers to keep verbatim: DiT's long-rollout pixel fidelity does not establish more accurate dynamics or perceptual realism; DiT improves unseen-map PSNR without a consistent unseen-map LPIPS improvement. Horizon-64 PSNR must not be called stability until a blur-control sweep (Gaussian sigma 0 to 4 px on predictions, PSNR and LPIPS recomputed, copy-seed as reference) shows the U-Net cannot close the 1.65 to 2.27 dB gap by smoothing. Table split: both DiT seeds as separate main-table rows, six columns (seen TF PSNR/LPIPS, unseen TF, rollout PSNR@64/LPIPS@64, FVD16/32, IDM), one main figure with PSNR and LPIPS horizon curves; all-live or all-EMA, never mixed (U-Net EMA gains 0.31 dB / 0.020 LPIPS, DiT EMA barely moves); appendix carries the live/EMA grid, per-horizon values with uncertainty, VAE ceiling and copy-last, and the old grid-label checkpoints as historical references. Red flags being worked: episode-level bootstrap CIs (windows within an episode are correlated), an audit of the 32-to-64 late drop (seed 1 0.97 dB, U-Net 1.70 dB) for a few-bad-clips effect, and a check that the three runs share trajectory ids and actions. An Opus audit (`rollout_audit.py`) is running on GPU 1 for all three. Full text in `.claude/analyses/astra-video-prior-2026-09-14.md`, Round 6.
- **2026-09-16 09:40 EDT: the corrected pair and the second DiT seed are done and evaluated (Spiderman ran unattended from Mon 01:30).** All three reached 90k updates without an excursion: final held-out v-loss DiT seed 0 0.2139, DiT seed 1 0.2139 (identical to four decimals), U-Net 0.2043. Fresh seeded corpus, 2,048 windows, 50 DDIM steps, raw pixels, live weights unless marked: DiT-s0 seen 21.06 dB / LPIPS 0.311 (EMA 21.06 / 0.306), unseen 19.52 / 0.450; DiT-s1 seen 21.21 / 0.307 (EMA 21.10 / 0.304), unseen 19.59 / 0.442; U-Net seen 21.36 / 0.270 (EMA 21.67 / 0.250), unseen 19.14 / 0.446; copy-last 19.41 seen / 18.50 unseen; VAE ceiling 28.61 / 0.051 seen, 26.36 / 0.070 unseen. Rollouts 256x64 on seen maps, psnr@8/16/32/64 and lpips@8/64, IDM top-1 (real clips 0.847, majority 0.232), FVD16/32: DiT-s0 19.66 / 18.93 / 18.51 / 18.30, 0.453 / 0.553, 0.492, 232 / 481; DiT-s1 19.86 / 19.09 / 18.65 / 17.68, 0.436 / 0.550, 0.476, 198 / 407; U-Net 19.55 / 18.66 / 17.73 / 16.03, 0.421 / 0.566, 0.509, 184 / 356. Reading: the U-Net wins the perceptual and short-horizon metrics (LPIPS by 0.04 to 0.06, FVD16 184 vs 198 to 232) and the held-out loss; the DiTs hold PSNR better at horizon 64 (17.7 to 18.3 vs 16.0) and are slightly ahead on unseen-map PSNR (19.5 to 19.6 vs 19.1), with LPIPS on unseen maps a wash. The two DiT seeds agree to within 0.15 dB and 0.004 LPIPS on seen-map teacher forcing (live weights); they differ more under rollout (0.62 dB at horizon 64, 74 FVD32) and by 0.008 on unseen LPIPS, so seed variance is small for the teacher-forced ordering and material for the long-horizon numbers (Astra's correction, round 6). The checkpoints are distinct files (md5 c05d06b3 vs b2678f37) with unrounded final losses 0.213941 and 0.213875. Against the old grid-label checkpoints (Sep 15 entry) the corrected DiT gains 0.9 dB / 0.047 LPIPS on seen maps at matched 75k-vs-90k. PixArt (033) is at 89k of 90k and evaluates next on GPU 2. Video row: the orchestrator launched 050-skyreels-l8-flow on GPU 3 at 04:24 UTC after the encode chain finished (220 pilot episodes, 399k decisions, plus the 80-episode evaluation corpus), but it died twice at launch: offline mode blocks diffusers' sharded-checkpoint index lookup (launcher now runs with the Hub reachable), and the Wan loader compared integer split ids against `ep_00000` file names (fixed, `episode_name`). Relaunched 09:35 EDT. Also fixed: the server repo's `git pull` had been failing since Sep 15 because hand-copied files shadowed the committed ones (removed; repo now tracks main). Spiderman /home is at 26 GB free and root at 1 GB; both are other users' usage and neither is on our write path.
- **2026-09-15 01:30 EDT: SkyReels fit check, L=8 flow, on GPU 2 next to the DiT:** peak 24.65 GB, 0.0583 updates/s at batch 8 (2,880 tokens per window), so 10k updates is about 48 h shared and roughly 30 h on a free card, inside the 36 h budget. This is the configuration the orchestrator will launch (050-skyreels-l8-flow) once the encode chain finishes; the evaluation-corpus encode was at 58 of 60 seen episodes at that time. The L=16 vp-v check follows for the record.
- **2026-09-15 01:15 EDT: SkyReels fit check, L=16 flow, on GPU 2 next to the DiT:** peak 25.12 GB, 0.0316 updates/s at batch 8 (20 updates, loss 3.13 in warmup, no non-finite, clip fraction 1.0 at lr 8e-7). Standalone will be faster, but 10k updates at this rate is 46 to 88 h, over the 36 h budget, so the orchestrator now prefers L=8 for the unattended launch and L=16 only if L=8 fails the memory gate. Two launch bugs found and fixed first: the trainer never placed the model on the device before building its optimizer (trained on the CPU; fixed in train_video.py), and fit_video.sh clobbered its GPU argument inside its loop (CUDA invisible; fixed).
- **2026-09-14 23:30 EDT: unattended mode until Wed Sep 17, 12:00 EST (Claude usage exhausted).** Spiderman runs itself from `orchestrator.sh` (tmux `orchestrator`, every 10 min, log `logs/orchestrator.log`; every server script is archived in `scripts/spiderman/`): self-heals 030/031 (`resume_run.sh`), PixArt, seed 1 and the video row (their launch scripts resume from recovery checkpoints); keeps the post-run waiters alive and starts waiters for PixArt and seed 1 when their logs appear; keeps the Wan-VAE encode chain alive (eval seen, eval unseen, then the 220-episode pilot list) and starts a second encoder on GPU 2 over the reversed pilot list once the fit checks finish; launches the SkyReels row (`launch_video.sh <gpu> <L> <objective>`, run 050-skyreels-l<L>-<objective>: L=16 or 8, flow objective, batch 8, lr 2e-5, warmup 500, 10k updates, cached null prompt) only when the encode chain is done, a fit check finished under 40 GB (throughput logged, judged Wednesday, since the fit checks share GPU 2 with the DiT), and a card has 30 GB free (GPU 3 preferred, then 2). Seed 1 now runs with gradient checkpointing so it can share a card with the video row. Chain unchanged: DiT evaluation on GPU 2 then PixArt; U-Net evaluation on GPU 3 then seed 1. Measured tonight: Wan-VAE encode 29 to 38 tic frames/s on the crowded spare card (7 to 9 decisions/s); SkyReels trainer CPU gates all pass (exact parity with the pretrained model, gradients on all 830 tensors, strict round trip, 1,435,571,776 params of which 46,080 new); `eval_video.py` reproduces the eval_tf and rollout_eval schemas exactly, a perfect predictor hits the VAE ceiling, and the Wan decoder needs the full context window (receptive field over 20 latent frames). IDM caveat: the video row's rollouts round-trip through the SD encoder to reach the IDM, so its idm_top1 is read against its own idm_real and is not directly comparable to the SD rows. For Wednesday: rollout sampler steps (50 vs 20) for the video evaluation, ctx-stabilize 0 vs the trained 20 for reporting, the 36-hour budget check, and Rohan's formal go on the row.
- **2026-09-15 01:40 EDT: side queue complete: grid-label ablation and K=2 IDM.** Old grid-label checkpoints (010 DiT at 75k, 011 U-Net at 20k, Superman) scored on the fresh seeded corpus with the current suite (2,048 windows, 50 DDIM steps, live weights). Teacher-forced raw PSNR / LPIPS: DiT-75k seen 20.18 / 0.358 (copy-last 19.41, VAE 28.61 / 0.051), unseen 18.79 / 0.472 (copy-last 18.49, VAE 26.36); U-Net-20k seen 19.82 / 0.372, unseen 18.71 / 0.479. Rollouts 256x64 on seen: DiT-75k psnr@8/16/32/64 18.06 / 17.51 / 16.90 / 16.07, lpips 0.513 / 0.545 / 0.577 / 0.605, IDM top-1 0.344 (real 0.847, majority 0.233), FVD16 329, FVD32 690; U-Net-20k psnr 17.54 / 16.88 / 16.57 / 15.90, lpips 0.500 / 0.534 / 0.550 / 0.562, IDM top-1 0.326, FVD16 265, FVD32 531. These are the label-noise reference rows; the corrected pair's evaluations on Monday give the matched comparison. K=2 IDM (`idm_aligned_k2`): top-1 70.1%, macro recall 40.6%, movement 72.7%, majority 22.7%, versus the K=8 judge's 82.9 / 54.8 / 84.9: most of the action is readable from a single transition, and 8 frames add about 13 points, so the judge is not a long-context artefact.
- **2026-09-15 00:30 EDT: video-pretrained row, Astra round 5 and 5b (decision pending Rohan).** Rohan asked about Cosmos. Independent proposals: mine a Wan-lineage 1.3B (SkyReels-V2 DF) with a Wan-VAE re-encode; Astra's a gated Open-Sora 1.1 row keeping our VAE. After round 5b Astra changed its pick to SkyReels-V2 DF 1.3B (`Skywork/SkyReels-V2-DF-1.3B-540P-Diffusers`) with two corrections adopted: latest-action-only conditioning on the target frame's tokens (same action information as the other rows) and the second DiT seed yields GPU 3 only to a gate-passed row. Agreed design, gates, budget (10k updates or 36 card-hours, 36 eval card-hours), stop rule (Sep 22) and framing (exploratory system comparison, each family's recipe, same data and evaluation) are in `.claude/analyses/astra-video-prior-2026-09-14.md` and the literature artifact. Cosmos: Predict1 7B out of budget; Predict2 2B's action recipe does not transfer as-is and single-card training is unverified; Matrix-Game 2.0 ships inference only. Astra retracted part of round 4: the pair does not establish pretraining exposure as the cause of the gap; wording is now "consistent with". Corrected the Cosmos tokenizer card: CV8x8x8 is 8x temporal. Started tonight without server compute: Opus implementers for the Wan-VAE chain-aligned encoder (`encode_wan.py`, `wan_data.py`) and the SkyReels trainer (`video_wm.py`, `train_video.py`), and a reader verifying the option table (`lit/video-warm-starts.md`). Go/no-go and the seed reordering are Rohan's call in the morning.
- **2026-09-14 round5b: revised video-row proposal (goal1):** Open-Sora1.1 remains first, SkyReels-V2 DF1.3B becomes runner-up; adopt shared VP-v/no variance and try L16 only if full throughput/evaluation gate fits (else L8). Gate-passed video now outranks unstarted seed1 for the expanded research goal: proposed GPU3 after U-Net, seed behind PixArt GPU2; no gate means existing seed starts, no idle reservation. No queue changes made. Native Matrix-Game2 tree has inference but no verified world-model training path. Updated `.claude/analyses/astra-video-row-2026-09-14.md` records forecasts as subjective, token-cost arithmetic, objective correction and conditional priority.

- **2026-09-14 night: Astra reviewed a video-pretrained row (goal1; proposal):** preserve PixArt and seed1; prototype Open-Sora 1.1 stage3 as a separate video-pretrained system row (8 decision-frame context, one target, native epsilon DDPM, same SD VAE), then use a released card only after fit and full-evaluation timing gates. Up to 36 training + 36 evaluation card-hours proposed, not allocated. Wan1.3B is runner-up; Cosmos2.5 is the Cosmos-specific alternative. The observed gap does not establish pretraining causality. Correct Cosmos1 CV8x8x8 temporal factor to 8; Cosmos2 video tokenizer and Cosmos2.5 Wan-family setup use factor4. Detailed verified sources, estimates, action-alignment and Sep22 stop gate: `.claude/analyses/astra-video-row-2026-09-14.md`. No remote/queue/code changes.

- **2026-09-14 22:30 EDT: PixArt-alpha third row built, gated, fit-checked, and chained; literature evidence pass delivered.** `backbones.py` gains `PixArtWorldModel` (`--backbone pixart`, warm start `PixArt-alpha/PixArt-XL-2-512x512`, 611.6M params, 753,664 new): patch projection inflated to 132 channels with the pretrained kernel on the target slice, sin-cos table regenerated for the 16x20 grid at base_size 32 and interpolation scale 1 (non-persistent buffer), action (with null row) and noise bucket as two learned 4096-wide tokens through the pretrained caption projection, timestep on PixArt's own adaLN-single path, `use_additional_conditions=False`, output `sample[:, :4]` (the epsilon half of the learn-sigma head, retrained as velocity). Gates from Astra all pass (Opus implementer, CPU): inflation parity exact (max diff 0.0 against the pretrained model on the same tokens), every new parameter receives finite nonzero gradient and moves after one step, fixed-batch overfit 1.738 to 0.485 in 30 steps monotone, save/reload round trip strict with 605/605 keys. Two diffusers-0.31 compatibility fixes were needed on Spiderman (no `output_type` in `get_2d_sincos_pos_embed`; `added_cond_kwargs` must be a dict). Spiderman fit check on GPU 2 sharing with the DiT: 26.41 GB peak, 0.68 steps/s at batch 32, no checkpointing. Evaluators take `--pixart-path` via `backbone_source()` so PixArt is never built from the SD repo. Hand-off chain is now `after_run2.sh`: DiT evaluation on GPU 2 then `launch_pixart.sh 2` (033-pixart-l32-aligned, identical recipe: L=32, lr 5e-5, warmup 2000, batch 32, dropout 0, 90k, seed 0); U-Net evaluation on GPU 3 then `launch_seed1.sh 3` (032). Waiters restarted; steward cron updated; dashboard tracks 033. Evidence pass: four Opus readers extracted a fixed schema from 25 sources into `.claude/analyses/lit/` (doom-world-models, dit-world-models-a/b, backbones-and-warm-starts) and the literature artifact https://claude.ai/code/artifact/fbcfe1db-6603-4943-ba07-c15101aa8371. Findings that bear on the claim: Nano World Models (SD KL-f8, 40M to 830M, 8 GPUs) finds eps worst and v about equal to x prediction, and prices action injection (additive 0, FiLM 14M, cross-attn 28M, adaLN 42M); every strong transformer world model is a fine-tune of a video foundation model (Matrix-Game 2.0 from SkyReels 1.3B, 120k steps at batch 256 on 1200 h; Matrix-Game 1.0 from HunyuanVideo 17B; The Matrix from Open-Sora 2.3B; Yume from SkyReels 14B / Wan2.2 5B; Hunyuan-GameCraft from HunyuanVideo); no adaLN DiT larger than XL/2 exists in the SD 1.x latent space (PixArt-Sigma, Open-Sora 1.2+, Hunyuan-DiT, Lumina use SDXL or 3D VAEs); SD 1.4's edge is about 1.17M steps at batch 2048 of LAION in our exact latent space versus PixArt-alpha's 25M images. Cheap follow-ups the cards support: REPA alignment loss, Oasis-style context stabilisation at sampling, Matrix-Game history-noise probabilities, DIAMOND's EDM-vs-DDPM check. Caveats: Cosmos evaluation tables did not load; Genie 3 discloses nothing; two readers discarded fabricated WebFetch summaries and re-derived numbers from raw HTML. Spiderman disk: root filesystem at 43 MB free (not ours; no sudo), /home at 63 GB free with our `~/.cache/wandb` 23 GB and `~/.cache/pip` 7.4 GB deletable on Rohan's word; /sata2 5.8 TB free.
- **2026-09-14 20:30 EDT: corrected-data context sweep complete (DiT, 5k updates, shared recipe):** L=2 0.2631, L=4 0.2561, L=8 0.2532, L=16 0.2565, L=32 0.2535 (main run's 5k). Not monotone above 8; L=8 is best at 5k and L=16 is worse than both neighbours, so the DiT does not exploit long context at this budget. The old noisy-label sweep read 0.2602 / 0.2583 / 0.2532 / 0.2525 / 0.2519. Label-noise ablation evaluation now running on GPU 1, K=2 IDM after it. EMA evaluation added to the post-run suite on the final recovery checkpoint; the evaluators now refuse `--use-ema` on a checkpoint without EMA weights instead of silently falling back to the live model. Tagged: goal 1.
- **2026-09-14 20:00 EDT: the DiT gap and the round-4 pairing.** At every matched step the U-Net leads the DiT by about 0.011 held-out v-loss (DiT 0.2211 at 50k, U-Net 0.2121 at 39k) and by 0.5 dB / 0.04 LPIPS on the fresh corpus; the DiT curve is flattening. Astra round 4 (new thread 01a0a160-b1ab-7f93-8bc4-86ddd74a0d07; brief `.claude/analyses/astra-brief-2026-09-14-dit-gap.md`, its literature review `astra-world-model-literature-2026-09-14.md`): no larger adaLN DiT exists in the SD 1.x latent space (Large-DiT 3B/7B, FiTv2-3B, DiT-MoE, UniDiffuser 1B exist but are different architectures or joint models; PixArt-Sigma, Hunyuan-DiT, Lumina use SDXL latents); from-scratch at 2.9M samples is not a rescue; the strong transformer world models (Matrix-Game 2.0 from SkyReels-V2 1.3B on Wan 2.1, 120k updates at batch 256 and lr 2e-5 on about 800 h; Hunyuan-GameCraft from HunyuanVideo 13B; The Matrix 2.3B video DiT; Yume from SkyReels/Wan; Cosmos own 7B/14B on 20M h) all start from video-pretrained transformers, model time with tokens and attention, and train on 12x to 120x our data, so their result is not reproducible in 4 card-days. Agreed plan: keep the pair to 90k; evaluate EMA and live; report matched-exposure and compute-matched; honest headline "under a shared adaptation recipe and limited compute, the ImageNet-initialised DiT-XL/2 shows no advantage over the SD-initialised U-Net"; add a third row, PixArt-alpha 512 (about 611M, image-text pretrained in exactly our latent space, cross-attention conditioning) warm-started under the identical recipe, on GPU 2 after the DiT's evaluation; run the ImageNet-DiT seed 1 on GPU 3 after the U-Net. Video-pretrained Open-Sora 1.1 is the follow-up experiment, not for this deadline. Rohan's process rule from today: Claude and Astra think and agree; Opus 5 agents do token-heavy reading and coding hand-offs (four literature agents and the PixArt wrapper implementation are running). Tagged: goal 1.
- **2026-09-14: Rohan agreed both follow-up runs and PixArt conditioning design (goal 1).** PixArt GPU 2 first, second ImageNet DiT seed GPU 3 after their respective original-run evaluations; no queue changes here. Two learned action/bucket tokens preserve the pretrained caption projection and timestep path. Installed diffusers inspection confirms PatchEmbed regenerates a 16x20 token grid for 32x40 latents while retaining native base_size=32; for sample_size=64 the installed default interpolation_scale is 1 (preserve the loaded value). Slice output[:, :4] after unpatchify: these are pretrained noise channels, subsequently optimized for velocity. Added parity, both-token/context gradients, real-data fit, EMA/evaluator roundtrip and deadline/resource gates.

- **2026-09-14: Astra verified world-model training literature and expanded the compatible-checkpoint inventory (goal 1; recommendations not adopted).** Read official Matrix-Game 2, GameGen-X, Hunyuan-GameCraft, The Matrix, Yume 1/1.5, Cosmos 1 diffusion papers, Oasis release/code and Genie 3 announcement. Video initialization and temporal token conditioning are prominent but not universal/disclosed for all; Genie 3's technical recipe is not verified. Added compatible Open-Sora 1.1, Latte variants, Large-DiT 3B/7B, FiT/FiTv2, MDT, SiT/REPA, U-ViT and DiT-MoE; Sigma/Lumina/Hunyuan image models use SDXL, modern video VAEs differ. Local meta counts: PixArt-alpha 512 has 610,856,096 parameters; Latte-1 has 1,057,246,880. For the explicit pretraining objection, now recommends a full Alpha512 third row instead of the second ImageNet DiT seed, subject to pilot/agreement; seed still best for narrow repeatability. Full sources, disclosed/undisclosed quantities and one-hour gates in `.claude/analyses/astra-world-model-literature-2026-09-14.md`. No server execution, queue edits or training changes.

- **2026-09-14: Astra round 4 reviewed the persistent corrected-run DiT gap (goal 1; recommendations, not adopted).** Recommends unchanged paired endpoints, paired EMA/live diagnostics, separate equal-exposure/equal-compute reporting and the queued full DiT seed as the single added run for the narrow claim. Public configs establish PixArt-alpha SD1.x compatibility but PixArt-Sigma 512 uses SDXL VAE / scale 0.13025; checked larger Hunyuan-DiT and Lumina-Next releases also use SDXL. UniDiffuser's released 1B U-ViT does use the SD autoencoder / 0.18215, but needs a joint-modality adaptation rather than a drop-in size change. Code and local meta-device checks confirm context projection is trainable and EMA parameter/key ordering aligns in current architectures. `best.pt` lacks EMA and evaluation silently falls back to live; use EMA-bearing snapshots/recovery checkpoints. Local optimizer test confirms resumed optimizer/scheduler override a new CLI LR. Full ranking and one-hour gates in `.claude/analyses/astra-review-2026-09-14-dit-gap.md`. No server commands, queue changes or training-code edits.

- **2026-09-14 15:40 EDT: gradient checkpointing off by default (Rohan).** Measured on one A6000: batch 16 to 32 gained only 8% samples/s, so the card is compute-bound and batch size is not a wall-clock lever; checkpointing was on by default from the 16 GB A4000 design and costs about a third of each step in recomputation. `train_wm.py` now takes `--grad-ckpt` to opt in; the running pair keeps it on (already loaded), the second DiT seed and any resumed run pick the new default up via `git pull`. Measured on GPU 2 while sharing it with the running DiT (so absolute rates are halved, the ratio holds): DiT batch 32 without checkpointing 0.70 steps/s at 24.2 GB versus 0.55 steps/s at 10.9 GB with it, a 28% speedup; U-Net batch 16 without checkpointing 20.5 GB. The second seed launches without checkpointing on GPU 2 (33 GB free). Resumed main runs and the remaining sweep runs (L=8, 16 on the crowded GPU 1) keep `--grad-ckpt` so their memory footprint does not change; the sweep queue hands over to `side_queue2.sh` after L=4 ends. Tagged: goal 1.
- **2026-09-14 10:20 EDT: status check and side work.** Corrected pair healthy: DiT 36k (held-out 0.2253 at 35k), U-Net 27.8k (0.2175 at 27k), no excursion flags, no clipping, adaLN update ratio 7e-5. Validation frames sampled from both checkpoints on the fresh corpus (64 windows, 50 steps, vs raw frames): DiT 31k 20.0 dB / 0.362 / HUD 28.3; U-Net 24k 20.5 dB / 0.324 / HUD 29.1; copy-last 19.2 dB; VAE ceiling 28.4 dB / 0.051. Side queue launched on Spiderman GPU 1 (`side_queue.sh`, tmux `side-queue`): context sweep L=2,4,8,16 at 5k steps on corrected data with the shared recipe (`040-dit-ctx{L}-aligned`), then the label-noise ablation (old 75k DiT and 20k U-Net `best.pt`, copied from Superman to `results_superman/*/final/`, scored on the fresh corpus with `eval_ckpt.sh`), then the K=2 IDM (`idm_aligned_k2`). Asset note for the paper, verified in Arnold's `game.py` and ViZDoom's package: every frame is rendered from Freedoom (`freedoom2.wad`), Arnold's own deathmatch maps, deathmatch against 8 bots, crosshair on; GameNGen's figures show id's assets and levels and the paper names neither WAD nor levels. Plan artifact status section rewritten with a progress table. Tagged: goal 1.
- **2026-09-14 00:40 EDT: standing authorization for autonomous continuation.** Rohan: stay updated, and if things go wrong, pair with Astra and reach the best literature-supported decision to keep advancing toward the goals. Encoded as: hourly steward in this session (resume dead runs from the newest recovery checkpoint via `resume_run.sh`; on a non-transient failure or an excursion flag, diagnose and pair with Astra on thread 01a08c22 before acting; pre-agreed excursion policy is to keep the run and its snapshots, evaluate EMA, and not change the learning rate without a controlled reason); post-run evaluation waiters; and the second DiT seed (`032-dit-l32-aligned-seed1`, identical recipe, seed 1) launching on GPU 2 as soon as the DiT's evaluation finishes (`launch_seed1.sh`, chained in `after_run.sh`). Dashboard collector tracks the seed run too. Human study still deferred. Tagged: goal 1.
- **2026-09-13 22:16 EDT: corrected paired runs launched on Spiderman.** Rohan's decisions: shared lr 5e-5 with 2,000-step warmup; action dropout 0 for both; fresh 80-episode evaluation corpus (60 seen-map, 20 unseen-map, seeded); human study deferred until results are in; placement Spiderman GPUs 2 (DiT) and 3 (U-Net). Runs `030-dit-l32-aligned` and `031-unet-l32-aligned` in `results_spiderman/`, logs `logs/train_{dit,unet}_aligned.log`, tmux `train-dit` and `train-unet`. Recipe: L=32, global batch 32 (DiT batch 32 x 1, U-Net 16 x 2), clip 1.0, EMA 0.9999, noise augmentation 0.7 x 10 buckets, verified-transition latents enforced, validation every 1,000 steps on 1,024 fixed windows, recovery checkpoints and snapshots every 5,000. Measured: DiT 1.26 steps/s (10.9 GB), U-Net 0.85 steps/s (15.6 GB; card shared with another user's small jobs), so DiT done about Sep 14 22:00 EDT and U-Net about Sep 15 04:00 EDT. First probes at step 100: DiT adaLN update ratios 1e-5 (lr still 2.5e-6 in warmup). Recorder reproducibility verified: one 60 s episode recorded twice under corpus `smoke-seed-v1` gave identical tics, actions, buttons, game variables, and frame hashes (1,980 tics). Fresh evaluation corpus launched (`record_eval_corpus.sh`, tmux `eval-corpus`): corpora `arnold-eval-seen-v1` (60 episodes, 4 per map 1 to 15) and `arnold-eval-unseen-v1` (20 episodes, 10 per map 16 and 17), then encoded on verified transitions with the main corpus's canonical table into `latents_arnold_eval/{seen,unseen}` with split files marking every episode as reporting-only; all 80 episodes recorded by 22:40 EDT and encoding. Step-1000 held-out v-loss: DiT 0.2988, U-Net 0.2733, no excursion flags. Live usage dashboard published (Lab Server Watch artifact, fed every 10 min by `tools/server_usage.py` through the artifact database; pointer in memory). Autonomous follow-on staged on Spiderman: `after_run.sh` waiters (tmux `after-dit`, `after-unet`) fire on each run's end event and run, on the freed card, teacher-forced evaluation against raw frames on the fresh seen and unseen corpora (2,048 windows each, LPIPS decoder), 256 rollouts x 64 steps on the seen corpus scored with the new IDM and its matched real reference, and FVD at 16 and 32 (I3D weights copied to Spiderman). Step-2000: DiT 0.2745, U-Net 0.2544. Tagged: goal 1.
- **2026-09-13 22:30 EDT: corrected data and IDM done; runs still unlaunched; decision brief published.** Aligned encode finished Sep 10 evening and verified exactly against `transition_stats.json` (850 episodes, 1,054,647 frames, 7,296 chains, every split episode present). New windowed IDM on the corrected latents: 82.9% top-1 over 29 actions, 84.9% movement, 54.8% macro recall, majority baseline 23.2% (old IDM on noisy labels 69.3%), so the label correction is measurable. No training has run since Sep 10 (three GPU-days unused). Spiderman GPUs 2 and 3 are at 0% utilisation with 23 and 38 GB free; Superman has six idle A4000s and 15 GB disk. Literature recipes checked for the learning-rate decision: DiT from scratch AdamW 1e-4 constant, no warmup, batch 256, EMA 0.9999 and "did not observe any loss spikes"; PixArt-alpha 2e-5 constant; GameNGen Adafactor 2e-5 constant, batch 128, clip 1.0, 700k steps, CFG on past frames only (p 0.1, w 1.5) and "didn't find guidance for the past actions condition to improve quality", human study 10 raters x 130 clips at 1.6 and 3.2 s (58 to 60% real chosen), held-out 2,048 trajectories from 5 levels, FVD on 512; DIAMOND AdamW 1e-4, batch 32, wd 1e-2. Decision brief with visuals published as the "DoomDiT Launch Decisions" artifact: (1) lr and warmup, recommend both 5e-5 with warmup 2,000; (2) action dropout, recommend 0; (3) fresh evaluation corpus of 80 seeded episodes, recommend yes; (4) human study, GameNGen-style 2AFC, 60 sources x 4 conditions x 24 raters, recommend if IRB allows; (5) placement, recommend Spiderman GPUs 2 and 3 tonight. Split counts confirmed: train 675, val 75, unseen_map 100. Tagged: goal 1.
- **2026-09-10 14:00 EDT: Astra round 3 paired on the relaunch; trainer hardened; evaluation plan set.** Round recorded in `.claude/analyses/astra-review-2026-09-09.md` (round 3 section) and the brief in `astra-brief-2026-09-10.md`. Adopted and pushed: exact recovery checkpoints (fp32 weights and EMA, optimizer, scheduler, best_val, accumulation and skip counters; per-rank reseeding on resume; data order reshuffled after a resume, stated), validation corruption seeded per window so it is identical across batch sizes and backbones, validation loss by timestep quartile, `--require-verified-transitions` that refuses latents without chain ids or 4-tic spacing, non-finite gradient updates skipped before Adam sees them, pre-clip grad norm and clip fraction logged, single-update parameter ratios probed on the input projection, output layer and adaLN of blocks 0/14/27 every 100 steps, compact bf16 snapshots to Spiderman at every validation, an excursion flag (val above 1.15x the trailing median), bounded remote housekeeping, one-hour process-group timeout, `--action-dropout` exposed, `--keep-remote`. Save-resume tested on a Spiderman A6000 (10 steps, save 10.8 GB recovery checkpoint, resume, continue; lr and schedule continuous). Probe numbers at step 100 from the ImageNet warm start (lr 2e-5, batch 4): adaLN modulation matrices move about 2.8% of their norm per update, the input projection 0.04%; warm start verified to load every adaLN matrix from the checkpoint (RMS 0.10 to 0.20), so this is Adam's normalized step, not a small-norm artifact. Astra's leading hypothesis for the old DiT excursion: an optimization excursion during adaptation, possibly through the adaLN pathway, not bf16; recommendation is to launch with the shared recipe and let the probes tell the story, no DiT-specific lr multiplier without a controlled comparison. IDM rebuilt (VPT structure) with macro recall, majority baseline, probability-summed movement classes, and the real-data reference computed through the identical sequence procedure per horizon. Recorder now seeds every RNG stream from (corpus id, episode id) and stores provenance in the parquet metadata (for a fresh evaluation corpus; not yet recorded). DiT warm-start weights copied to Spiderman. Rohan's decisions pending: shared lr 5e-5 with warmup 2000; action dropout 0; a fresh evaluation corpus of 60 seen-map plus 20 unseen-map episodes excluded from all fitting; the human-study protocol (2AFC real-vs-generated, 60 sources, 4 conditions, 24 raters, 1,440 judgments, primary contrast at 3.2 s); placement of the two corrected runs. Tagged: goal 1.
- 2026-09-10: reviewed single-update probe in 8f8546c against reported A6000 step-100 data (global batch 4, warmup 500). Probe calculation is single-update as intended. About 3% relative adaLN motion increases interest in conditioning-path adaptation but does not establish instability or predict sustained 14% motion at peak LR: Adam moments and weight norms evolve, and launch global batch is 32. Warm start loads pretrained modulation tensors despite zero initialization before loading. Recommend unchanged shared lr 5e-5 / warmup 2000, no 0.1x group multiplier; log step-zero weight/load checks and absolute/functional scales. No training code changed.

- **2026-09-10: Astra narrow launch-gate review of 1106c97 (goal 1).** Pulled (already current). Local CPU AdamW accumulation fault injection (accum=3, NaN in a group spanning epochs) passed: skip at micro=3 leaves step/optimizer/scheduler unchanged and clears grads, valid update at micro=6 advances once. Confirmed `--clip 0` bypasses nonfinite detection because gn is set to zero; skipped counter resets on resume. Probe ratios currently describe displacement over 100 updates, not a single update. Inspected public glample/Arnold master `src/doom/game.py` and `src/model/dqn/base.py`: Game.start constructs a fresh DoomGame and calls init; anti-stuck counters initialize in Game.__init__ but do not reset in start. Therefore seed the fresh engine before init, reset the two counters per episode, and preserve within-episode randomness through deaths. No server commands executed, no full ViZDoom replay verification, no trainer/recorder implementation edits.

- **2026-09-10: Astra follow-up review of 10a81f9 (goal 1).** Pulled (`Already up to date`) and checked the recovery, validation, verified-data, and IDM changes. Independently passed batch-4 versus batch-12 equality for all seven SeededCorruption fields. Accepts 5000-step recovery checkpoints and post-training pixel-IDM audit. Remaining code concerns: only rank 0 RNG is saved/restored by every DDP rank; nonfinite counting observes losses after updates rather than preventing a nonfinite-gradient update; best saves have no coordinated pause on validation-only steps; barriers remain subject to distributed timeouts, prune timeout is uncaught, and rsync has no overall timeout. The existing split builder already separates `unseen_map` from `val`; actual archived split/config must resolve the reported difference. Prior log includes unseen-map evaluation and decoder/WM selection on old val: retrospective subdivision cannot create untouched data. Proposed study: 60 sources, 2 models x 2 durations, 6 ratings/cell, 24 raters x 60 trials, crossed episode/rater uncertainty; primary duration 3.2 s. Proposed fresh test corpus: 60 ID episodes (4/map) plus 20 OOD (10/map), never used for training/selection, new recorded simulator/agent seeds and namespaced IDs. No server commands executed or training code modified; recommendations remain pending.

- **2026-09-10: Astra round-3 independent code review (goal 1; recommendations not yet adopted).** Reviewed the prior rounds, Sep 10 brief, trainer, backbones, velocity diffusion, transition construction/statistics, and windowed IDM/scoring. Local PERSEVE Python checks passed constant-control, death, off-phase control change, override, and weapon-bit transition cases; IDM `(3,8,4,32,40) -> (3,7,29)` and sequence lengths 1/2/7/8/9/16; velocity inversion. Reproduced validation corruption differing with batch 16 versus 32 and local ENOSPC propagating after mocked remote checkpoint success. Code findings: `best_val` is saved but reset on resume; model/EMA checkpoints quantize to bf16 and omit RNG/data position, so resume is not exact; best.pt lacks optimizer state; local logging can still fail on a full disk; remote writes lack an overall timeout and remote retention. Current objective has four-channel v output and no learned variance. IDM checkpoint reference uses individual windows while rollout scoring averages overlapping-window logits; recompute real references on exactly the rollout sequences and report an empirical reference, not an upper bound. Movement probabilities should be summed by group before argmax. `transition_stats.py` window counts undercount by one per eligible chain. No server commands executed and no training code changed.

- **2026-09-10 12:30 EDT: Rohan's decisions and the corrected-data pipeline launched.** Decisions: one latent per agent decision, as GameNGen (action held 4 frames, one prediction per action) and Arnold do; every tic stays in the raw release. VAE stays the SD 1.4 KL-f8 autoencoder with frozen encoder and our MSE+LPIPS decoder, because no pretrained DiT-XL/2 or SD-scale U-Net exists in a newer latent space and GameNGen used the same one; 16-channel latents are the follow-up. Restart both backbones from their published warm starts on verified transitions rather than continuing the 75k/20k checkpoints, so the pair starts equal; the 75k DiT continued on corrected labels is a later ablation. IDM rebuilt in the structure of the VPT inverse dynamics model that MineWorld and Matrix-Game use for controllability (bidirectional window of K=8 latents, shared conv encoder, 4-layer transformer, one head per transition, 11.4M params; `train_idm.py`, `rollout_eval.py` scores whole sequences). Neither GameNGen nor MultiGen measures action following. Superman: the five archived or truncated checkpoints and the pip cache deleted with Rohan's approval, 16 GB free; the trainer now streams every checkpoint to Spiderman (`--remote-results`) and skips local writes on a full disk. Spiderman fit checks: DiT 1.26 steps/s on one A6000 at batch 32, 1.60 on two; U-Net 1.08 steps/s on one A6000 at batch 16. Launched: aligned re-encode (`reencode_aligned.sh`, four shards on GPUs 2 and 3, about 2.3 h) followed automatically by `step2_idm.sh` (verification against `transition_stats.json`, split reuse, windowed IDM on GPU 2). Spiderman's idle GPUs were taken by other users within the hour, so the training launch waits on Rohan and on a free card. Tagged: goal 1.
- **2026-09-10 10:40 EDT: both runs died overnight when Superman's shared root disk hit zero.** At 00:10 the DiT (step 75,000) and the U-Net (step 21,450) both failed while writing checkpoints (`No space left on device`; the DiT's 0075000.pt is truncated, deleted from the Spiderman archive by hand still pending on Superman). Other users' home directories hold about 1.9 TB of the 3.5 TB; ours is 130 GB (Doom 35, lego_project 46, conda 21, perseve 15). Intact and loadable: DiT `best.pt` at step 75,000 (val 0.2259) and U-Net `best.pt` at step 20,000 (val 0.2283); 0070000.pt and the U-Net 0010000/0015000/0020000 are md5-identical to the Spiderman archive. **DiT validation series** (5k to 75k): 0.2552, 0.2478, 0.2387, 0.2341, 0.2300, 0.2320, 0.2283, 0.2321, **0.3047, 0.2817, 0.2731, 0.2677**, 0.2376, 0.2291, 0.2259. The 45k to 60k excursion is a training instability at lr 1e-4 that took 20k steps to recover; the U-Net at lr 5e-5 was monotone (0.2445, 0.2379, 0.2336, 0.2283 at 5k to 20k) and sits at the DiT's 35k value at 20k. **Context sweep done (DiT, 5k steps each):** L=2 0.2602, L=4 0.2583, L=8 0.2532, L=16 0.2525, L=32 0.2519; monotone, diminishing above 8. **64-step rollouts of the 25k DiT** (64 rollouts, LPIPS decoder): PSNR 20.7 at h=1 to 17.3 at h=64, copy-seed 19.7 to 17.1, so the model tracks persistence beyond h=8; LPIPS 0.35 to 0.56; IDM action agreement 0.66 at h=1 falling to about 0.2 by h=48 (ceiling 0.69); FVD 532 at 16 frames, 880 at 32. All under the grid-aligned labels, so the action-following numbers are a lower bound. Nothing has been relaunched; resume and restart scripts are staged on Superman. Tagged: goal 1.
- **2026-09-09 12:10 EDT: action-label audit and the restart question.** `audit_actions.py` on the Arnold recording: 62.2% of decisions start off the tic%4 grid (deaths shift the phase), so **39.1% of stride-4 training windows straddle two actions**; 9.0% of tics carry an override or weapon-switch button vector; 0.6% of windows contain a death. Both backbones train on the same labels, so the paired comparison stands, but the action-following claim does not without a fix. Astra round 2 recommends restarting both runs on verified fixed-duration transitions (thread and positions in `.claude/analyses/astra-review-2026-09-09.md`). Built and unit-tested `transitions.py` (lives, canonical controls, accepted transitions, chains); **Survival on the real Arnold set (`transition_stats.py`, 12:30):** 1,047,351 verified transitions from 1,053,453 stride-4 frames (99.4%), in 7,296 chains over 7,178 lives, median chain 108 transitions (p10 39, p90 277); 818k L=32 windows survive versus about 1,020k on the raw grid. Only 0.17% of tics have movement bits that differ from the action's canonical vector, so the earlier 9% override figure was weapon-switch bits; the correction is almost entirely about aligning windows to real decision boundaries, not about discarding data. Pipeline wired end to end (`encode_parquet.py --align-decisions` stores `chain_id` and real `tic`; `LatentWindowDataset`, `rollout_eval`, `train_idm` never cross a chain; `eval_tf.py` looks up raw frames by stored tic). Also fixed for any relaunch: the scheduler is no longer wrapped by Accelerate (it stepped 4x per update under 4-GPU DDP, so the DiT reached lr 1e-4 at step 150 versus the U-Net's 500), and checkpoints now carry optimizer, scheduler, and best-val state so `--resume` is exact. **Decision for Rohan:** restart both runs on corrected data (about a day of DiT and 10 h of U-Net progress lost; U-Net still done by Sep 13) or keep them and report the noise rate. DiT at 25k with the LPIPS decoder, 256 windows: 20.30 dB / 0.358, copy-last 19.68, HUD 31.6. Tagged: goal 1.

- **2026-09-09 12:30 EDT: Astra review and fixes.** Second-engineer review of the whole stack (`.claude/analyses/astra-review-2026-09-09.md`). Math and layout passed; nine findings, none fatal. Confirmed from the logs: the DiT's warmup was 125 steps (Accelerate scheduler stepping 4x under DDP) versus the U-Net's 500. Evaluation fixes applied (rollout clip assembly for FVD, deterministic validation, IDM movement mapping, inference-noise buckets, accumulation counter, split hash in provenance). Action-label audit of the recording started on Spiderman. **LPIPS decoder final:** 28.34 dB, HUD 32.09, LPIPS 0.051, HUD LPIPS 0.0017 (frozen: 23.69 / 17.93 / 0.095); chosen for all reported numbers. Two-slide Doom status deck for the 1-on-1 at `~/Downloads/doom-status-sep9.pptx`. Tagged: goal 1.

- **2026-09-09 (independent paper-framing review).** Read current plans, paper skeleton, and local evaluation/training code; no remote jobs changed. Recommendation pending Rohan: retain the comparison under either outcome, narrow architecture-only/matched-compute claims because pretraining, parameter counts, native conditioning and execution differ, and spend remaining compute primarily on evaluation and action interventions. Static review found rollout clip export stacks decode chunks as independent clips (default 16 frames, so FVD32 is not actually 32-frame FVD), decoded rather than raw rollout references, and missing episode-level uncertainty. Local results contain April artifacts; current-run numbers were read from this log, not independently remeasured. No experiment or framing decision adopted. Tagged: goal 1.

- **2026-09-09 10:20 EDT (resumed after the break).** All jobs alive; Spiderman's sshd is back (uptime 9 days, no reboot). DiT `010-dit-l32` held-out v-loss: 0.2341 at 20k, **0.2299 at 25k**; teacher-forced at 25k with the fine-tuned decoder, 256 held-out windows: **PSNR 20.38 dB, LPIPS 0.341, latent MSE 0.390, HUD PSNR 31.7** (copy-last 19.73). U-Net at 6,700 (0.23 steps/s after resume). Sweep: L=2 0.2603, L=4 0.2583 at 5k; L=8 running. LPIPS decoder variant at 4.2k of 6.25k on Spiderman. Archiver has 0005000 to 0020000 plus best on Spiderman. **MSE + 0.1 LPIPS decoder after epoch 1:** PSNR 28.18 dB, HUD 31.09 dB, LPIPS 0.052 (frozen 0.095, MSE-only 0.276), HUD LPIPS 0.002. Recommendation for D1: use this decoder for all reported numbers; it trades 0.9 dB PSNR for a 5x better perceptual score. **Raw-frame scoring path validated on Spiderman** (archived DiT best.pt at 25k, MSE decoder, 64 windows, 50 DDIM steps): against the lossless frame PSNR 18.71 dB, LPIPS 0.539, HUD 26.5; copy-last against raw 18.87 dB; VAE ceiling against raw 28.95 dB / 0.284, HUD 34.9. So against raw frames the DiT is still at persistence level at 28% of training, and the decoder's own LPIPS (0.284) is inside every raw LPIPS number until the LPIPS decoder is used. FVD scorer validated (I3D weights on Superman); HF release conversion running on Spiderman (`hf_release_arnold/`); 64x64 rollout of the 25k checkpoint queued on GPU 1. Tagged: goal 1.

- **2026-09-09 08:45 EDT, handoff before a session break.** Technical plan artifact republished with a status layer (section 0 where we sit, section 12 decision ledger, section 13 what Rohan decides next): https://claude.ai/code/artifact/3a3e186d-5dcc-400c-9d26-27417ebfd086. Spiderman's sshd stopped answering at about 08:35 (connection refused); the Superman runs do not depend on it, the archiver retries every 20 min, and the LPIPS decoder job on Spiderman is unmonitored until it comes back. Superman: 6 of 8 GPUs ours, 12 GB free on the root disk, jobs `train-dit`, `train-unet`, `sweep`, `archive`, `pull-vae-lpips`. Tagged: goal 1.

- **2026-09-09 05:00 EDT: April headline reproduced, with its caveat.** The released April checkpoint (`best.pt`, md5 verified, live weights, step 87,200) scored with the rebuilt legacy harness (`eval_metrics.py`, respaced DDPM 50) on 512 held-out windows from 12 Stiegler-agent episodes recorded losslessly and resized to 160x120 (`encode_parquet.py --legacy`): **PSNR 27.43 +- 0.15 dB, LPIPS 0.117 +- 0.003** (decoded target), versus the April 26.04 / 0.153 measured on JPEG training data. The copy-last-frame baseline on that per-tic data is **26.24 dB / 0.141**, so the April task sits 1.2 dB above persistence; on the new stride-4 Arnold data the copy-last baseline is 19.3 dB. Outputs: Spiderman `eval_april_on_stiegler160/`, latents `latents_stiegler_160/` (60 episodes). **Decoder fine-tune (MSE, GameNGen recipe) after epoch 1 of 2 on 2,000 held-out frames:** PSNR 23.69 to 28.95 dB, HUD PSNR 17.93 to 33.40 dB, HUD LPIPS 0.060 to 0.003, but full-frame LPIPS 0.095 to 0.279 (MSE blur on textures). An MSE + 0.1 LPIPS variant is queued (`vae_decoder_arnold_lpips/`) to pick the decoder for the paper. **DiT at step 10,000:** val v-loss 0.2478 (5k: 0.2552); teacher-forced on 256 held-out windows, frozen VAE: PSNR 19.52 dB (5k: 18.97), LPIPS 0.404 (0.431), HUD PSNR 23.4; copy-last 19.33. Above persistence from 10k on; trend is right. **Decoder fine-tune final (2 epochs, 50k frames, MSE):** PSNR 23.69 to 29.11 dB, HUD PSNR 17.93 to 34.68, HUD LPIPS 0.060 to 0.002, full-frame LPIPS 0.095 to 0.276; saved to Spiderman `vae_decoder_arnold/vae` and Superman `weights/vae_decoder_arnold/vae`. **DiT at step 15,000 (val v-loss 0.2387) with the fine-tuned decoder, 256 held-out windows:** PSNR 20.29, LPIPS 0.356, HUD PSNR 30.6; copy-last 19.73. MSE + 0.1 LPIPS decoder variant now training on Spiderman. Unseen maps 16 and 17 at step 15k (fine-tuned decoder, 256 windows): PSNR 20.11, LPIPS 0.427, HUD 28.6, copy-last 19.52; the perceptual gap to seen maps (0.356) is the generalization cost. `plot_results.py` produces the loss and drift figures under `results/figures/` on Superman. **Incident 08:03 EDT:** `011-unet-l32` crashed with CUDA OOM in the first forward after writing its step-5k checkpoint (val v-loss **0.2445**, below the DiT's 0.2552 at the same step): the save cast tensors to bf16 on the GPU with no headroom at 14.8 GB. Fixed in `train_wm.py` (cast on CPU copies, prune before writing, `--resume`); relaunched from `0005000.pt` at 08:12 with a fresh optimizer and warmup (logged). The DiT process still runs the old code but has survived three saves at 14.1 GB peak. Tagged: goal 1.

- **2026-09-09 (06:45 server time) MAIN RUNS LAUNCHED on Superman.** Data: 850 Arnold episodes encoded (1.05M stride-4 latents, 11 GB) synced to `/home/rohan/Doom/data/latents_arnold`; final split `data/split_arnold.json` (maps 16 and 17 held out entirely, 10% of episodes on the rest). `010-dit-l32`: DiT-XL/2, L = 32, velocity, GPUs 4 to 7 via torchrun, batch 8 x 4, 90k steps, 0.86 steps/s (about 29 h), loss 0.30 at step 450. `011-unet-l32`: SD 1.4 U-Net, same recipe, one GPU (GPU 2) with 8-step accumulation because DDP gradient buffers overflowed 16 GB on two GPUs; 0.30 steps/s (about 3.5 days), same fp32 AdamW. IDM training on GPU 1 (8k steps). Checkpoints keep only latest plus best locally (19 GB free) and are archived every 30 min to Spiderman `/sata2/data/rnagabhi/doom/results_superman/`. Decoder fine-tune running on Spiderman (`vae_decoder_arnold/`). Session names: `train-dit`, `train-unet`, `train-idm`, `archive`, `sweep` (DiT context sweep L in {2,4,8,16,32}, 5k steps each, GPU 3) on Superman; `decoder-ft` on Spiderman. **First numbers:** IDM ceiling on real held-out pairs 69.3% top-1 over 29 actions, 71.6% on the movement collapse (`results/idm/`). Frozen `sd-vae-ft-mse` reconstruction of Doom frames: 23.7 dB PSNR full frame, 17.9 dB on the HUD rows, LPIPS 0.095: the VAE ceiling before decoder fine-tuning is far below GameNGen's 29.4 dB, so decoder fine-tuning is load-bearing, not cosmetic. **Early DiT numbers at step 5,000 (5% of training, held-out, 256 windows, decoded-vs-decoded, 50 DDIM steps):** val v-loss 0.255, PSNR 18.97 dB, LPIPS 0.431, HUD PSNR 22.3; copy-last baseline 19.33 dB. Not yet above persistence, expected this early; note the copy-last baseline on stride-4 Arnold data is 19.3 dB versus the strong per-tic baseline on the April set, so the task is genuinely dynamic. Sampling 2 frames/s on one A4000 at 50 steps. Early rollout at step 5,000 (32 rollouts x 32 frames, DDIM 50, GT actions): PSNR 19.2 dB at horizon 1 falling to 16.1 at 32, tracking the copy-seed baseline (19.1 to 16.6); IDM movement agreement 0.59 at horizon 1, about 0.3 beyond, against a 0.72 ceiling. The autoregressive pipeline runs end to end on a trained checkpoint; the numbers are the 5% mark, not results. Outputs archived under `results_superman/010-dit-l32/` on Spiderman. Tagged: goal 1.

- **2026-09-09 (06:10 server time)** Arnold recording complete: 850 episodes, 17 maps x 50, 4.21M tics, 1.05M decision frames, 33.4 h of play, 51 KB per PNG, 202 GB at `/sata2/data/rnagabhi/doom/raw_arnold/`; zero failed episodes; per-map coverage 38% to 89% of 64x64 cells (card in `docs/cards/arnold/`). Stiegler continuity set card in `docs/cards/stiegler/`. Encoding to stride-4 latents about 90% done. Paper skeleton in `paper/`, FVD scorer `fvd.py`, figure script, README rewritten. Next in the chain: `finalize_data.sh` on Superman (sync, split), `launch_main.sh` (DiT on 4 GPUs, U-Net on 2), IDM training, decoder fine-tune on Spiderman. Tagged: goal 1.

- **2026-09-09 (overnight, autonomous)** Arnold recorder `record_arnold.py` (hooks Arnold's own entry point, replays its decision preamble, steps one tic at a time; 29 actions, button vector stored) smoke-tested and launched: tmux `arnold-gen`, 20 CPU workers, 850 episodes x 150 s over the 17 `full_deathmatch` maps, output `/sata2/data/rnagabhi/doom/raw_arnold/` (about 50 KB per PNG, 16 kills per episode). First launch died on a respawn-at-episode-end edge case; fixed and relaunched. Stiegler set complete and separated: `raw_stiegler/`, 1,000 episodes, 4.96M tics, 219 GB. New training stack committed: `diffusion_v.py` (velocity DDPM, DDIM, context noise augmentation), `backbones.py` (DiT-XL/2 and SD 1.4 U-Net behind one interface, warm-start inflation), `train_wm.py` (one recipe, accelerate, fp32 CPU EMA, held-out v-loss selection, fit-check mode), `encode_parquet.py` (stride-4 latents with image-level padding to 320x256), `doom_data.LatentWindowDataset` and `make_split_by_map`, `make_split.py`, `finetune_decoder.py`. **Fit check on Superman (one A4000, L = 32, global batch 32):** DiT-XL/2 0.39 steps/s at batch 4 (11.6 GB) and 0.50 steps/s at batch 8 (11.8 GB); SD 1.4 U-Net 0.30 steps/s at batch 4 (14.8 GB), plain fp32 AdamW for both. Projected: DiT 90k steps in about 13 h on 4 GPUs; U-Net in about 21 h on 4 or 42 h on 2. Superman weights: ImageNet DiT-XL/2-256 and SD 1.4 U-Net fp32 safetensors only (5.8 GB), disk 30 GB free. Encoding of the Arnold set runs on Spiderman as four restartable shards under `encode_loop.sh` (about 170 frames/s total, GPUs shared with other users). **Validated on real data on Superman:** 145 encoded episodes synced (Superman pulls from Spiderman with a dedicated ssh key), provisional split, 60-step DiT run (loss 1.43 to 0.90, held-out v-loss 0.745, checkpoints written), then `eval_tf.py`, `train_idm.py` (42% top-1 over 29 actions after 400 steps), `rollout_eval.py --rollout` and `--score` with IDM all ran end to end. Also added `dataset_stats.py` (Stiegler card: 1,000 episodes, 4.95M tics, 39.2 h, 47 KB per frame, 66% arena coverage), `release/hf_upload.py` and `release/DATASET_CARD.md`, `plot_results.py`, a DoomDiT README, and `/home/rohan/Doom/launch_main.sh` on Superman (DiT on GPUs 4 to 7, U-Net on 2 and 3). Tagged: goal 1.

- **2026-09-09 (early)** Decision from Rohan: follow published work, so the data-collection agent becomes **Arnold** (Lample and Chaplot, AAAI 2017; the agent MultiGen used in 2026), replicating MultiGen's collection recipe. Arnold cloned to Spiderman `/sata2/data/rnagabhi/doom/Arnold`; its five 2017 checkpoints load under torch 2.4.1; three pre-0.4 idioms patched (`third_party/arnold-torch2.patch`). Smoke evaluation, track2 weights on `full_deathmatch.wad` map 2 (a training map) with 8 bots, 900 game-seconds on CPU: 100 kills, 31 deaths, weapon and item pickups, about 140 tics/s. `full_deathmatch.wad` holds 17 maps (Arnold trained on 2 to 5). Native settings are 400x225, HUD off, crosshair on, 108x60 network input, frame skip 4, full action set with doors. Two background evaluations launched: held-out map 8 at native settings, and map 2 at 320x240 with HUD on, to decide the recorder's render settings. Obsidian map generator: Linux needs a cmake/FLTK build, only Windows binaries released; deferred. Stiegler-agent generation continues (660 of 1,000 episodes, 144 GB) as the continuity set. Surveys saved in `.claude/analyses/survey-2026-09-08-agents-data-related-work.md`. Held-out map 8: 73 kills / 46 deaths (generalizes); map 2 at 320x240 with HUD on: 94 / 30 (settings do not hurt). Decisions with Rohan (REQUIREMENTS.md section 7b): follow MultiGen's recipe, velocity parameterization for both backbones with fixed variance, context L = 32 with a DiT sweep over {2, 4, 8, 16, 32}, no map-layout extraction. Tagged: goal 1.

- **2026-09-08 (night)** Data generation started on Spiderman. Env `doom` (python 3.10, vizdoom 1.2.4, sb3 2.3.2, gymnasium 0.29.1, shimmy 1.3.0, torch 2.4.1) under `~/miniconda3/envs`, repro checkout and outputs under `/sata2/data/rnagabhi/doom/`. New `record_episodes.py`: every tic recorded (frame as PNG bytes, action held from this tic, health, ammo, frags, x, y, angle) into per-episode parquet, which is also the release layout. Smoke test: 320x240x3, 18 actions, 4,998 tics per episode, 48 KB per PNG at level 6 (about 240 GB for 5M tics), 110 tics/s per CPU worker (the agent on a busy shared GPU was 46 tics/s, so workers run CPU-only). Full job: tmux `doom-gen`, 20 workers x 50 episodes = 1,000 episodes, seed 1, logs in `/sata2/data/rnagabhi/doom/logs/`. Decision from Rohan: store and release every tic; stride is a training-time choice. Tagged: goal 1.

- **2026-09-08** Scope decision with Rohan: the paper targets GameNGen's protocol (per-frame PSNR/LPIPS at 320x240 on held-out trajectories, plus our autoregressive metrics); MultiGen-style multi-map generalization and memory are future work. Extra maps demoted to P1, unseen-map row dropped, single-arena limitation stated. Checked for existing datasets: none at the needed quality (arnaudstiegler set is JPEG q85 single arena; p-doom/doom-dataset is 10M frames at 60x80 from the same agent; GameNGen and MultiGen released no data), so regeneration is a contribution, not duplication. Superman disk fell to 37 GB free; the sheet's disk plan needs revisiting or Spiderman. Later the same day: finalized the one-paragraph project goal (backbone is the only variable between our two rows; GameNGen is a reference point, not a matched baseline), fixed R1.1 to generate-and-encode in one pass, reset the go/no-go dates, and wrote M1 (data and fit check, Sep 9 to 11) as `REQUIREMENTS.md` section 9. Evening: Spiderman inventory (7.3 TB free on /sata2/data, writable; all four A6000s busy with ruixuanl and boweili; 64 cores) fixed the server split: generate, store, encode, and release on Spiderman, train and evaluate on Superman. Checked public Doom datasets: none usable (p-doom 60x80, arnaudstiegler JPEG); regeneration plus an HF release is a contribution. Wrote `TECHNICAL_PLAN.md` (VAE choice, both backbones, conditioning, recipe, rollouts, evaluation, servers, 14 decisions) with a rendered artifact. No code changes since Sep 2. Tagged: goal 1.

- **2026-09-02** Wrote `REQUIREMENTS.md` (requirement sheet for the CoRL submission: data regeneration at 320x240 lossless with at least three maps and stride 4, decoder fine-tuning for the HUD per GameNGen, matched U-Net vs DiT with noise augmentation on both, evaluation protocol with copy-last and VAE ceiling, compute and go/no-go dates, recent-work list). Facts learned: the HF dataset is JPEG q85 from `deathmatch_simple.wad` with the PPO agent in arnaudstiegler/gameNgen-repro, every tic recorded with the action held 4 tics, 18 legal button combinations; GameNGen evaluates 2,048 trajectories over 5 levels at 320x240 with 64-frame context and fine-tunes the VAE decoder for the HUD. Venue slide and deadline slide made for the Sep 2 1-on-1. Tagged: goal 1.
- **2026-09-01** Kickoff session. Superman inventory: April project directory deleted, only conda env and logs remain; disk 98 GB free. Google Drive latent links now require login. Fixed `eval_checkpoint.py` (`ema_state`) and moved checkpoint/VAE loading into `doomdit_utils.py`. Added `doom_data.py` (per-episode dataset, episode split, legacy-segment mapping; verified equal to `build_dataset.py` layout), `encode_episodes.py` (Hugging Face parquet to per-episode latents, restartable, unit-tested), `eval_metrics.py` (PSNR, LPIPS, latent MSE, copy-last baseline, VAE ceiling; CPU end-to-end test passed). Wrote the Keerthana ask and the autoregressive eval design. Findings that change the plan: headline was scored on training data and the checkpoint saw all episodes, so both backbones must be retrained on the split; sampling is respaced DDPM not DDIM. Deadline working assumption: CoRL PhysWM Sep 30. Tagged: goal 1.

## 8. Technical facts most likely to matter (full detail in the agent context brief)

- Objective check against the papers (Sep 14): GameNGen trains v-prediction with an L2 loss on a linear schedule, DDIM sampling, actions as one cross-attention token, MSE decoder fine-tune; MultiGen trains the standard velocity objective with actions through cross-attention and context frames on channels; the original DiT trains eps-prediction plus a variational term on a learned variance, which we disable. Our loss therefore matches both Doom papers exactly and differs from DiT's own paper by design.

**As built, Sep 14 (read from the code; supersedes the older bullets below where they disagree).** Third backbone (Sep 14): PixArt-alpha 512 transformer, 611.6M, same 132-channel input, action + noise bucket as two cross-attention tokens through the pretrained caption projection, timestep via adaLN-single, epsilon half of the 8-channel head retrained as velocity; recipe identical to the pair. Freedoom assets on ViZDoom, Arnold's deathmatch maps 1 to 17, 8 bots, crosshair on, 29 actions held 4 tics. Corpus 850 episodes; split 675 / 75 / 100 (maps 16, 17); fresh seeded reporting corpus 60 + 20 episodes. Training frames are verified transitions (1,054,647 in 7,296 chains). Latents `sd-vae-ft-mse`, 320x256 padding, 4x32x40, fp16; decoder fine-tuned MSE + 0.1 LPIPS. Input: 32 context latents (128 ch) + noisy target (4) = 132 channels, action of the transition into the target, noise bucket. DiT-XL/2 (675M, adaLN-Zero on t + action + bucket, learned variance off, 4-ch output, ImageNet-256 warm start); SD 1.4 U-Net (860M, action as one cross-attention token, bucket via class embedding). Objective: velocity prediction, MSE on v, linear betas 1e-4 to 0.02 over 1,000 steps, uniform t. Context noise augmentation U(0, 0.7) with 10 buckets; inference context clean. Action dropout 0, no CFG. AdamW lr 5e-5, warmup 2,000, wd 0, clip 1.0, global batch 32, bf16 autocast with fp32 master, fp32 EMA 0.9999 (diagnostic), 90k updates, seed 0. Validation every 1k on 1,024 per-window-seeded held-out windows; recovery checkpoints every 5k on Spiderman. Sampling DDIM 50, eta 0. Metrics: teacher-forced PSNR / LPIPS / HUD PSNR vs raw frames on 2,048 windows (seen and unseen corpora), copy-last and VAE ceiling; 256 x 64 rollouts with drift, copy-seed, FVD 16 / 32; VPT-style IDM (K=8, 82.9% real reference) with matched real-data reference per horizon and a K=2 check. Same table lives in the plan artifact, section T.

- Dataset scale for the paper's data section (checked against the papers Sep 10): GameNGen trains on a random subset of 70M examples from 50M environment steps of agent play at 320x240 padded to 320x256, context 64. MultiGen (arXiv 2603.06679) collects over 10M frames with the Lample and Chaplot agent on 100 Obsidian-generated maps for level design, and over 10M frames of 1-vs-4 deathmatch on a single map for multiplayer; it does not state resolution, frame skip, train/test map split, base model, steps, batch, or sampler. Its context ablation: L=2/4/8/16/32 gives PSNR 27.6/29.5/29.8/29.8/30.0 and LPIPS 0.121/0.097/0.094/0.093/0.089, diminishing above 8. Ours: 1.05M decision frames (4.2M tics) on 17 maps, so 10x smaller than MultiGen and 70x smaller than GameNGen in frames; the recorder is restartable if more is wanted.
- Training windows in the current runs (010, 011, 020) sit on the tic%4 grid; 39% of them straddle a decision boundary. The corrected encoding (`--align-decisions`) keeps 99.4% of frames and gives every window a single verified 4-tic action; nothing has been re-encoded or relaunched yet.
- Latents from `stabilityai/sd-vae-ft-mse`, frames 160×120 → (4, 15, 20), height padded to 16, 80 tokens at patch 2.
- Context by channel concatenation: 4 past latents (16 ch) + noisy target (4 ch) → `in_channels=20`; `learn_sigma=True`.
- Action via repurposed `LabelEmbedder` (18 VizDoom actions), `c = t_emb + action_emb` → adaLN-Zero; 10% dropout retained; CFG available but unused.
- Data: HF `arnaudstiegler/vizdoom-500-episodes-skipframe-4-lvl5`: 97 parquet shards, 48 GB, 2,470,229 rows of (episode_id, JPEG frame, action, health, step_id), rows grouped by episode. 500 episodes minus 4 windows each gives the N = 2,468,229 of the old consolidated arrays. `doom_data.EpisodeWindowDataset` indexes per-episode files directly; `build_dataset.py` is kept only for the legacy layout.
- Recipe: global batch 32, fused AdamW lr 1e-4, wd 0, warmup 500, grad clip 1.0, bf16, DDPM 1000 linear; in-training and eval sampling is respaced ancestral DDPM at 50 steps via `p_sample_loop` (the code and docs call it DDIM; `--sampler ddim` in `eval_metrics.py` is the true DDIM); EMA 0.9999 in fp32 math.
- Companion projects: `~/Documents/Github/perseve` (adversarial SDG) and `~/Documents/Github/Lego_Manipulation` (LEGO seg + pose). The fall plan's goal 3 carries this world model into LEGO trajectory decisions using the Isaac Sim pipeline from perseve.
