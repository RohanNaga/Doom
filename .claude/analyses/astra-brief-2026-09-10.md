# Astra round 3 brief, Sep 10 2026, 13:00 EDT

You reviewed this project twice on Sep 9 (thread continues). Since then: the action-label audit you asked for came in, the verified-transition construction you recommended is built and running, both training runs died when Superman's disk filled, and the compute picture changed. Rohan wants you paired on every design decision from here to the Sep 30 submission. Read the repo state yourself; do not trust my summaries where a file exists.

## Where things stand (facts, verifiable in the repo)
- Repo: /Users/rohan/Documents/Github/Doom (branch main, head 2aacfc5). Read RESEARCH_CONTEXT.md section 7 top three entries and section 8 first two bullets first.
- Verified transitions: transitions.py (unit-tested), transition_stats.py; on the real 850-episode set 99.4% of stride-4 frames survive, 7,296 chains, median chain 108 transitions; only 0.17% of tics have movement bits off the action's canonical vector (the 9% "override" figure was weapon-switch bits). Numbers in raw_arnold/card/transition_stats.json on Spiderman; I can relay.
- Pipeline wired: encode_parquet.py --align-decisions stores chain_id and real tic; doom_data.LatentWindowDataset, rollout_eval.collect_rollout_windows, train_idm.WindowDataset never cross a chain; eval_tf.py looks up raw frames by stored tic.
- Trainer fixes: scheduler no longer wrapped by Accelerate (it stepped 4x per update under 4-GPU DDP, so the DiT hit lr 1e-4 at step 150 vs the U-Net's 500); checkpoints carry optimizer, scheduler, best_val; --resume is exact; save_checkpoint streams every checkpoint to Spiderman over ssh and skips the local write when the disk is nearly full (train_wm.py).
- Dead runs (grid-aligned labels, unequal warmup): DiT val v-loss 5k..75k = 0.2552 0.2478 0.2387 0.2341 0.2300 0.2320 0.2283 0.2321 0.3047 0.2817 0.2731 0.2677 0.2376 0.2291 0.2259. U-Net 5k..20k = 0.2445 0.2379 0.2336 0.2283, lr 5e-5. Context sweep (DiT, 5k steps): L=2/4/8/16/32 = 0.2602 0.2583 0.2532 0.2525 0.2519. 25k DiT rollouts (64 x 64 steps, LPIPS decoder): PSNR 20.7 at h=1 to 17.3 at h=64, copy-seed 19.7 to 17.1; IDM top-1 0.66 at h=1 falling to about 0.2 by h=48 against a 0.69 ceiling; FVD 532 (16 frames), 880 (32).
- Compute: Superman 6 idle A4000 16 GB, 16 GB disk free on a shared root disk that other users fill; Spiderman 4 A6000 48 GB, all currently running other users' jobs with 10 to 30 GB free each, 6.6 TB data disk. Fit checks on an A6000: DiT-XL/2 1.26 steps/s at batch 32 (10.8 GB), 1.60 on two cards; U-Net 1.08 steps/s at batch 16 x accum 2 (15.6 GB). On A4000s: DiT 0.93 steps/s on four cards, U-Net 0.28 on one.
- Running now on Spiderman: the aligned re-encode (done about 14:40 EDT), then automatically the new IDM.
- Decided by Rohan today: one latent per agent decision (GameNGen and Arnold both hold an action 4 tics); keep the SD 1.4 KL-f8 VAE, frozen encoder, our MSE+LPIPS decoder (28.3 dB, HUD 32.1, LPIPS 0.051); restart both backbones from their published warm starts on corrected data rather than continuing 75k/20k; IDM rebuilt in the VPT structure.
- Literature checked today: GameNGen trains on 70M examples from 50M env steps, 320x240 padded to 320x256, context 64, PSNR 29.43, LPIPS 0.249, FVD 114/186; MultiGen (2603.06679) 10M+ frames on 100 Obsidian maps with Arnold, context ablation L=2..32 PSNR 27.6/29.5/29.8/29.8/30.0, states no resolution, steps, batch, base model, or sampler; neither measures action following. MineWorld and Matrix-Game score controllability with VPT's IDM (90.6% keypress accuracy).

## Open questions: give me your approach before I give you mine
1. Relaunch recipe. The DiT's 45k-60k excursion at lr 1e-4 (took 20k steps to recover) versus the U-Net's monotone curve at 5e-5. For the corrected relaunch, what would you change, ranked by expected gain per GPU-hour, under the constraint that the pair must share every hyperparameter you cannot justify as backbone-native? Candidates I have not evaluated: lr, warmup length, EMA-evaluated validation, grad-clip, batch, bf16 numerics in adaLN, the learned-variance head, timestep sampling.
2. IDM design for the action-following metric. Read train_idm.py (IDM class, WindowDataset, predict_sequence) and the scoring block in rollout_eval.py. Is the windowed bidirectional design the right one for a 29-action Doom space at 1M frames, and what would make its ceiling on real data meaningful rather than a number we apologize for? Consider: window size, whether to predict from latents or decoded pixels, class imbalance, movement-only grouping, and how to report agreement relative to the ceiling.
3. Data scale. We hold 1.05M decision frames (4.2M tics) on 17 maps, 10x below MultiGen and 70x below GameNGen; the recorder is restartable and one overnight run adds 850 episodes for about 12 CPU-hours plus 2 GPU-hours of encoding. Is more data the best use of the next 20 days, or is it better spent on seeds and ablations? Also: is there any value in using the three intermediate tics per decision as phase-shifted windows?
4. Placement. Spiderman's A6000s are shared with no posted rule; Superman's A4000s are idle but its disk is hostile. Where would you run the corrected DiT and U-Net, and what would you do to protect the runs?
5. Anything I have not asked about that you would change before the runs start. The runs are the last chance to change training-time choices before Sep 30.

## How to check things
- Unit tests: python -c "import transitions" plus the tests in the Sep 9 thread; train_idm shape test: instantiate IDM(29,32,8,2,4) on torch.randn(3,8,4,32,40) -> (3,7,29).
- Fit checks: train_wm.py --fit-check 30 on either server (I relay).
- Server commands: give me the exact command and I run it and paste output verbatim.

Reply with your approach to 1 to 5 first; then I will put mine on the table and we compare.
