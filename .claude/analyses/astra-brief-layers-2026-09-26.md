# Brief for Astra: where do physics and appearance live in the world model, and is a middle-layer post-training worth it? (Sep 26 2026, 17:30 EDT)

Repo: /Users/rohan/Documents/Github/Doom (main). Read RESEARCH_CONTEXT.md section 0 and the entries from 2026-09-26 16:50 EDT on, then docs/RESEARCHER_DOSSIER.md sections 3.1 to 3.3 (channel-stacked latents, controls as tokens), 4 (evaluation instruments, especially the directional check), 6 (closed-loop stability). Code: backbones.py (control tokens per backbone: cross-attention for the U-Net and PixArt, joint attention plus a pooled control vector for SD 3.5), directional_check.py, eval_tf.py, diffusion_v.py.

Context. Three backbones (SD 1.4 U-Net, PixArt-alpha, SD 3.5 medium) trained as Doom world models on four maps; evaluation on 30 maps. Rohan's hypothesis: the engine physics (what a control does to the camera) is identical on every map, appearance differs, so failures on unseen maps should be appearance, not physics. His co-author Keerthana, from LLM post-training work, asks: in LLMs early layers hold syntax, middle layers context, last layers the prediction, and feeding middle-layer features into the prediction improves it; by analogy, are physics and background learned in early or middle blocks of our backbones, and would incorporating those features into the final prediction improve next-frame prediction? Deadline: CoRL PhysWM workshop, Sep 30; the draft goes to the advisor Sunday evening; GPUs: one A6000 free in gaps (GPU 3) this weekend; GPU 1 busy tonight with decoder tunes; GPU 2 free from Monday. Rohan's rule: default recipes unless we agree on something that sounds really good.

Propose before you critique, and do not assume main's design (it is withheld on purpose): (1) how would you test where physics (control-to-motion) and appearance are represented in these diffusion backbones, cheaply, this weekend, with the instruments we have (the directional check gives a physics score per checkpoint; LPIPS and PSNR against persistence give appearance); (2) is the LLM layer analogy the right axis for a diffusion world model, or is another axis (denoising timestep, control pathway, resolution level) more informative; (3) is a middle-layer-to-output post-training (a learned skip or auxiliary head from middle blocks into the final prediction, then a short fine-tune) worth a GPU-day before Wednesday, and if so the minimal design; (4) what would you present in a four-page paper from this. Then, after your proposal, critique what a strong reviewer would say. Do not touch any server; you may write scratch under /tmp. Report with file and line where you cite code.
